name: CI

on:
  push:
  pull_request:

# A branch with an open PR triggers both push and pull_request runs — the
# price of seeing breakage on PR-less branches (a red bandit gate from #80
# lived in main for half a day because branch pushes weren't built). Rapid
# re-pushes to the same ref cancel the stale run; pushes to main never cancel.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Lint (ruff)
        run: python -m ruff check app/ tests/ tools/ main.py

      - name: Type check (mypy)
        run: python -m mypy app/

      - name: Run unit tests (with coverage)
        run: |
          python -m coverage run -m unittest discover tests -v
          python -m coverage report

      - name: Security scan (bandit)
        run: python -m bandit -c pyproject.toml -r app tools main.py -x tests --severity-level medium --confidence-level medium

      - name: Dependency scan (pip-audit)
        run: python -m pip_audit -r requirements.lock

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t pepe-bot:latest .
