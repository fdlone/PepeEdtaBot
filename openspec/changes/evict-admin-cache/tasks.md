## 1. Политика вытеснения

- [ ] 1.1 Задать пределы рядом с `ADMIN_CACHE_TTL_SECONDS` в `app/filters/admin_or_owner.py`: срок хранения записи и максимальное число записей — в том же стиле, что `state_ttl_sec` / `state_max_keys` у `ThrottlingMiddleware`
- [ ] 1.2 Реализовать чистку по ходу обращения к кэшу: удаление протухших записей и вытеснение самых старых при переполнении
- [ ] 1.3 Сверить порядок операций с `ThrottlingMiddleware._prune_state`, чтобы паттерн читался одинаково в обоих местах
- [ ] 1.4 Убедиться, что чистка не выполняется на каждом обращении, если это заметно дороже самой проверки (у соседа для этого есть счётчик тиков)

## 2. Тесты

- [ ] 2.1 Протухшая запись удаляется, а не только перезаписывается
- [ ] 2.2 При переполнении вытесняются записи с самым давним обращением
- [ ] 2.3 Недавно использованный чат не вытесняется
- [ ] 2.4 После вытеснения права запрашиваются заново и решение о доступе не меняется
- [ ] 2.5 Ошибка запроса прав по-прежнему означает отказ (fail-closed)

## 3. Закрытие

- [ ] 3.1 Прогнать полный сьют, `ruff`, `mypy`
- [ ] 3.2 Перенести пункт из `docs/OPEN.md` в `docs/CLOSED.md`
