schema: spec-driven
created: 2026-08-05
# Требование «числовая настройка ограничена с обеих сторон» уже введено
# спекой runtime-knob-validation (change add-knob-upper-bounds). Здесь оно
# лишь распространяется на оставшиеся поля — нового поведения на уровне
# контракта нет, поэтому дельта-спеки нет.
skip_specs: true
