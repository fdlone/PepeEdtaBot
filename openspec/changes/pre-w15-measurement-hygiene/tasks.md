## 1. Счётчик режима ответа (M3R-140.1)

- [x] 1.1 `GenerationTelemetry`: поля `ctx_generations` / `noctx_generations` и метод `note_context_mode(*, with_context: bool)`; агрегат `ctx_generation_share` в `snapshot()` (`None`, пока генераций нет)
- [x] 1.2 `ResponseGenerator`: вызов на каждую генерацию по `request.context_tokens` (D1), ровно один на генерацию — включая путь, где ответ не собрался
- [x] 1.3 `/stats` (`app/presentation/bot_messages.py`, вызов из `app/handlers/common.py`): строка с долей ответов, собранных с контекстом
- [x] 1.4 Тесты: обе ветки режима, отсутствие второго инкремента при пустом ответе, `None` до первой генерации

## 2. Наблюдаемость пустых каналов (M3R-141)

- [x] 2.1 `GenerationTelemetry`: три пары «спросили / данных нет» по таблице D3 плюс их агрегаты в `snapshot()`
- [x] 2.2 Seeded: «спросили» и «корпус пуст» отмечаются **внутри `rank_seeds`**, в той самой ветке `n_docs <= 0` (уточнение D3 при реализации: сигнатура не меняется, второго чтения `n_docs` не появляется, а знание о том, *какая именно* пустота случилась, не покидает место, где оно есть)
- [x] 2.3 L1 hot: в `ReplyPipeline._hot_ngram_seed` отметить «спросили» на каждом розыгрыше, дошедшем до `get_hot`, и «пусто» на пустом ответе
- [x] 2.4 Контекст: признак сброса собирается в `ResponseGenerator` (флаг генерации), а **не** полем `GenerationTrace` — трасса пер-попыточная и строится в `markov.py`, который про бюджет попыток вызывающего не знает; спека требует записи в трассе (2.5), а не поля в датаклассе
- [x] 2.5 `gen_trace_log.log_context_dropped`: строка события в опциональной трассе, один раз на генерацию, на первой попытке без контекста
- [x] 2.6 Счётчик «контекст отброшен» на генерацию (D2), знаменатель — ctx-генерации из задачи 1.1
- [x] 2.7 Тесты: для каждой пары — «спросили и ответили», «спросили и пусто», «не спрашивали» (числитель и знаменатель нули, доля `None`)

## 3. Двухрежимный прогон eval (M3R-140.2)

- [x] 3.1 `tools/eval/__main__.py`: флаг `--context-mode {ctx,noctx}` (дефолт `ctx`), протаскивание в раннер
- [x] 3.2 `tools/eval/run.py`: в режиме `noctx` запрос уходит с пустыми `context_tokens`; выбор промптов и сиды не меняются (D4). **Правка плана:** `current_message_normalized` остаётся заполненным в обоих режимах — он кормит гейт эха и якоря seeded, к режиму отношения не имеющие; обнуление выключило бы seeded-канал и подменило бы «другой вход» на «другую машину»
- [x] 3.3 `tools/eval/report.py`: режим в заголовке отчёта, в JSON-выводе и в имени файла отчёта
- [x] 3.4 Пороги/гейты: `requires_both_modes: true` у `phase9_interp` + общий даунгрейд в `evaluate_gates` — гейт печатает свои числа, но вердикт становится `insufficient data`; правило действует только на гейты, объявившие его (закрытые фазы не переоткрываются)
- [x] 3.5 Тесты: дефолт `ctx` воспроизводит прежние числа; в `noctx` ни одна генерация не получает контекстных токенов; пара прогонов с одним сидом берёт одинаковые промпты в одном порядке

## 4. Приёмка и документация

- [x] 4.1 `python -m tools.generation_hash --synthetic --check` — хеш не сдвинулся (заявлено в proposal; расхождение = дефект правки)
- [x] 4.2 `ruff`, `mypy app/`, полный `unittest`, покрытие не ниже храповика (`fail_under = 87`)
- [x] 4.3 `python -m tools.eval --smoke` зелёный (смок остаётся однорежимным)
- [x] 4.4 `docs/GENERATION_MAP.md`: §3.1, §3.2 и §1.3 отмечены закрытыми с указанием счётчиков; реестр наблюдаемости обновлён по §5 карты
- [x] 4.5 `docs/v2/05_MARKOV_2_0R_EVAL_PROTOCOL.md`: раздел о двух режимах и правиле «один режим ⇒ гейт не снят»
- [x] 4.6 `docs/PRE_ROADMAP.md`: M3R-140/141 отмечены сделанными; правило бюджета noctx привязано к появившемуся счётчику
- [x] 4.7 Заметка в `docs/eval_reports/` не заводится (вердикта нет); вместо неё — строка в `docs/OPEN.md` о том, что доля режима станет известна после следующего рестарта прода
