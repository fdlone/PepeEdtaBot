## 1. Предрегистрация порога (первым делом, до кода)

- [x] 1.1 Зафиксировать в `tools/eval/eval_thresholds.yaml`, блок
      `phase5_promotion`, два ключа — `n_docs_min: 500` и
      `df_singleton_share_max: 0.60` — с обоснованием на месте (D3: разброс IDF
      для первого, признание слабости планки для второго) и датой регистрации.
      Проверка: ключи читаются `yaml.safe_load`, комментарий называет, почему
      частей две и почему 0.60 — слабейшая из них.
- [x] 1.2 Записать в журнал изменения ожидание по `generation_hash` **до**
      правок: сдвига нет ни на одном снимке (путь генерации не затрагивается).
      Проверка: файл `hash-log.md` в change'е содержит ожидание и правило
      «расхождение останавливает работу, ожидание не переписывается».
- [x] 1.3 Снять контроль `generation_hash` до правок на обоих снимках.
      Проверка: `python -m tools.generation_hash --synthetic --check` даёт
      `baseline OK`; значение прод-копии записано в `hash-log.md`.

## 2. Чтение корпуса до его модификации

- [x] 2.1 Прочитать из **исходного** файла БД, до копирования снапшота и до
      `_populate_token_df`, две величины для чата, который вернёт
      `pick_chat_id`: `n_docs` из `chat_model_volume` и долю
      `messages_seen = 1` в `markov_token_df`. Проверка: тест на копии-фикстуре
      с заранее известными числами возвращает их, а не размер удержанного окна.
- [x] 2.2 Тест на саму ловушку (Context §2 дизайна): после `_populate_token_df`
      значения в рабочей копии отличаются от прочитанных на шаге 2.1, и в
      вердикт идут прочитанные. Проверка: тест падает, если чтение перенести
      после подготовки df.
- [x] 2.3 Пустой `markov_token_df` у чата даёт «df пуст», а не деление на ноль.
      Проверка: тест на чате без строк df.
- [x] 2.4 Ошибка чтения исходной БД (нет файла, нет прав) даёт
      `insufficient data` с причиной, а не падение прогона. Проверка: тест с
      недоступным путём.

## 3. Предусловие в вердикте гейта

- [x] 3.1 В `_phase5_arm_verdict` добавить обе части предусловия в тот же
      список `missing`, которым уже обрабатывается оконный df. Проверка: тест —
      при недоборе объёма вердикт `insufficient data`, причина называет порог и
      измеренное число.
- [x] 3.2 Недобор по форме при пройденном объёме даёт `insufficient data` с
      причиной, называющей потолок и измеренную долю. Проверка: тест.
- [x] 3.3 Отсутствие любого из двух ключей в файле порогов даёт
      `insufficient data`, а **не** дефолт из кода (D4). Проверка: тест с
      блоком `phase5_promotion` без новых ключей.
- [x] 3.4 Оба условия выполнены ⇒ предусловие снято, вердикт считается по
      остальным предрегистрированным условиям. Проверка: тест на фикстуре,
      где объём и форма проходят.

## 4. Отчёт

- [x] 4.1 Печатать обе величины и оба порога рядом с вердиктом фазы 5.
      Проверка: тест на текст отчёта — присутствуют измеренные числа, а не
      только слова `insufficient data`.
- [x] 4.2 Проверить на живой копии прода, что «ещё рано» читается без обращения
      к БД. **Отклонение от ожидания задачи, зафиксировано при выполнении:**
      копия в репозитории старше замера 24.08 (n_docs=0, df пуст), поэтому
      прогон напечатал `prod corpus: n_docs 0 (floor 500), singleton share
      df empty (ceiling 60%)` и `insufficient data` с обеими причинами — тот
      же код-путь и то же читаемое «ещё рано», но числа 35/0.81 на этой копии
      невоспроизводимы; заодно на живых данных проверен путь «df пуст»

## 5. Спека и документация

- [x] 5.1 Синхронизировать `openspec/specs/generation-eval/spec.md` с дельтой
      этого change'а. Проверка: `openspec validate --change
      gate-phase5-ndocs-floor` без ошибок.
- [x] 5.2 Убедиться, что `docs/OPEN.md` (строки M2R-3, M2R-4 и раздел «Почему
      окно накопления df не измеряется неделями») описывает уже реализованное
      предусловие, а не намерение. Проверка: в файле нет календарной
      формулировки блокера.
- [x] 5.3 Отразить предусловие в `docs/v2/00_STATUS.md`, строка фазы 5: гейт
      ждёт не «накопленного df», а порога. Проверка: строка называет оба числа.

## 6. Закрытие

- [x] 6.1 Снять `generation_hash` после правок на обоих снимках и сверить с
      ожиданием из 1.2. Проверка: совпадение; расхождение останавливает работу
      и разбирается.
- [x] 6.2 Полный сьют, `ruff check app/ tests/ tools/ main.py`, `mypy app/`.
      Проверка: всё зелёное.
- [ ] 6.3 Открыть PR, дождаться зелёного CI, смержить. Проверка:
      `gh pr checks` перед мержем.
