# Tasks — l1-hot-route (M3R-230)

## 1. До правок

- [x] 1.1 `hash-log.md`: ожидание «сдвига нет» при дефолте 0

## 2. Маршрут

- [x] 2.1 `CandidateRoute.HOT`; ручка `hot_ngram_slot_ratio` (реестр,
      `RuntimeTunables`, `.env.example`)
- [x] 2.2 Бюджет и розыгрыш n-грамм (D2, D3), первые попытки цикла с затравкой
      маршрута (D1), атрибуция `hot`, `attempted` при бюджете > 0
- [x] 2.3 Тесты: ratio 0 — ни розыгрыша, ни чтения; два слота — две разные
      n-граммы, пул ≤ target, обход присутствует; контекст — маршрут не
      запускается; пустой отбор — attempted без present; отклонённая
      hot-попытка отдаёт место обходу; RNG-нейтральность при 0

## 3. Замер

- [x] 3.1 `matrix_l1_route_grid.yaml` (C0, C7r20, C7r40 при 2 / 0.25); прогон в
      обоих режимах на прод-копии 01.09; вердикт-заметка

## 4. Приёмка

- [ ] 4.1 `ruff`, `mypy app/`, полный сьют, покрытие
- [x] 4.2 `generation_hash` на обоих снимках, факт в `hash-log.md`
- [x] 4.3 Роадмап (M3R-230), карта §1.4/§1.5/§2.1, пайплайн §4, CLOSED/OPEN
