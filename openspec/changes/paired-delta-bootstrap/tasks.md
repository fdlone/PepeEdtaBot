## 1. Парный бутстрап

- [x] 1.1 Добавить в `tools/eval/bootstrap.py` функцию парной оценки дельты:
      ресэмплировать индексы наблюдений один раз и брать обе половины пары по
      этому индексу. Проверка: на синтетических коррелированных данных парный
      интервал уже непарного, точечная оценка совпадает.
- [x] 1.2 Отбрасывать неполные пары (арм не дал ответа). Проверка: тест на
      входе, где часть наблюдений односторонняя.
- [x] 1.3 Перевести `delta_ci` на парную оценку и убедиться, что все её
      вызывающие получают пары, а не два независимых списка. Проверка:
      `tests/test_eval_protocol.py` зелёный.

## 2. Пары в метриках

- [x] 2.1 Проверить, какие метрики фильтруются по `record.success`, и дать им
      форму, из которой пара восстанавливается по индексу записи. Проверка:
      тест на метрике с фильтром — пары строятся по записи, а не по позиции в
      усечённом списке.

## 3. Отчёт и протокол

- [x] 3.1 Пометить в отчёте, что интервал дельты парный. Проверка: строка
      видна в выводе `python -m tools.eval --smoke`.
- [x] 3.2 Записать разрыв сопоставимости в
      `docs/v2/05_MARKOV_2_0R_EVAL_PROTOCOL.md`: дата, причина, и что прошлые
      отчёты не пересчитываются. Проверка: запись на месте и датирована.
- [ ] 3.3 Измерить ρ по уже накопленным записям хотя бы на одном арме и
      записать фактический коэффициент раздувания рядом с записью о разрыве —
      чтобы масштаб эффекта был числом, а не оценкой из формулы. Проверка:
      число в документе.

## 4. Приёмка

- [ ] 4.1 `ruff` + `mypy app/` + полный `unittest` зелёные, покрытие ≥ 87.
- [ ] 4.2 `python -m tools.eval --smoke` проходит.
- [ ] 4.3 `python -m tools.generation_hash --synthetic --check` — хеш не
      сдвинут (генерация не затрагивается).
