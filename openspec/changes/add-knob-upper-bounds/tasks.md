## 1. Подготовка

- [x] 1.1 Сверить действующий `.env` и `.env.example` с диапазонами из `design.md` (D3) — убедиться, что ни одно живое значение не выпадает
- [x] 1.2 Проверить, что ни один тест не задаёт этим пяти ручкам значения вне новых диапазонов через реестр

## 2. Границы

- [x] 2.1 `typing_max_ms` → `_int_in_range(0, 15000)`
- [x] 2.2 `min_cooldown_sec` → `_int_in_range(0, 3600)`
- [x] 2.3 `min_tokens_for_model` → `_int_in_range(0, 1000000)`
- [x] 2.4 `reply_burst_boost_sec` → `_int_in_range(0, 3600)`
- [x] 2.5 `reply_burst_suppress_sec` → `_int_in_range(0, 3600)`
- [x] 2.6 Прокомментировать в реестре, почему у `typing_max_ms` потолок именно такой (связь с `TYPING_HARD_CAP_MS` и пиннящим тестом)

## 3. Тесты

- [x] 3.1 На каждую из пяти ручек: значение на границе принимается, значение за границей отвергается
- [x] 3.2 Отказ приходит одинаково из `/set` и из `load_settings`, и в обоих случаях в тексте назван диапазон
- [x] 3.3 Убедиться, что `test_cap_never_cuts_below_configured_max` остался зелёным и по-прежнему достижим через легальный конфиг

## 4. Документация и закрытие

- [x] 4.1 Отразить диапазоны в `.env.example` у этих пяти переменных
- [ ] 4.2 Прогнать полный сьют, `ruff`, `mypy`
- [x] 4.3 Закрыть пункт в `docs/OPEN.md` → `docs/CLOSED.md` с явной оговоркой, что ревизия точечная (пять ручек), а не сплошная
