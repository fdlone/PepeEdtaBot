# Tasks: pre-w1c-seeded-ordering-and-routes

Порядок блоков задан протоколом перезаморозки хеша (design.md D5): блок 1 —
контрольное измерение, блок 2 — единственная правка, которой позволено сдвинуть
хеш, блоки 3–4 — правки, которые сдвинуть его не должны, и это проверяется.
Блок 2 нельзя менять местами с блоками 3–4: тогда атрибуция сдвига теряется.

## 1. Контрольная точка

- [x] 1.1 Записать `generation_hash` на текущем `HEAD` для замороженного снимка и для синтетического снапшота (оба значения, с ревизией) — это база, относительно которой атрибутируются шаги 2 и 3 — `hash-log.md`, шаг 1: прод-копия `13e496c0…`, синтетика `555f552a…` (`baseline OK`), ревизия `126b158`
- [x] 1.2 Зафиксировать в рабочей заметке **ожидание** до правок: шаг 2 сдвигает хеш, шаг 3 — нет. Записать до прогона, чтобы результат нельзя было перечитать задним числом — `hash-log.md`, раздел «Ожидание», включая решающее правило и обратный случай по шагу 2

## 2. Детерминизм порядка (M3R-144) — единственный источник сдвига хеша

- [x] 2.1 `pick_replacement` упорядочивает пул кандидатов замены перед `rng.choices`, отчего исход перестаёт зависеть от порядка входного отображения (design.md D2) — `app/core/slot_mutation.py:206`
- [x] 2.2 Снять с `frequencies_by_ending` докстринг-опору на «порядок вставки не меняется» — после 2.1 утверждение больше не несёт нагрузки и вводит в заблуждение — снято и в `slot_mutation.frequencies_by_ending`, и в `LearningService.get_word_frequencies_by_ending`
- [x] 2.3 `get_word_frequencies` получает тотальный `ORDER BY`; тест упорядоченности по образцу `test_transition_rows_are_ordered` — `test_word_frequency_rows_are_ordered`. **Оговорка:** клауза оказалась no-op — `GROUP BY` в SQLite уже отдаёт порядок ключа группировки при обоих планах запроса; тест сторожит свойство, а не клаузу, и это записано в его докстринге
- [x] 2.4 `get_hot` получает вторичный ключ сортировки; тест упорядоченности на синтетических строках с **равными** счётчиками (на снимке список пуст, данными это не проверяется — design.md D3) — `TestHotNgramTieOrder`. **Та же оговорка:** план идёт по первичному ключу, чей порядок совпадает с порядком n-граммы, поэтому тай-брейк сегодня тоже тождественен
- [x] 2.5 Тест «тёплый кэш == холодное чтение» для словаря частот: прогреть кэш, доучить сообщения с **новыми** словами, сравнить с холодным перечитыванием. Тест обязан падать на состоянии до 2.1 — проверить это явно, иначе он ничего не сторожит — `test_warm_frequency_cache_draws_like_a_cold_read`; сторож подтверждён: без правки падает на сиде 0, с правкой проходит на всех 30. **Это единственная нагруженная правка блока**
- [x] 2.6 Измерить `generation_hash` после блока 2. ~~**Ожидание: сдвинулся.**~~ **Факт: НЕ сдвинулся** (оба снапшота бит-в-бит равны шагу 1). Разбор в `hash-log.md`: холодный путь был упорядочен всегда, а `generation_hash` гоняет только холодный старт и потому структурно слеп к расхождению тёплого и холодного. Перезаморозка по итогам блока 2 не требуется

## 3. Полная финализация seeded (M3R-101)

- [x] 3.1 Выделить тело `_finalize_attempt` (хвостовые трансформации + четыре гейта формы) из обёртки, собирающей `_GenerationAttempt`; существующий вызов идёт через обёртку и не меняется (design.md D1) — `finalize_candidate_tokens` + `FinalizedCandidate` в `app/core/markov.py`; обёртка ужалась с 96 строк до 40 (ушла вторая копия конструктора `_GenerationAttempt`). Хеш после чистого рефакторинга не сдвинулся — проверено отдельным замером
- [x] 3.2 Узкий вход в то же тело для внешних сборщиков кандидата — `finalize_candidate_tokens` модульного уровня, рядом с гейтами, которые он зовёт
- [x] 3.3 `_append_seeded_candidates` собирает кандидата через этот вход вместо прямого `detokenize` — `app/core/response_generator.py`
- [x] 3.4 Исправить докстринг `_append_seeded_candidates`: «passes the same reject gates as every other candidate» до этой задачи неверно — применялись только два гейта `_candidate_reject_reason` — переписан, с явным указанием, что утверждение было ложным до M3R-101
- [x] 3.5 Тесты финализации seeded при **явно поднятом** `markov_seeded_candidate_ratio` (на дефолте 0 ветка не исполняется и тестами общего прогона не покрывается): терминальная пунктуация, отсутствие ведущей пунктуации, срабатывание каждого из четырёх гейтов формы — `tests/test_markov2r_phase5_seeded_finalization.py`, 9 тестов. Сторож подтверждён: оба теста проводки падают без правки. **Побочно найдено:** `low_diversity` почти недостижим через конвейер — `trim_repetitive_tail` идёт первым и срезает хвостовой повтор раньше гейта; гейт ловит только вырождение в начале ответа. Записано в докстринге теста
- [x] 3.6 Тест нейтральности: при `ratio = 0` seeded-чтения не происходит и вывод не меняется — требование `generation-lexical-anchoring` «Neutral default leaves replies unchanged» остаётся в силе — покрыто существующими `tests/test_markov2r_phase5_neutrality.py` (16 тестов фазы 5 зелёные) плюс глобальным доказательством: хеш не сдвинулся
- [ ] 3.7 Снять долю seeded-кандидатов, отклонённых четырьмя гейтами формы, — новая величина, её надо измерить и записать, а не предсказать (design.md, Risks) — **замер запущен, не завершён**. Харнесс: скретчпад `seeded_rejection_share.py` (обвязка `tools/generation_hash.py` + `_populate_token_df` из `tools/eval/run.py`, прод-копия, `ratio=0.3`). Без заполнения df замер бессмыслен: `markov_token_df` на снимке пуст, `n_docs=0`, `rank_seeds` возвращает `[]` и ветка не срабатывает вовсе (map §3.2а). Число подставить в вердикт-заметку по готовности; на приёмку пакета не влияет — это справочная величина, а не гейт

## 4. Атрибуция маршрутов (M3R-102/103)

- [x] 4.1 Перечисление маршрутов из **живых** производителей кандидата: основной обход, seeded, слот-мутация, verbatim-дописка. `interp`/`association`/`phrase`/`retrieval` не заводить (design.md D4) — `CandidateRoute` в `app/core/generation_telemetry.py` (лист-модуль, поэтому импортируется и трассой, и генератором без цикла)
- [x] 4.2 Поле маршрута на кандидате, проставляется в точке создания на каждой из четырёх ветвей — `_ScoredCandidate.route`; три места создания в `response_generator.py` (основной обход/дописка различаются флагом `was_extended`, мутант, seeded). Побочно: второй сайт дописки не выставлял `was_extended` — исправлено, иначе дописанный кандидат считался бы `vanilla`
- [x] 4.3 Маршрут виден в трассе для каждого кандидата, включая отклонённых, вместе с причиной отклонения — `route=` в трёх строках блока `SELECTION` и в `log_attempt_rejected`; ветки вне основного цикла получили `_note_rejected` (до этого отклоняли молча)
- [x] 4.4 По-маршрутная разбивка телеметрии: доля пула, доля побед, причины отклонений (на существующих причинах трассы — не F-классы, design.md Non-Goals) ~~affinity, copy~~ — `route_breakdown()` и `route_rejection_reasons()`. **Сокращение объёма:** affinity и copy по маршрутам — метрики eval-отчёта, а не процессной телеметрии; считает их `tools/eval/metrics.py`, это зона E. Поле `route` на кандидате даёт им вход, сама разбивка — отдельная задача зоны E
- [x] 4.5 Сохранить правило двух знаменателей для каждого маршрута: «не появился в пуле» и «появился и проиграл» остаются различимы — `attempts`/`present`/`won` + `present_rate`/`win_rate_given_present`
- [x] 4.6 Маршрут, включённый настройкой но не давший кандидата, отличим от выключенного — `attempted` собирается по ручкам, а не по перечислению: выключенный маршрут не имеет попыток (`present_rate=None`), включённый и пустой имеет попытки и `present_rate=0.0`
- [x] 4.7 Тест privacy: в по-маршрутной телеметрии только метки перечисления и числа, без текста и идентификаторов — `tests/test_route_attribution.py`, 7 тестов
- [x] 4.8 Измерить `generation_hash` после блока 4. **Ожидание: не сдвинулся относительно 2.6.** При расхождении — стоп и разбор: это утечка поведения в нейтральном режиме, а не повод обновить запись — **подтвердилось**: оба снапшота бит-в-бит равны шагам 1 и 2

## 5. Перезаморозка и вердикт

- [x] 5.1 ~~Обновить запись ожидаемого `generation_hash` один раз, с причиной, называющей блок 2 как источник сдвига~~ — **не потребовалось**: все три измерения дали одно значение на обоих снапшотах. Запись не трогалась; причина, почему перезаморозки нет, записана в `hash-log.md` и в вердикт-заметке
- [x] 5.2 Вердикт-заметка в `docs/eval_reports/` по образцу существующих: три измерения хеша, что сдвинуло и почему это законно, доля отклонений seeded из 3.7 — `docs/eval_reports/eval_2026-08-14_w1c-hash-package.md`. Доля отклонений seeded помечена там как незакрытая (см. 3.7)
- [x] 5.3 Актуализировать `docs/GENERATION_MAP.md`: §3.4 и §3.7 закрываются; §1.5 теряет строку про seeded; **§3.7 дополнить тем, что `ORDER BY` сам по себе тёплый/холодный разрыв не закрывал** — предписание карты было неполно — сделано; §3.7 получил поправку к диагнозу, §3.4 — отметку о закрытии и побочную находку про `low_diversity`, §1.5 — вычеркнутую строку seeded
- [x] 5.4 Отметить в `docs/OPEN.md`, если остаётся незакрытый хвост — два: **O5** (слепота `generation_hash` к тёплому пути, класс шире одного случая) и поправка к строке **M2R-3** (ранний сигнал фазы 5 +0.064 снят до M3R-101 и с новыми числами несравним)

## 6. Close-out

- [x] 6.1 `openspec validate --strict` зелёный для этого change'а — зелёный
- [x] 6.2 Полный прогон: `unittest` + `ruff` + `mypy app/`, покрытие не ниже храповика (`fail_under = 87`) — 1194 теста OK (было 1178, +16 новых), `ruff` чисто, `mypy` 75 файлов без ошибок, покрытие **94%** при храповике 87
- [x] 6.3 Проверить, что тесты с синтетическими ID не задеты (`tests/test_no_real_chat_ids.py`) — 2 теста OK; все новые тесты используют синтетические chat_id (4848, 5151, CHAT=4242)
- [ ] 6.4 Архивировать change после мержа — ветка `pre-w1c-seeded-ordering-and-routes`, коммита нет: по правилам проекта коммит делается по явной просьбе владельца
