## 1. Метрика

- [x] 1.1 `tools/eval/metrics.py`: `trajectory_edges(text)` — множество смежных пар токенов; `edge_overlap(a, b)` — доля общих пар, нормированная на меньшее множество (D1)
- [x] 1.2 `distinct_trajectories(texts, *, similar_at)` — число групп при транзитивной склейке похожих (D2); детерминировано и не зависит от порядка входа
- [x] 1.3 Поля `GenRecord`: `pool_ecb: int = 0`, `window_escape: int = 0`
- [x] 1.4 `metric_values`: `structural_pool_ecb` и `structural_window_escape` — обе или ни одной (спека: пара не разделяется)
- [x] 1.5 Тесты: одинаковые кандидаты → 1; подпуть → та же траектория; разные проходки из одних слов → 2; пустой пул → 0; независимость от порядка входа

## 2. Сбор в прогоне

- [x] 2.1 `tools/eval/run.py`: хук отбора считает окно по `margin`, полученному от кода (D3), и обе величины кладёт в запись
- [x] 2.2 Порог сходства раннер берёт из порогов, а не из константы модуля
- [x] 2.3 Тест: генерация без кандидатов даёт нули и остаётся в знаменателе

## 3. Отчёт и гейт

- [x] 3.1 `METRIC_ORDER`: обе метрики рядом, парой
- [x] 3.2 Блок `structural_escape` в `eval_thresholds.yaml`: `edge_overlap_similar: 0.5`, `window_escape_min: 2.0`, `pool_ecb_min: 4.0`, `requires_both_modes: true` (D5, D6) — с комментарием о том, что пороги пре-регистрированы и почему выбраны именно эти
- [x] 3.3 Строка гейта `structural_escape` в `evaluate_gates`: pass только при окне ≥ минимума **и** пуле ≥ пола; `insufficient data`, пока чисел нет
- [x] 3.4 Распределение **счёта по окну** в строке гейта вместо распределения overlap: на вопрос «зависит ли вердикт от порога 0.5» отвечает то, где лежит масса (`1:36%, 2:27%, …`), и оно не требует хранить попарные пересечения
- [x] 3.5 Тесты гейта: провал по окну, провал по полу пула, `insufficient data` без данных, даунгрейд по одному режиму

## 4. Базлайн и приёмка

- [x] 4.1 `generation_hash --synthetic --check` — хеш не сдвинулся
- [x] 4.2 `ruff`, `mypy app/`, полный `unittest`, покрытие не ниже храповика
- [x] 4.3 Прогон C0 в обоих режимах на прод-копии — базлайн escape/ECB; p95 30.0/20.0 мс. **Первый прогон показал p95 41.7 мс:** метрика считалась внутри замеряемого участка и биллилась генератору. Счёт вынесен за таймер (хук только захватывает пул), p95 вернулся к 30.0 при неизменных структурных числах
- [x] 4.4 Цифры базлайна в `docs/PRE_ROADMAP.md` (M3R-011 сделан) и в `docs/v2/05_MARKOV_2_0R_EVAL_PROTOCOL.md` §3 (определение метрики)

## 5. Первое применение (в этом же прогоне)

- [x] 5.1 Прогнать метрику на плечах фазы 9 — дошло ли добавленное ветвление до окна отбора; результат дописать в вердикт фазы 9 как продолжение, а не переписывая его
