## 1. Таблица лимитов

- [ ] 1.1 Расширить `COMMAND_COOLDOWNS_SECONDS` в `main.py` по классам из `design.md` (D1): `ping`/`help`/`config`/`pivo_privacy` — 15 с, `stats` — 60 с, `pivo_on`/`pivo_off` — 30 с, `clear` — без изменений
- [ ] 1.2 Добавить `pivo_on` и `pivo_off` в `notify_on_throttle` (D2)
- [ ] 1.3 Комментарий над таблицей: классы и правило, по которому новая команда получает окно
- [ ] 1.4 Явно зафиксировать исключение для `/set` и `/setprob` с причиной (owner-only после фикса O5)
- [ ] 1.5 Назначить окно `/pivo` как страховку от частоты, не подменяя суточную квоту

## 2. Тесты

- [ ] 2.1 По представителю каждого класса: повторный вызов внутри окна не выполняется, побочных эффектов нет
- [ ] 2.2 Лимит считается на пару «участник + чат»: другой участник и другой чат не затронуты
- [ ] 2.3 Разные команды имеют независимые окна
- [ ] 2.4 `/config` в личном чате ограничивается так же, как в группе
- [ ] 2.5 `/pivo_on` при отказе отвечает один раз, дальнейшие попытки внутри окна — молча
- [ ] 2.6 `/pivo` продолжает применять суточную квоту независимо от кулдауна
- [ ] 2.7 Тест-сторож: у каждой зарегистрированной команды есть окно либо запись в списке исключений

## 3. Закрытие

- [ ] 3.1 Прогнать полный сьют, `ruff`, `mypy`
- [ ] 3.2 Упомянуть наличие лимитов в README рядом со списком команд
- [ ] 3.3 Перенести пункт из `docs/OPEN.md` в `docs/CLOSED.md`, отметив, что решение про доступ к `/config` не пересматривалось
