## 1. Таблица лимитов

- [x] 1.1 Расширить `COMMAND_COOLDOWNS_SECONDS` в `main.py` по классам из `design.md` (D1): `ping`/`help`/`config`/`pivo_privacy` — 15 с, `stats` — 60 с, `pivo_on`/`pivo_off` — 30 с, `clear` — без изменений
- [x] 1.2 Добавить `pivo_on` и `pivo_off` в `notify_on_throttle` (D2)
- [x] 1.3 Комментарий над таблицей: классы и правило, по которому новая команда получает окно
- [x] 1.4 Явно зафиксировать исключение для `/set` и `/setprob` с причиной (owner-only после фикса O5)
- [x] 1.5 ~~Назначить окно `/pivo`~~ — **не сделано намеренно**: `282051f` снял троттлинг с `/pivo` потому, что мидлварь глушит вызов до хендлера и человек вместо «лимит исчерпан» получал тишину. Окно вернуло бы этот баг; частоту ограничивает суточная квота. Причина записана в `COOLDOWN_EXEMPT_COMMANDS` и в тесте

## 2. Тесты

- [x] 2.1 По представителю каждого класса: повторный вызов внутри окна не выполняется, побочных эффектов нет
- [x] 2.2 Лимит считается на пару «участник + чат»: другой участник и другой чат не затронуты
- [x] 2.3 Разные команды имеют независимые окна
- [x] 2.4 `/config` в личном чате ограничивается так же, как в группе
- [x] 2.5 `/pivo_on` при отказе отвечает один раз, дальнейшие попытки внутри окна — молча
- [x] 2.6 ~~`/pivo` + кулдаун~~ — неприменимо после решения по 1.5: у `/pivo` окна нет, существующий тест пиннит его отсутствие с причиной
- [x] 2.7 Тест-сторож: у каждой зарегистрированной команды есть окно либо запись в списке исключений

## 3. Закрытие

- [x] 3.1 Прогнать полный сьют, `ruff`, `mypy`
- [x] 3.2 Упомянуть наличие лимитов в README рядом со списком команд
- [x] 3.3 Перенести пункт из `docs/OPEN.md` в `docs/CLOSED.md`, отметив, что решение про доступ к `/config` не пересматривалось
