## 1. Ручка отката

- [x] 1.1 Добавить в реестр (`app/config/registry.py`) bool-ручку `pivo_mention_by_id` с дефолтом `true` и переменной окружения `PIVO_MENTION_BY_ID`
- [x] 1.2 Описать ручку в `.env.example` рядом с остальными `PIVO_*` — назначение и то, что она аварийная
- [x] 1.3 Тест: `/set pivo_mention_by_id false` меняет поведение следующего вызова без рестарта

## 2. Построение упоминания

- [x] 2.1 Переписать `build_pivo_mention` (`app/domain/pivo.py`): при наличии `user_id` строить `<a href="tg://user?id=<id>">@ник</a>`, где подпись — `@ник`, если он есть, иначе отображаемое имя (D1)
- [x] 2.2 Оставить путь голого `@ника` для случая, когда `pivo_mention_by_id` выключена, и для записей без пригодного `user_id`
- [x] 2.3 Пропускать подписчика, у которого непригодны и `user_id`, и ник, — сообщение при этом должно собираться и отправляться
- [x] 2.4 Сохранить экранирование HTML на обоих путях (подпись и `user_id` — как сейчас, `html.escape`)
- [x] 2.5 Вернуть из построения признак выбранного пути (по id / строкой / пропущен) для агрегата и диагностики — не меняя сигнатуру `collect_pivo_mentions` на что-то, что тащит расшифрованные данные наружу
- [x] 2.6 Тесты в `tests/test_pivo*.py`: подписчик с ником, без ника, с выключенной ручкой, непригодная запись; проверка, что видимый текст сообщения для читателя не изменился

## 3. Наблюдаемость

- [x] 3.1 Считать типы entity в объекте `Message`, который возвращает `message.reply(...)` в `app/handlers/pivo.py` (D3)
- [x] 3.2 Логировать агрегат: число подписчиков, `text_mention`, `mention`, пропущенных; `chat_id` — через `mask_chat_id`; персональных данных в записи быть не должно
- [x] 3.3 Тест: в логе нет расшифрованных ников, имён и `user_id`, а `chat_id` замаскирован

## 4. Диагностика `/pivo_check`

- [x] 4.1 Добавить хендлер `/pivo_check` в `app/handlers/pivo.py` под фильтрами `OwnerOnly` и `GroupOnly`
- [x] 4.2 Ответ: по каждому подписчику чата — выбранный путь и причина; в лог расшифрованные ники не писать
- [x] 4.3 Тесты: владелец получает список; админ чата и обычный участник получают отказ и не видят состав подписчиков
- [x] 4.4 Описать команду в `README.md` рядом с остальными `/pivo*`

## 5. Проверка перед выкаткой

- [x] 5.1 Проверить длину собранного сообщения на максимальном списке (`PIVO_SUBSCRIBER_FANOUT_LIMIT`): не упирается ли в лимит 4096 после удлинения упоминаний — худший случай (20 подписчиков, ники по 32 символа, 10-значные id) даёт 1439 символов сырой строки против 679 прежних, +38 на подписчика, запас до лимита 2657; вдобавок лимит Telegram считается по разобранному тексту, где href не учитывается, так что оценка консервативная
- [x] 5.2 Прогнать полный сьют и `ruff`/`mypy` по изменённым файлам
- [ ] 5.3 Живой прогон `/pivo` в тестовой группе: снять агрегат из лога и убедиться, что `text_mention` появился и его число равно числу подписчиков с `user_id` (закрывает Open Question про дублирование entity)

## 6. Выкатка и наблюдение

- [ ] 6.1 Выкатить на рестарте; сразу после первого живого `/pivo` снять агрегат D3 из лога
- [ ] 6.2 Если `text_mention` в агрегате не появился — не наблюдать, а вернуться к разбору: гипотеза о механике неверна
- [ ] 6.3 Спросить поимённо пожаловавшихся людей, приходит ли уведомление теперь (явный опрос, не ожидание тишины)
- [ ] 6.4 Обновить `docs/OPEN.md`: снять с O2 гейт «механику упоминаний не трогать», записать, что именно измерено
- [ ] 6.5 По итогу окна — закрыть O2 в `docs/CLOSED.md` с числами из агрегата либо оставить открытым с зафиксированным исключением строкового пути
