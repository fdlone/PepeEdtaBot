## 1. Ручка

- [x] 1.1 Добавить bool-ручку `pivo_report_to_owner` (`PIVO_REPORT_TO_OWNER`, дефолт `true`) в `app/config/registry.py`, поля в `Settings` и `RuntimeState`
- [x] 1.2 Описать в `.env.example` рядом с `PIVO_MENTION_BY_ID`
- [x] 1.3 Тест: при выключенной ручке личное сообщение не уходит, а агрегат в логе остаётся

## 2. Отправка отчёта

- [x] 2.1 Собрать текст отчёта в `app/handlers/pivo.py`: название чата, число подписчиков, пути (`by_id` / `by_username` / пропущено), `text_mention` и `mention` из entity ответа
- [x] 2.2 Отправлять после успешного `message.reply`, до или после записи анти-повтора — но так, чтобы отказ не влиял ни на то, ни на другое
- [x] 2.3 Не отправлять, если `OWNER_ID` не задан
- [x] 2.4 Обернуть отправку так, чтобы **любая** ошибка ловилась и логировалась, а `/pivo` продолжал считаться выполненным (квота не возвращается, исключение не всплывает)
- [x] 2.5 Убедиться, что в отчёт не попадают ники, имена и идентификаторы подписчиков

## 3. Тесты

- [x] 3.1 Владельцу уходит сообщение с числами из агрегата и названием чата
- [x] 3.2 В отчёте нет персональных данных подписчиков
- [x] 3.3 `OWNER_ID` не задан — отправки нет, `/pivo` работает
- [x] 3.4 Ошибка отправки (например, владелец не начинал диалог) — `/pivo` выполнен, квота не возвращена, исключение не всплыло, в логе есть след
- [x] 3.5 Ручка выключена — отправки нет

## 4. Закрытие

- [x] 4.1 Полный сьют, `ruff`, `mypy`
- [x] 4.2 README: описание отчёта и ручки рядом с `/pivo_check`
- [x] 4.3 `docs/OPEN.md`: протокол наблюдения по O2 переписать на личку вместо лога — владелец читать логи не может
