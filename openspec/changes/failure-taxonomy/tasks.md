# Tasks — failure-taxonomy

## 1. Перечисление и отображение

- [x] 1.1 `app/core/failure_taxonomy.py`: закрытое перечисление девяти классов
      и отображение существующих причин в классы по таблице design D1.
      Проверка: юнит-тест — каждая причина из отображения даёт свой класс,
      неизвестная причина класса не получает
- [x] 1.2 Причины «не из чего генерировать» в отображение не входят (D4).
      Проверка: тест — `no_starts` / `no_start_transition` не размечены

## 2. Разрез телеметрии

- [x] 2.1 `GenerationTelemetry`: разбивка отклонений по классам рядом с
      существующей разбивкой по причинам (D3), с отдельным разрядом
      «не размечено» (D2). Проверка: юнит-тест на счётчиках двух маршрутов —
      суммы по классам плюс «не размечено» равны сумме по причинам
- [x] 2.2 Строка в `/stats`: классы с ненулевыми счётчиками и число
      неразмеченных. Проверка: тест текста — при пустой телеметрии строка не
      печатается, при непустой видны классы

## 3. Приёмка

- [x] 3.1 `ruff` + `mypy app/` + полный сьют, покрытие не ниже храповика.
      Влияние на `generation_hash`: отсутствует по построению (агрегация над
      готовыми счётчиками, RNG не тронут) — снятие снимков не требуется
- [x] 3.2 `docs/GENERATION_MAP.md`: таблица классов и покрытие автоматикой
      (актуализация по §5 карты); синк спеки при архиве
