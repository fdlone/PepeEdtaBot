## 1. Чтение объёма из счётчика

- [ ] 1.1 Добавить в `MarkovRepo` (или отдельный репозиторий) чтение `volume2`/`volume3` из `chat_model_volume`
- [ ] 1.2 Перевести `Database.get_chat_token_volume` на это чтение с той же семантикой «volume3, иначе volume2»
- [ ] 1.3 Реализовать разовый бэкфилл: если строки для чата нет, посчитать полный агрегат, записать строку и вернуть значение
- [ ] 1.4 Тест: значение совпадает с полным агрегатом после серии обучений
- [ ] 1.5 Тест: чат без строки в `chat_model_volume` получает корректное значение и строка появляется
- [ ] 1.6 Тест: после `clear_chat` объём равен нулю

## 2. `/stats` без лишних агрегатов

- [ ] 2.1 Убрать из `Database.get_stats` агрегаты, не участвующие в выводе
- [ ] 2.2 Сохранить контракт возвращаемого словаря для тестов либо обновить тесты вместе с ним
- [ ] 2.3 Тест: текст `/stats` не изменился

## 3. Закрытие

- [ ] 3.1 `ruff check app/ tests/ tools/ main.py`
- [ ] 3.2 `mypy app/`
- [ ] 3.3 `python -m unittest discover tests -v`
