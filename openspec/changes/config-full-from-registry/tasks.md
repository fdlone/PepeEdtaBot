## 1. Полнота вывода

- [x] 1.1 Заменить рукописный блок полной формы в
      `app/presentation/bot_messages.py` на обход перечня изменяемых настроек
      из `app/config/registry.py`. Проверка: в выводе присутствуют все 97
      ключей реестра.
- [x] 1.2 Тест полноты против реестра: множество показанных настроек равно
      множеству изменяемых. Проверка: тест падает, если ручку добавить в
      реестр и не добавить в вывод.
- [x] 1.3 Проверить 1.2 мутацией: временно исключить одну настройку из вывода
      — тест обязан упасть с именем этой настройки в сообщении.

## 2. Длина сообщения

- [x] 2.1 Оценить фактическую длину полного вывода на дефолтах реестра.
      Проверка: число байт зафиксировано в комментарии теста, чтобы следующий
      рост был виден.
- [x] 2.2 Разделить полный вывод на части существующим механизмом
      последовательной отправки в `app/handlers/admin.py`. Проверка: тест на
      выводе, превышающем лимит, — все настройки присутствуют в сумме частей,
      усечения нет.

## 3. Краткая форма и права

- [x] 3.1 Убедиться, что краткая форма не изменилась ни на строку. Проверка:
      существующие тесты краткой формы зелёные без правок.
- [x] 3.2 Убедиться, что отказ без прав по-прежнему не подменяется краткой
      формой. Проверка: существующий тест отказа зелёный.

## 4. Документация

- [x] 4.1 Дополнить инструкцию по добавлению runtime-mutable параметра в
      `docs/ARCHITECTURE.md`: вывод в полной форме появляется сам, отдельного
      действия не требует. Проверка: инструкция прочитана и соответствует коду.
- [x] 4.2 Сверить обещания в `README.md` и тексте `/help` с фактическим
      поведением. Проверка: слово «полный» теперь истинно.

## 5. Приёмка

- [x] 5.1 `ruff` + `mypy app/` + полный `unittest` зелёные, покрытие не ниже
      храповика 87.
- [x] 5.2 `python -m tools.generation_hash --synthetic --check` — хеш не
      сдвинулся (генерация не затрагивается; ожидание заявлено в
      `proposal.md` § Impact).
- [x] 5.3 Обновить `docs/OPEN.md`: перенести O9 в закрытое с датой и сутью
      решения.
