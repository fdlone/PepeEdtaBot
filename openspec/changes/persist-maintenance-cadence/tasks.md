# Tasks — persist-maintenance-cadence

## 1. Заявка по хешу — до правок

- [x] 1.1 Снять `generation_hash` на прод-копии и синтетике до правок,
      записать в `hash-log.md`; ожидание: сдвига нет (путь генерации не
      тронут, миграция аддитивная). Расхождение = стоп и разбор (§2)

## 2. Миграция и метка

- [x] 2.1 `app/migrations/023_maintenance_meta.sql`: таблица
      `maintenance_meta(key TEXT PRIMARY KEY, value TEXT NOT NULL)`.
      Проверка: миграция применяется на временной БД и на копии прода
- [x] 2.2 `Database`: приватные чтение/запись ключа `last_flavor_decay_at`
      (UTC, формат `_decay_cutoff`) через `_get_conn`/`_lock` (D4).
      Проверка: юнит-тест раундтрипа
- [x] 2.3 В `forget_chat`/`clear_chat` таблица НЕ добавляется — метка
      глобальная (D1). Проверка: комментарий у таблицы называет причину

## 3. Каденция

- [x] 3.1 `Database.init()`: срок следующего пасса — от метки: нет метки или
      прошло ≥ интервала → «сейчас»; иначе остаток; отрицательный остаток →
      полный интервал (D2). Проверка: юнит-тесты трёх веток
- [x] 3.2 `decay_flavor_stats_if_due`: на `DONE` записать метку; на `FAILED`
      не трогать, ретрай как есть (D3). Проверка: тесты обеих веток
- [x] 3.3 Сквозной тест рестарта: первый `Database` выполняет пасс и
      закрывается; второй `Database` на том же файле в пределах интервала
      получает `SKIPPED`, за пределами (метка, сдвинутая в прошлое) — `DONE`.
      Проверка: тест падает при возврате чисто монотонного срока (мутация)

## 4. Приёмка

- [x] 4.1 `generation_hash` после правок сверен с 1.1, факт в `hash-log.md`
- [x] 4.2 `ruff` + `mypy app/` + `unittest` зелёные, покрытие не ниже
      храповика; миграция прогнана на копии прода (§7/меморандум о миграциях)
- [x] 4.3 `docs/OPEN.md`: O11 закрыт, запись в `docs/CLOSED.md` с датой и
      решением; побочный пункт про §7 — явной строкой уходит в проход по
      документации
