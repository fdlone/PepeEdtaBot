## 1. Чтение агрегата

- [x] 1.1 Добавить в `ChatUserInteractionsRepo` метод, возвращающий агрегат по чату: число строк, максимум `cnt`, число строк с `cnt >= порога`
- [x] 1.2 Пробросить через `Database` и `LearningService` по образцу `get_user_interaction_count`
- [x] 1.3 Пустой чат (нет строк) возвращает нули, а не ошибку

## 2. Команда

- [x] 2.1 Добавить `/quirk_stats` под `OwnerOnly()` и `GroupOnly()`
- [x] 2.2 Ответ: число людей со счётчиком, максимум, сколько достигли порога, сам порог — только числа
- [x] 2.3 Fallback-хендлер для отказа тем, кто не владелец
- [x] 2.4 Не писать счётчики и идентификаторы в лог
- [x] 2.5 Внести команду в кулдауны (`main.py`) либо явно в список исключений с причиной — по правилу из `command-rate-limits`

## 3. Тесты

- [x] 3.1 Агрегат считается верно на подготовленных данных
- [x] 3.2 Пустой чат: ответ про отсутствие накопленных обращений, не ошибка
- [x] 3.3 В ответе нет хэшей, имён и идентификаторов
- [x] 3.4 Не владелец получает отказ и не видит чисел
- [x] 3.5 Команда ничего не меняет: счётчики после вызова прежние

## 4. Закрытие

- [x] 4.1 Полный сьют, `ruff`, `mypy`
- [x] 4.2 README рядом с `/pivo_check`
- [ ] 4.3 После рестарта — снять числа и решить, что делать с порогом (отдельным изменением, с числом на руках)
