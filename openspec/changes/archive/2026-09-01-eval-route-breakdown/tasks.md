# Tasks — eval-route-breakdown

## 1. Заявка по хешу — до правок

- [x] 1.1 Записать в `hash-log.md` ожидание до правок: сдвига нет (правки
      только в `tools/eval`, генерация не тронута)

## 2. Запись и телеметрия

- [x] 2.1 `GenRecord`: `winner_route`, `pool_routes` (D1); захват в
      `_on_selection`, вне таймера латентности
- [x] 2.2 Плоские ключи `route_<маршрут>_<ключ>` и отклонения по классам
      M3R-021 в снапшоте прогона (D2)

## 3. Отчёт

- [x] 3.1 Раздел «Per-route breakdown» с колонками D3; `not attempted` при
      нуле попыток (D4)
- [x] 3.2 Тесты: два знаменателя печатаются раздельно; маршрут без попыток —
      не нулевая строка; `metrics_summary` не зависит от полей маршрутов (D5);
      синтетический прогон заполняет маршруты у каждой успешной записи
- [x] 3.3 `tools/eval/README.md` — раздел отчёта описан

## 4. Приёмка

- [ ] 4.1 `ruff`, `mypy app/`, полный сьют, покрытие не ниже храповика
- [x] 4.2 `generation_hash --synthetic --check` после правок, факт в
      `hash-log.md`
- [x] 4.3 `docs/PRE_ROADMAP.md` — M3R-103 закрыт целиком; `docs/CLOSED.md`
