# hash-log — eval-route-breakdown

## До правок (2026-09-01)

- синтетика: `555f552a76cb84899c40d3b49069b96b8100f9d65203125641820712af6635f9`
  (baseline OK, снято на этой ветке заявкой `tune-user-quirk-channel`)
- прод-копия: `f108b13da3571bb080290e6e37d56a5713f502c3b4bbfc62e8641ff6a1c3d54a`

## Ожидание — записано до правок

Сдвига **нет**: правки только в `tools/eval` (запись, отчёт, тесты);
`app/core` не трогается, `tools/generation_hash.py` пакет `tools/eval` не
импортирует.

## После правок (2026-09-01)

- синтетика: `555f552a76cb84899c40d3b49069b96b8100f9d65203125641820712af6635f9` (baseline OK, `--synthetic --check`)

Ожидание подтвердилось. Прод-копия отдельно не снималась: пакет `tools/eval` в `tools/generation_hash.py` не импортируется, генерация не тронута.
