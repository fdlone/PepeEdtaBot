# Tasks — chat-timezone-for-temporal-branches

## 1. Заявка по хешу — до правок

- [x] 1.1 Снять `generation_hash` на прод-копии и на синтетике **до** любых
      изменений, записать оба значения в `hash-log.md` change'а:
      `.venv/Scripts/python.exe -m tools.generation_hash --db
      db_prod_copy/markov.db` и `--synthetic --check`
- [x] 1.2 Записать там же ожидание, до правок: **сдвига нет ни на одном из двух
      снимков** — момент времени не питает ни RNG, ни цепь. Расхождение с
      ожиданием останавливает работу и разбирается (§2 `CLAUDE.md`)

## 2. Зависимость

- [x] 2.1 Добавить `tzdata` в `requirements.txt` с комментарием-обоснованием
      (данные IANA без кода, fallback stdlib для `zoneinfo`; Windows-разработка
      и slim-образ без гарантии системной базы) и в `requirements.lock`;
      проверить установкой в `.venv` и
      `python -c "from zoneinfo import ZoneInfo; ZoneInfo('Europe/Moscow')"`

## 3. Настройка

- [x] 3.1 `Settings`: читать `CHAT_TIMEZONE` (дефолт `UTC`), валидировать через
      `ZoneInfo` на старте; при `ZoneInfoNotFoundError` — ошибка запуска,
      называющая настройку и значение (D3). Хранить готовый `tzinfo` (D1).
      Проверка: юнит-тесты на дефолт, валидную зону и опечатку
- [x] 3.2 `RuntimeState`: plain-поле `chat_timezone` по образцу
      `runtime_state_ttl_sec`, прокинуть в `build_runtime_state`. Проверка:
      тест, что поле доезжает из `Settings` и не попадает в реестр
      runtime-mutable (вывод `/config` не меняется)
- [x] 3.3 `.env.example`: `CHAT_TIMEZONE` с пояснением (что двигает, что
      остаётся на UTC) и примером `Europe/Moscow`. Проверка: строка на месте,
      рядом с остальными настройками окружения

## 4. Точки вызова

- [x] 4.1 `app/handlers/pivo.py:241`: `now=datetime.now(runtime_state.chat_timezone)`.
      Проверка: тест — при зоне `Europe/Moscow` и UTC-часах 23:30 четверга
      `/pivo` видит пятницу и ночь (сценарий спеки)
- [x] 4.2 `app/services/reply_pipeline.py:670`: тот же вызов для фолбэков.
      Проверка: тест — ночной пул активен/неактивен по местному времени зоны,
      не по UTC (сценарий «04:00 UTC = 07:00 MSK — ночь не играет»)
- [x] 4.3 Убедиться, что служебные `datetime.now(UTC)` (капы, квоты,
      обслуживание) не задеты диффом. Проверка: `git diff` не содержит правок
      этих точек; существующие тесты квот/капов зелёные

## 5. Карта и приёмка

- [x] 5.1 Актуализировать `docs/GENERATION_MAP.md` по её §5 (правка задевает
      `reply_pipeline.py`). Проверка: запись в карте соответствует дельте
- [x] 5.2 Снять `generation_hash` на обоих снимках **после** правок, сверить с
      1.1, записать факт в `hash-log.md`. Проверка: значения совпали
- [x] 5.3 `ruff` + `mypy app/` + `unittest` зелёные, покрытие не ниже храповика
      (`fail_under = 87`). Проверка: команды выполнены, вывод чистый
- [x] 5.4 Обновить `docs/OPEN.md`: O12 закрыт выбором «явная настройка»,
      с оговоркой «в проде включается `CHAT_TIMEZONE=Europe/Moscow` на
      редеплое»; перенести запись в `docs/CLOSED.md` по заведённому порядку.
      Проверка: в OPEN.md строки O12 нет, в CLOSED.md есть с датой
