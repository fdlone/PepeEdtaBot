## 1. Гард до починки

- [x] 1.1 `tests/test_solo_rating_round.py`: `build_round` с одинаковым текстом в двух руках даёт ему одно предъявление (плюс возможный плановый повтор), ключ несёт `arms` с обеими руками, `score_round` засчитывает одну оценку обеим рукам и минимум на руку выполняется. Проверка: тест **падает** на текущем коде (текст встречается дважды, `arms` нет)

## 2. Починка

- [x] 2.1 `_round_entries`: склейка по `tokenize`-нормализации после сэмплирования на руку (design Р1), поле `arms`; декои склеиваются тем же правилом; повторы — от склеенного списка. Проверка: тест 1.1 зелёный, весь `BuildRoundTest` и `UnusableRoundTest` зелёные
- [x] 2.2 `build_round`: ключ несёт `arms`; `score_round` читает `meta.get("arms") or [meta["arm"]]` (design Р2). Проверка: `ScoreRoundTest` зелёный на старом формате ключа, тест 1.1 — на новом

## 3. Документация

- [x] 3.1 `docs/SOLO_RATING_ROUND.md` «Устройство раунда»: абзац про склейку и почему (связность — свойство текста). Проверка: перечитать раздел

## 4. Приёмка и раунд

- [x] 4.1 `ruff check`, `mypy app/`, `unittest` зелёные; хеш не задет (`tests/test_generation_hash_baseline.py`). Проверка: вывод в отчёте
- [x] 4.2 Собрать `l1-route-v5` тем же способом, что v4 (копия 01.09, noctx, сид 4242), сравнить число позиций с v4 (119) и число различных текстов; сгенерировать `form.html`. Проверка: `prepare` без `cannot be scored`, различных текстов = оцениваемых позиций
