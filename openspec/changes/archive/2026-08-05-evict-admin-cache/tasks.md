## 1. Политика вытеснения

- [x] 1.1 Задать пределы рядом с `ADMIN_CACHE_TTL_SECONDS` в `app/filters/admin_or_owner.py`: срок хранения записи и максимальное число записей — в том же стиле, что `state_ttl_sec` / `state_max_keys` у `ThrottlingMiddleware`
- [x] 1.2 Реализовать чистку по ходу обращения к кэшу: удаление протухших записей и вытеснение самых старых при переполнении
- [x] 1.3 Сверить порядок операций с `ThrottlingMiddleware._prune_state`, чтобы паттерн читался одинаково в обоих местах
- [x] 1.4 Убедиться, что чистка не выполняется на каждом обращении, если это заметно дороже самой проверки (у соседа для этого есть счётчик тиков)

## 2. Тесты

- [x] 2.1 Протухшая запись удаляется, а не только перезаписывается
- [x] 2.2 При переполнении вытесняются записи с самым давним обращением
- [x] 2.3 Недавно использованный чат не вытесняется
- [x] 2.4 После вытеснения права запрашиваются заново и решение о доступе не меняется
- [x] 2.5 Ошибка запроса прав по-прежнему означает отказ (fail-closed)

## 3. Закрытие

- [x] 3.1 Прогнать полный сьют, `ruff`, `mypy`
- [x] 3.2 Перенести пункт из `docs/OPEN.md` в `docs/CLOSED.md`
