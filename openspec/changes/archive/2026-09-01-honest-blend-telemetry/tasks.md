# Tasks — honest-blend-telemetry

## 1. Хеш до правок

- [x] 1.1 Снять `generation_hash` на прод-копии и синтетике, записать в
      `hash-log.md` с ожиданием «сдвига нет» (телеметрия, RNG не тронут)

## 2. Метрика

- [x] 2.1 `BlendedPool.raw_displacement` (дефолт 0.0) + расчёт на всех путях
      `blend()` (D2). Проверка: юнит-тесты — «короткий пуст» даёт
      raw_displacement > 0 при log-сжатии и displacement == 0; основной путь —
      оба поля; `Σcnt = 0` — 0.0 без деления
- [x] 2.2 Нейтральность: `alpha = 0` → `None` до арифметики (существующий
      контракт не задет). Проверка: существующие тесты нейтральности зелёные

## 3. Проводка

- [x] 3.1 `_WalkDiagnostics` + обе трассы + `GenerationTelemetry` + `/stats`
      + gen-трасса — `mean_blend_raw_displacement` рядом с существующей парой
      (D3). Проверка: юнит-тест агрегации; строка в `/stats` под тем же
      гейтом знаменателя
- [x] 3.2 Тест-репродукция дефекта карты: пул с пустым коротким слоем и
      log-сжатием — прежняя пара нули, новая метрика ненулевая; мутация
      «вернуть сравнение с long_p» роняет тест

## 4. Приёмка

- [x] 4.1 `generation_hash` после правок сверен с 1.1
- [x] 4.2 `ruff` + `mypy app/` + полный сьют, покрытие не ниже храповика
- [x] 4.3 `docs/GENERATION_MAP.md` §3.3 аннотировать закрытием (по §5 карты);
      синк спеки при архиве
