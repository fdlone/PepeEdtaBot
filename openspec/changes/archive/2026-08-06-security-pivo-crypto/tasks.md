## 1. Вывод ключа

- [x] 1.1 Вывести ключ Fernet через HKDF-SHA256 с меткой `pivo:fernet` (по образцу `app/log_masking.py`)
- [x] 1.2 Собрать `MultiFernet`: первым — новый ключ (шифрует), вторым — прежний `base64(sha256(secret))` (только читает)
- [x] 1.3 Тест: строка, зашифрованная прежней схемой, расшифровывается после перехода
- [x] 1.4 Тест: новая запись зашифрована новым ключом (прежним ключом не читается)

## 2. Ленивая перешифровка

- [x] 2.1 Убедиться, что `upsert` и ежедневное обновление профиля перезаписывают все зашифрованные поля целиком
- [x] 2.2 Тест: после обновления профиля подписчика строка читается только новым ключом

## 3. Порог секретов

- [x] 3.1 Поднять минимальную длину `PIVO_HMAC_SECRET` и `PIVO_ENCRYPTION_SECRET` до 32 символов в `load_settings`
- [x] 3.2 Сообщение об ошибке должно называть настройку и требуемую длину
- [x] 3.3 Тест: секрет длиной 16 символов останавливает старт
- [x] 3.4 Обновить `.env.example` и `docs/OPERATIONS.md` (как сгенерировать секрет)

## 4. Сравнение вызывающего с подписчиком

- [x] 4.1 Выбирать `user_hash` в `ChatMembersRepo.list_members`
- [x] 4.2 Сравнивать хеш вызывающего с хранимым через `hmac.compare_digest`, без расшифровки `encrypted_user_id`
- [x] 4.3 Тест: вызывающий исключён из списка упоминаний
- [x] 4.4 Тест: расшифровка `encrypted_user_id` для исключения вызывающего не выполняется (счётчик вызовов на моке)

## 5. Закрытие

- [x] 5.1 `ruff check app/ tests/ tools/ main.py`
- [x] 5.2 `mypy app/`
- [x] 5.3 `python -m unittest discover tests -v`
- [x] 5.4 README (раздел Privacy): схема вывода ключа, набор ключей, требования к секретам
