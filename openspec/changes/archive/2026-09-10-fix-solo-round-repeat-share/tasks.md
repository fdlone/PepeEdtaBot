## 1. Гард до починки

- [x] 1.1 Добавить в `tests/test_solo_rating_round.py` тест свойства: для 2, 3, 4 и 5 рук подготовленный `build_round` даёт долю повторов не ниже `manual_repeat_share_min`. Проверка: тест **падает** на текущем коде на 2 и 4 руках и проходит на 3 и 5 — если он зелёный сразу, он написан неверно и дефект не воспроизведён

## 2. Починка арифметики

- [x] 2.1 В `_round_entries` заменить `round(len(entries) * repeat_share)` на округление вверх (`math.ceil`). Проверка: тест 1.1 зелёный на всех четырёх числах рук
- [x] 2.2 Проверить, что число позиций и `counts` в ключе сошлись с новым числом повторов и что `score_round` на синтетическом раунде из 1.1 больше не называет причину про долю повторов. Проверка: прогон `build_round` + `score_round` в тесте, без БД

## 3. Разведение повторов

- [x] 3.1 Заменить существующий `test_repeats_do_not_sit_next_to_each_other` на перебор сидов и чисел рук. Проверка: тест **падает** на текущем коде — свойство держалось на одном удачном сиде, поэтому односидовый тест годами подтверждал то, чего в коде нет
- [x] 3.2 Разводить повторы вставкой дублей в перемешанный список различных элементов, в слот не рядом с близнецом (design Р3), вместо общего `rng.shuffle`. Проверка: тест 3.1 зелёный
- [x] 3.3 Убедиться, что остальные свойства списка не пострадали: раунд воспроизводим по сиду, декои сохранены, ключ сходится с листингом. Проверка: весь `BuildRoundTest` и `SyntheticSnapshotTest` зелёные

## 4. Предупреждение при подготовке

- [x] 4.1 В `prepare` после сборки списка прогнать `score_round` на синтетических «идеальных» ответах (все настоящие связны, все декои опознаны) и напечатать причины, если раунд негоден по составу. Порог не дублировать — брать из уже загруженных `thresholds` (design Р2). Проверка: тест, где подготовка с искусственно заниженной долей повторов печатает причину, а нормальная — нет
- [x] 4.2 Убедиться, что вывод остаётся счётчиками и путями, без текстов ответов (privacy-инвариант capability). Проверка: существующий тест на отсутствие текстов в stdout зелёный; при его отсутствии — добавить

## 5. Документация протокола

- [x] 5.1 Обновить `docs/SOLO_RATING_ROUND.md`: в «Шагах» сказать, что подготовка проверяет собранный раунд на выполнимость и называет причину; в таблице «Как гейт читает результат» уточнить, что доля повторов — свойство подготовки, а не оценщика. Проверка: перечитать раздел — противоречий с новым поведением нет

## 6. Фиксация раунда pool-c8s40 (O19)

- [x] 6.1 Написать вердикт-заметку в `docs/eval_reports/` по образцу соседних: раунд **непосчитываем**, причина — «дефект подготовки, не качество оценки»; цифры обоих оценщиков (само-согласие 1.00 и 0.96 при пороге 0.80, декои 13/13 и 12/13 при пороге 0.80, доля повторов 26/132 = 19.70% при пороге 20%); межоценочное согласие 0.50 точное / 0.68 бинарное; дельты к C0 в пределах ±3.3 п.п. у обоих. Проверка: заметка содержит только числа, без дословных ответов
- [x] 6.2 Записать в заметке ограничение, снижающее вес r2: доли 93–100% — потолок, на котором рубрика почти не различает руки, опора на r1. Проверка: ограничение названо явно, а не выведено читателем
- [x] 6.3 Обновить запись O19 в `docs/OPEN.md`: раунд связности собран и непосчитываем по названной причине, гейт не снят, решение «пересобирать или засчитывать» за владельцем. Проверка: запись не читается как «связность потеряна»

## 7. Приёмка

- [x] 7.1 `ruff`, `mypy app/`, весь `unittest` зелёные; покрытие не ниже `fail_under = 87`. Проверка: команды запущены, вывод приложен к отчёту
- [x] 7.2 Подтвердить, что `generation_hash` не сдвинулся: `tests/test_generation_hash_baseline.py` зелёный. Проверка: заявлено «не затрагивается» в proposal — факт обязан совпасть, расхождение = стоп и отчёт владельцу
