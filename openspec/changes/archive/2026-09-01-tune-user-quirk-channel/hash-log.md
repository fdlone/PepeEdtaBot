# hash-log — tune-user-quirk-channel

## До правок (2026-09-01, база `264bb77` + правки документов)

Значения унаследованы от `route-slot-budget/hash-log.md` (снято на той же
базе; после него в ветке менялись только документы и архивы OpenSpec):

- прод-копия `db_prod_copy/markov.db` (ротация 01.09):
  `f108b13da3571bb080290e6e37d56a5713f502c3b4bbfc62e8641ff6a1c3d54a`
- синтетика: `555f552a76cb84899c40d3b49069b96b8100f9d65203125641820712af6635f9`
  (baseline OK)

## Ожидание — записано до правок

Сдвига **нет ни на одном снимке**: канал причуд исполняется в
`ReplyPipeline._apply_user_quirk` после `generate_with_result`, розыгрыш
берёт из модульного `random`, а не из `rng` генерации; `tools/generation_hash`
конвейер ответа не вызывает (design D3). Расхождение = правка задела не то,
работа останавливается (§2 `CLAUDE.md`).

## После правок (2026-09-01)

- синтетика: `555f552a76cb84899c40d3b49069b96b8100f9d65203125641820712af6635f9`
  (baseline OK, `--synthetic --check`)
- прод-копия: `f108b13da3571bb080290e6e37d56a5713f502c3b4bbfc62e8641ff6a1c3d54a`

Ожидание подтвердилось: байт-в-байт на обоих снимках, перезаморозка не нужна.
