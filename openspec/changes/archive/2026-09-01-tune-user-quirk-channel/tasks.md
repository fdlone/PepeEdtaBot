# Tasks — tune-user-quirk-channel

## 1. Заявка по хешу — до правок

- [x] 1.1 Записать в `hash-log.md` текущие значения `generation_hash` на
      синтетике и прод-копии и ожидание **до** правок: сдвига нет ни на одном
      снимке (канал живёт после генерации, design D3)

## 2. Рабочая точка

- [x] 2.1 `user_quirk_chance` в `app/config/registry.py`: `0.1` → `0.3`;
      `user_quirk_min_interactions`: `25` → `10`; обоснование в комментарии
      реестра — ссылка на воронку 01.09
- [x] 2.2 `.env.example`: те же значения, пояснение про рабочую точку
      (тест `test_settings` сверяет файл с реестром)
- [x] 2.3 Тесты, пиннящие прежние дефолты, — обновить; проверить, что суточный
      лимит и порядок гейтов не задеты (существующие тесты воронки зелёные)

## 3. Приёмка

- [x] 3.1 `ruff`, `mypy app/`, полный сьют; покрытие не ниже храповика
- [x] 3.2 `generation_hash` после правок сверен с 1.1 на обоих снимках,
      факт записан в `hash-log.md`
- [x] 3.3 `docs/GENERATION_MAP.md` §2.1 — строка `user_quirk_*`;
      `docs/CLOSED.md` — запись с числами воронки, новой рабочей точкой и
      решающим правилом D2; `docs/OPEN.md` — окно наблюдения канала

## 4. Окно наблюдения — после выкатки

- [ ] 4.1 Сутки активности без рестартов, снять воронку из `/stats`
- [ ] 4.2 Прочитать по таблице D2; при отклонении — разбор, а не подкрутка
