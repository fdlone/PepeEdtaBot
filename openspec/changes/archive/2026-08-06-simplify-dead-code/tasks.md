## 0. Базовая линия

- [x] 0.1 Зафиксировать `tools/generation_hash.py` до изменений

## 1. Неиспользуемое

- [x] 1.1 Удалить константу минимальной длины обучаемого сообщения, зафиксировав комментарием, чем обеспечена нижняя граница
- [x] 1.2 Удалить проверку существования сообщения в базе (`Database.message_exists`, `MessagesRepo.exists`) и её тесты
- [x] 1.3 Проверено: обёртка генерации без трассировки — вторая форма публичного API (около пятнадцати вызывающих тестов), не мёртвый код. Оставлена с пояснением; см. раздел «Что не удалено и почему»
- [x] 1.4 Удалить обёртку отправки ответа с явными таймингами, тесты перевести на используемый вариант
- [x] 1.5 Обёртка сборки сообщения `/pivo` перенесена в тестовый модуль: из продового кода ушла, краткость вызова у тестов осталась
- [x] 1.6 Перенести построение окон 4-грамм по тексту в инструмент оценки, который им пользуется
- [x] 1.7 Удалить поле «похожести» из результата сопоставления состояний и из ключа сортировки
- [x] 1.8 Перед каждым удалением — проверка отсутствия вызовов из `app/`, `main.py`, `tools/`

## 2. Дубли и лишние вычисления

- [x] 2.1 Оставить строку запасного обращения в одном модуле, второй перевести на импорт
- [x] 2.2 Тест: подавление строки с упоминаниями срабатывает при запасном обращении (защита от расхождения констант)
- [x] 2.3 Конструирование переведено на именованные аргументы (в этом и был дефект). Типы не объединены: у них разный смысл — суммарная трассировка против одной попытки; обосновано в proposal
- [x] 2.4 Убрать вычисление релевантности контекста там, где оно всегда перезаписывается
- [x] 2.5 Свести нормализацию кандидата к одной функции с явными вариантами

## 3. Закрытие

- [x] 3.1 `ruff check app/ tests/ tools/ main.py`
- [x] 3.2 `mypy app/`
- [x] 3.3 `python -m unittest discover tests -v`
- [x] 3.4 Хеш генерации совпадает с базовой линией
- [x] 3.5 Покрытие не ниже действующего порога
