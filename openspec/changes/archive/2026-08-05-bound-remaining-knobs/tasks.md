## 1. Границы

- [x] 1.1 `hot_ngram_min_count` → `_int_in_range(1, 1000)`: порог «сколько раз n-грамма встретилась в окне»; при тысяче ни одна n-грамма чата в окно не укладывается, то есть локальные мемы выключены
- [x] 1.2 `rare_event_daily_cap` → `_int_in_range(0, 100)`: суточный бюджет редких событий на чат; сотня в сутки — это уже не бюджет, а его отсутствие
- [x] 1.3 `user_quirk_min_interactions` → `_int_in_range(1, 10000)`: счётчик обращений затухает за ~30 дней (`CHAT_USER_INTERACTION_DECAY_DAYS`), так что десять тысяч недостижимы — причуды завсегдатаев просто не сработают
- [x] 1.4 `reply_context_max_tokens` → `_int_in_range(2, 300)`: масштаб взят от соседнего `max_reply_tokens` (`1..300`); контекст длиннее самого длинного ответа матчеру бесполезен, но стоит латентности
- [x] 1.5 Обосновать каждое число комментарием в реестре, как у соседей

## 2. Тесты

- [x] 2.1 Перенести четыре ручки из `KNOWN_UNBOUNDED` в `BOUNDED` (`tests/test_registry.py`) — там уже проверяются граница, значение за границей и наличие диапазона в тексте отказа
- [x] 2.2 Убедиться, что `KNOWN_UNBOUNDED` сократился до `typing_min_ms`, и объяснить в комментарии, почему он остаётся (транзитивная граница через `typing_min <= typing_max`)
- [x] 2.3 Проверить, что тест от протухания списка по-прежнему осмыслен на сократившемся списке

## 3. Проверка и закрытие

- [x] 3.1 Сверить `.env` и `.env.example` с новыми диапазонами
- [x] 3.2 Задокументировать диапазоны в `.env.example`
- [x] 3.3 Полный сьют, `ruff`, `mypy`
- [x] 3.4 Закрыть пункт из бэклога `docs/OPEN.md` записью в `docs/CLOSED.md`
