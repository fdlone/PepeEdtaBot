# Tasks: fix-eval-manual-summary-and-baseline-drift

Блок 1 независим от остальных и делается первым — он маленький и закрывает
находку целиком. Блоки 2–5 идут по порядку: класс расхождения (блок 2)
определяет, что именно чинится в блоке 4.

## 1. Секция ручной разметки в отчёте (D1)

- [x] 1.1 Секция «Manual eval summary» печатает переданный агрегат: размечено/признано настоящими по обеим половинам, приманки, число оценщиков, согласованность
- [x] 1.2 Формулировка «раунд не проводился» достижима только при отсутствующем `--manual-rating`
- [x] 1.3 Тесты на оба случая — с переданным рейтингом и без него
- [x] 1.4 Перегенерировать `eval_2026-08-13_phase4-verdict.md` тем же прогоном, чтобы вердикт фазы 4 лежал в репозитории без внутреннего противоречия

## 2. Бисект расхождения хеша (D4)

- [x] 2.1 Зафиксировать решающее правило из design.md D4 в рабочих заметках **до** прогонов — оно уже записано, задача в том, чтобы не переписать его после результата
- [x] 2.2 Прогнать `python -m tools.generation_hash --db db_prod_copy/markov.db` на каждой ревизии диапазона `3e60891`…`2ec6bda`, записать значения — `app/` в диапазоне трогает только `2ec6bda`; измерено на границах: `3e60891` → `5a72e2d4…`, `e2eb904` (родитель #148) → `5a72e2d4…`, `2ec6bda` → `13e496c0…`
- [x] 2.3 Назвать коммит (или коммиты), сдвинувшие хеш, и конкретную правку внутри него — `2ec6bda` (#148), строка `order_mix_probability=state.order_mix_probability` в `_extend_verbatim_candidate` (`app/core/response_generator.py`). Подтверждено измерением, а не чтением: откат ровно этой строки на `2ec6bda` возвращает `5a72e2d4…`
- [x] 2.4 Классифицировать по правилу 2.1: утечка V2-фичи при нейтральных ручках / законный сдвиг общего пути 1.x — **законный сдвиг 1.x**. `order_mix_probability` — ручка 1.x с дефолтом 0.75; до правки verbatim-расширение звало обход без неё (дефолт параметра 0.0), то есть шло по другой политике order-mix, чем основной обход. Правка устраняет это расхождение и действует независимо от любых ручек V2 ⇒ ветка 4.2
- [x] 2.5 Если сдвигов было несколько — зафиксировать все и взять худший класс — сдвиг один, границы измерены

## 3. Запись ожидаемого хеша и сверка (D2, D3)

- [x] 3.1 Записать ожидаемое значение для синтетического снапшота: хеш, снапшот, ревизия, **причина** (почему значение таково)
- [x] 3.2 `generation_hash` получает режим сверки: совпало — успех; разошлось — печать обоих значений и ненулевой код возврата
- [x] 3.3 Ручная печать хеша без сверки сохраняется для сравнения ревизий на прод-копии
- [x] 3.4 Тесты: совпадение, расхождение (ненулевой код и оба значения в выводе), отсутствие записи для незнакомого снапшота
- [x] 3.5 Подключить сверку к CI там же, где сейчас запускается смок ядра генерации

## 4. Действие по итогу бисекта (взаимоисключающие ветки)

- [ ] 4.1 **Если утечка V2-фичи:** починить код так, чтобы при нейтральных ручках вывод снова совпадал с `5a72e2d4`; тест на нейтральность; подтвердить хеш на прод-копии до и после — **ветка не выбрана**: бисект дал законный сдвиг 1.x (2.4), код не трогался
- [x] 4.2 **Если законный сдвиг 1.x:** перезакрепить запись на новое значение с указанием изменения-источника и причины; код не трогать — запись заведена на синтетическом снапшоте (`555f552a…`, измерена дважды), `reason` называет и происхождение значения, и то, что прод-копейный `5a72e2d4 → 13e496c0` — законный сдвиг из #148
- [x] 4.3 Ни при каком исходе не подгонять запись под текущий вывод без названной причины — тест `test_record_is_complete` требует непустой `reason` в записи, то есть правило проверяется, а не только декларируется

## 5. Документация

- [x] 5.1 Привести в соответствие места, повторяющие `5a72e2d4`: `docs/v2/00_STATUS.md`, `docs/GENERATION_PIPELINE.md`, `docs/CLOSED.md` — с указанием, где теперь живёт нормативное значение — `GENERATION_PIPELINE.md` §5 больше не называет число и отсылает к §10.1; `00_STATUS.md` получил сноску под таблицей фаз. `CLOSED.md` и eval-отчёты **намеренно не тронуты**: это записи измеренного тогда, переписывать их значило бы подменять историю
- [x] 5.2 Записать сам факт дрейфа и его причину (не только новое значение) — иначе через полгода это прочтётся как «хеш всегда был таким» — `GENERATION_PIPELINE.md` §10.1: коммит-источник, конкретная строка, объяснение (расширение шло с чужой политикой order-mix) и как подтверждено (откат строки возвращает прежний хеш)
- [x] 5.3 Отметить в `docs/OPEN.md`, если по итогу остаётся незакрытый хвост — хвоста нет: причина установлена, класс определён, запись заведена и сверяется в CI. В `OPEN.md` добавлять нечего

## 6. Close-out

- [x] 6.1 `openspec validate --strict` зелёный для этого change'а
- [x] 6.2 Полный прогон тестов + ruff + mypy — 1174 unittest OK, ruff чисто, mypy 75 файлов без ошибок; дополнительно `--synthetic --check` зелёный
- [x] 6.3 Архивировать change после мержа — влит PR #150 (`d7743e9`), архивирован 2026-08-13 с синхронизацией дельты в `generation-eval`
