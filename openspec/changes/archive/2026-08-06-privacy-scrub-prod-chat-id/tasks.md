## 1. Замена в тестах и фикстурах

- [x] 1.1 Заменить реальный идентификатор чата на синтетический в `tests/test_log_masking.py`
- [x] 1.2 То же в `tests/test_error_handler.py`
- [x] 1.3 То же в `tests/test_migrator.py` и `tests/fixtures/legacy_real_schema.sql`
- [x] 1.4 Убедиться, что синтетические значения сохраняют форму (отрицательное число нужной длины), иначе проверки маскирования потеряют смысл

## 2. Документация и результаты прогонов

- [x] 2.1 Заменить идентификатор в `docs/CLOSED.md`
- [x] 2.2 Вычистить идентификатор из `tools/sweeps/*_results.jsonl` (файлы оставлены отслеживаемыми: документы ссылаются на них поимённо, а метрики от идентификатора не зависят)
- [x] 2.3 Записать причину в `tools/sweeps/README.md`: результаты прогонов на копии прода не коммитятся
- [x] 2.4 Добавить `scrin/` в `.gitignore`

## 3. Защита от повторения

- [x] 3.1 Тест: в отслеживаемых исходниках проекта нет строк, распознаваемых как идентификатор чата (та же форма, что ищет `mask_chat_ids_in_text`)
- [x] 3.2 Тест не должен ломаться на синтетических значениях — выбрать форму, которую санитайзер не считает идентификатором, либо явно занести в исключения по файлам

## 4. Закрытие

- [x] 4.1 `ruff check app/ tests/ tools/ main.py`
- [x] 4.2 `mypy app/`
- [x] 4.3 `python -m unittest discover tests -v`
- [x] 4.4 README: раздел Privacy — правило про артефакты репозитория
