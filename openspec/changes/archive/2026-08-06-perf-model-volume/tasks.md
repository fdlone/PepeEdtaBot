## 1. Чтение объёма из счётчика

- [x] 1.1 Добавить в `MarkovRepo` (или отдельный репозиторий) чтение `volume2`/`volume3` из `chat_model_volume`
- [x] 1.2 Перевести `Database.get_chat_token_volume` на это чтение с той же семантикой «volume3, иначе volume2»
- [x] 1.3 Реализовать разовый бэкфилл: если строки для чата нет, посчитать полный агрегат, записать строку и вернуть значение
- [x] 1.4 Тест: значение совпадает с полным агрегатом после серии обучений
- [x] 1.5 Тест: чат без строки в `chat_model_volume` получает корректное значение и строка появляется
- [x] 1.6 Тест: после `clear_chat` объём равен нулю

## 2. `/stats` без лишних агрегатов

- [x] 2.1 Перевести `/stats` на чтение одного счётчика вместо полного среза по чату
- [x] 2.2 Полный срез оставить как диагностику (по нему тесты проверяют записи в базу), но объёмы в нём читать из счётчика; сделать его read-only — без побочной записи строки счётчика
- [x] 2.3 Тест: `/stats` печатает объём и не вызывает полный срез

## 3. Закрытие

- [x] 3.1 `ruff check app/ tests/ tools/ main.py`
- [x] 3.2 `mypy app/`
- [x] 3.3 `python -m unittest discover tests -v`
