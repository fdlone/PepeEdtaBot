## 1. Санитайзер

- [x] 1.1 Добавить в `app/log_masking.py` функцию маскирования идентификаторов чатов внутри произвольного текста (D1, D2)
- [x] 1.2 Обработать отказ маскирования: `MaskingNotInitialized` не пробрасывается, на месте идентификатора — заглушка (D3)
- [x] 1.3 Задокументировать в докстринге модуля, что инвариант распространяется и на тексты сторонних исключений

## 2. Точка применения

- [x] 2.1 Прогнать текст `TelegramAPIError` через санитайзер в `app/handlers/errors.py` перед логированием
- [x] 2.2 Убедиться, что применяется ко всем `TelegramAPIError`, а не к перечисленным классам
- [x] 2.3 Проверить, что диагностика (тип ошибки, метод, `retry_after`) остаётся в записи

## 3. Тесты

- [x] 3.1 Текст в форме `TelegramRetryAfter` («Flood control exceeded on method … in chat -100…») — идентификатор замаскирован
- [x] 3.2 Текст в форме `TelegramMigrateToChat` с целевым идентификатором — замаскирован
- [x] 3.3 Отрицательные случаи: время ожидания, код ошибки, идентификатор сообщения не искажены
- [x] 3.4 Один чат в двух записях даёт одинаковую маску
- [x] 3.5 Логирование при неинициализированном маскировании: заглушка, без исключения, запись состоялась

## 4. Закрытие

- [x] 4.1 Прогнать полный сьют, `ruff`, `mypy`
- [x] 4.2 Проверить README: формулировка инварианта маскирования покрывает и этот случай
- [x] 4.3 Перенести пункт из `docs/OPEN.md` в `docs/CLOSED.md`
