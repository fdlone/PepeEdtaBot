## 1. Счётчик уборки

- [x] 1.1 Перенести счётчик периодической уборки в общее изменяемое хранилище (например, список/словарь), чтобы поверхностная копия работала с тем же значением
- [x] 1.2 Проверить остальные поля `RuntimeState` на тот же класс дефекта: изменяемые скаляры вне словарей быть не должно
- [x] 1.3 Тест: активность через представление чата с переопределениями двигает счётчик базового состояния
- [x] 1.4 Тест: уборка по сроку хранения запускается для чата с переопределениями
- [x] 1.5 Тест: поведение чата без переопределений не изменилось (представление — тот же объект)

## 2. Отметка затухания статистики

- [x] 2.1 Обновлять отметку времени только после успешного выполнения затухания
- [x] 2.2 Тест: исключение внутри затухания не откладывает следующую попытку на сутки

## 3. Токенизация в финальной проверке

- [x] 3.1 Проверено: дефектом не является — обе стороны сравнения уже согласованы по регистру. Правка отменена, инвариант объяснён в коде (см. раздел «Что оказалось не дефектом» в proposal)
- [x] 3.2 Характеризационные тесты и хеш генерации прогнаны: изменений нет
- [x] 3.3 Тест не нужен: поведение не менялось

## 4. Закрытие

- [x] 4.1 `ruff check app/ tests/ tools/ main.py`
- [x] 4.2 `mypy app/`
- [x] 4.3 `python -m unittest discover tests -v`
