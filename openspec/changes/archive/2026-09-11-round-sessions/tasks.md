# Tasks — round-sessions

## 1. Инструмент

- [x] 1.1 Инструкция «повтор оценивать заново» в `HEADER` списка; шаблон формы `tools/rating_form_template.html` с той же инструкцией и без текстов чата; `render_form` / `session_items`; тест: форма без плейсхолдеров, инструкция в списке и шаблоне
- [x] 1.2 Сессии: `_split_into_sessions` (декои по кругу, реальные в самый короткий блок), повторы и разведение внутри блока, сквозная нумерация, заголовки сессий в списке, `session` в ключе, `per_session` в счётчиках; `prepare --session-size` (дефолт 25) пишет `form_s<N>.html`; тесты: повтор и декой не выходят за сессию, доля повторов по сессии ≥ порога, нумерация сквозная, короткий раунд — одна сессия
- [x] 1.3 `score`: несколько `--answers`, диагностика по сессиям в агрегате и в печати; старые ключи без `session` читаются; тест агрегата по сессиям

## 2. Приёмка

- [x] 2.1 `ruff`, `mypy app/` (инструмент вне `app/`, но проверить), полный `unittest`; пробная подготовка на синтетике не нужна — покрыто `SyntheticSnapshotTest`
- [x] 2.2 `docs/SOLO_RATING_ROUND.md`: устройство (сессии), оценка (инструкция), подсчёт (несколько файлов, диагностика), известные ограничения (дрейф — теперь вне контроля по построению); OPEN — снять «мелкую заявку» из O21/CLOSED-остатков
