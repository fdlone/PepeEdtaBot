## 1. Порт

- [x] 1.1 Описать в `app/core` протокол чтения модели: старты (2/3), переходы (2/3), точечные проверки старта, состояния цепи, объём токенов, частоты слов, индекс 4-грамм
- [x] 1.2 Включить в протокол только то, что реально вызывает ядро

## 2. Переход на порт

- [x] 2.1 Перевести `MarkovGenerator` на порт вместо `Database`
- [x] 2.2 Перевести `ContextStateMatcher` на порт
- [x] 2.3 Убедиться, что `Database` удовлетворяет протоколу без изменений сигнатур
- [x] 2.4 `mypy app/` подтверждает соответствие

## 3. Устранение дублей

- [x] 3.1 Убрать из `app/infrastructure/database.py` копии набора пунктуации, размера n-граммы и функции построения окон; импортировать их из ядра
- [x] 3.2 Убедиться, что цикла импортов больше нет
- [x] 3.3 Оставить копию в миграции с комментарием о причине (застывшая история)
- [x] 3.4 Тест: окна 4-грамм, записываемые при обучении, совпадают с теми, что считает оценка кандидатов

## 4. Закрытие

- [x] 4.1 `ruff check app/ tests/ tools/ main.py`
- [x] 4.2 `mypy app/`
- [x] 4.3 `python -m unittest discover tests -v`
- [x] 4.4 `docs/ARCHITECTURE.md`: направление зависимостей
