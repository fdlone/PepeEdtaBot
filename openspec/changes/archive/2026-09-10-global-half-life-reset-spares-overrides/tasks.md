## 1. Гард до починки

- [x] 1.1 В `tests/test_handlers.py` добавить тест: `/set global markov_short_half_life_days 7` при оверрайде ключа у чата 200 (и без него у чата 100) вызывает сброс с исключением `{200}` и упоминает в ответе один сохранённый чат. Проверка: тест **падает** на текущем коде (сброс вызван с `None` без исключений)
- [x] 1.2 В `tests/test_markov2r_phase3_temporal.py` добавить тест: `reset_short_layer(None, exclude={88})` обнуляет слой чата 77 и не трогает 88; пустой `exclude` ведёт себя как прежний глобальный сброс. Проверка: тест **падает** на текущем коде (`TypeError` по сигнатуре)

## 2. Починка

- [x] 2.1 `Database.reset_short_layer(chat_id=None, *, exclude=())`: при `chat_id is None` и непустом `exclude` — `WHERE chat_id NOT IN (?, …)` с параметрами по всем четырём таблицам; при заданном `chat_id` непустой `exclude` — `ValueError` (design Р3). Проверка: тест 1.2 зелёный
- [x] 2.2 `LearningService.reset_short_layer(chat_id, *, exclude=())` пробрасывает исключения в БД; инвалидация кэшей не меняется. Проверка: тест 1.2 через сервис или существующие тесты сервиса зелёные
- [x] 2.3 В глобальной ветке `cmd_set` собрать `exclude` из `runtime_state_base.chat_overrides` (чаты, у которых есть ключ `markov_short_half_life_days`), передать в сброс и при непустом наборе дописать в предупреждение строку со счётчиком, без ID чатов (design Р4). Проверка: тест 1.1 зелёный; существующие тесты глобальной/пер-чатовой формы и `reset` не изменились и зелёные

## 3. Документация

- [x] 3.1 `docs/GENERATION_MAP.md` §3.9б: пометить закрытым с датой и ссылкой на change; строку в таблице §4 (`markov_short_half_life_days` → долговечность данных) поправить: глобальный сброс щадит чаты с оверрайдом. Проверка: в карте нет утверждений, противоречащих новому поведению
- [x] 3.2 `docs/PRE_ROADMAP.md` M3R-150: пункт §3.9б вычеркнуть как сделанный с датой. Проверка: перечитать абзац
- [x] 3.3 Если текст ответа `/set` описан в пользовательской справке или `docs/OPERATIONS.md` — дополнить одной фразой про сохранённые чаты. Проверка: `grep "короткий слой" docs/ app/` — все упоминания согласованы

## 4. Приёмка

- [x] 4.1 `ruff`, `mypy app/`, весь `unittest` зелёные; покрытие не ниже `fail_under = 87`. Проверка: команды запущены в `.venv`, вывод в отчёте
- [x] 4.2 `tests/test_generation_hash_baseline.py` зелёный — хеш не сдвинулся, как заявлено в proposal. Проверка: расхождение = стоп и отчёт владельцу
