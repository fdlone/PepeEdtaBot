# Tasks — extension-seam-accounting

## 1. Хеш до правок

- [x] 1.1 Снять `generation_hash` на прод-копии и синтетике, записать в
      `hash-log.md` с ожиданием «сдвига нет» (поле результата и оффлайн-харнесс)

## 2. Маршрут победителя в результате

- [x] 2.1 `ResponseGenerationResult.winner_route: str | None = None`,
      заполняется `selected.route` (D1). Проверка: юнит-тест — победитель-
      дописка отдаёт `extension`, пустой ответ — `None`

## 3. Честный числитель в харнессе

- [x] 3.1 `tools/eval_prod.py`: числитель `connective_reply_rate` = текст-скан
      ∨ (маршрут победителя `extension` ∨ `jump_count > 0`) (D2). Проверка:
      юнит-тест на решающую функцию — молчаливая дописка считается, ответ без
      шва и связок — нет
- [x] 3.2 Отдельная метрика `extension_seam_rate` в отчёте свипа (D3) +
      обозначение разрыва сопоставимости в выводе (D4). Проверка: обе строки
      присутствуют в отчёте прогона

## 4. Приёмка

- [x] 4.1 `generation_hash` после правок сверен с 1.1
- [x] 4.2 `ruff` + `mypy app/` + полный сьют, покрытие не ниже храповика
- [x] 4.3 `docs/GENERATION_MAP.md` §3.5 аннотировать закрытием, §4 —
      обновить строку про нижнюю оценку доли швов (по §5 карты); синк спеки
      при архиве
