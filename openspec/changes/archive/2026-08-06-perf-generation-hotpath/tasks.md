## 0. Базовая линия

- [x] 0.1 Зафиксировать `tools/generation_hash.py` до изменений
- [x] 0.2 Замерить задержку генерации на копии прода (несколько зёрен), записать в предложение

## 1. Порядок в кэшах генератора

- [x] 1.1 Порядок обеспечивает источник: запросы стартов и переходов уже идут с ORDER BY, поэтому сортировка в кэше не нужна — предусловие закреплено тестом `test_transition_rows_are_ordered`
- [x] 1.2 Убрать сортировку из `weighted_next_choice`, `weighted_start2_choice`, `weighted_start3_choice`
- [x] 1.3 Проверить все пути, подающие в эти функции несортированные списки (варианты после подстановки, объединённые списки кандидатов) — либо сортировать на входе, либо сортировать в источнике
- [x] 1.4 Тест: хеш генерации совпадает с базовой линией

## 2. Индекс частотного словаря по окончанию

- [x] 2.1 Строить индекс «окончание → слова» один раз при загрузке частот чата
- [x] 2.2 Перевести подбор замены на обход корзины вместо всего словаря
- [x] 2.3 Сохранить остальные фильтры и порядок их применения (частота, длина, морфология, защищённые слова)
- [x] 2.4 Тест: множество кандидатов на замену совпадает с полученным полным обходом

## 3. Агрегат горячих n-грамм

- [x] 3.1 Ограничить подзапрос по всей таблице переходов теми n-граммами, что присутствуют в окне
- [x] 3.2 Тест: выдача «горячих» n-грамм не изменилась на подготовленных данных

## 4. Поиск общей подпоследовательности

- [x] 4.1 Заменить алгоритм на линейно-квадратичный с тем же результатом
- [x] 4.2 Тест: результат совпадает с прежней реализацией на наборе случайных входов

## 5. Закрытие

- [x] 5.1 `ruff check app/ tests/ tools/ main.py`
- [x] 5.2 `mypy app/`
- [x] 5.3 `python -m unittest discover tests -v`
- [x] 5.4 Повторный замер задержки, сравнение с базовой линией
