# Tasks — knob-census (M3R-151)

## 1. До прогона

- [x] 1.1 `hash-log.md`: ожидание «сдвига нет» (правки только в `tools/eval`)
- [x] 1.2 Блок `knob_census` в `eval_thresholds.yaml` — полоса допуска и планка
      силы по метрикам, с обоснованием, до прогона

## 2. Инструмент

- [x] 2.1 `tools/eval/knob_census.py`: `plan` (ручки, экстремумы, родители),
      `run` (C0 или подмножество ручек, режим → JSON выборок), `launch`
      (параллельные отсоединённые процессы), `report` (классы, таблица)
- [x] 2.2 Тесты: разбор подсказок домена в экстремумы; булева — инверсия;
      экстремум = дефолт пропускается; классификация на синтетических
      выборках (inert / weak / strong / gated); отчёт без текста ответов

## 3. Прогон

> Первый запуск 2026-09-02 прерван владельцем (воркеры закрыты вручную).
> Повтор 2026-09-02 с 10 воркерами без окон завершён за 34 минуты. Команда:
> `python -m tools.eval.knob_census launch --db db_prod_copy/markov.db --out <каталог> --workers 18`,
> затем `report --inputs <каталог> --out docs/eval_reports/eval_<дата>_knob-census.md`.

- [x] 3.1 C0 в двух режимах, сверка с базлайном версии `bababb4b7693`
- [x] 3.2 Все измеримые ручки в двух режимах на прод-копии 01.09
- [x] 3.3 `docs/eval_reports/eval_2026-09-02_knob-census.md` — таблица классов
      с числами и предложениями по строкам

## 4. Приёмка

- [x] 4.1 `ruff`, `mypy app/`, полный сьют, покрытие
- [x] 4.2 `generation_hash --synthetic --check`, факт в `hash-log.md`
- [x] 4.3 Роадмап (M3R-151 → вход M3R-150), карта §2.1, CLOSED/OPEN, README
