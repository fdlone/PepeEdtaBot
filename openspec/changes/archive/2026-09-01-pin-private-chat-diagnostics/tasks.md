# Tasks — pin-private-chat-diagnostics

## 0. Правка поведения

- [x] 0.1 Добавить `GROUP_ONLY` к хендлеру `/config`
      (`app/handlers/admin.py:75`). Проверка: существующие тесты `/config`
      зелёные (при необходимости — с групповым чатом в фикстуре)

## 1. Тест состава исключений

- [x] 1.1 Написать тест, который обходит зарегистрированные командные хендлеры
      роутеров приложения и собирает множество команд, у которых нет фильтра
      `GROUP_ONLY`. Проверка: после 0.1 множество равно `{ping, help}` и тест
      зелёный
- [x] 1.2 Сообщение об ошибке теста называет разошедшиеся команды поимённо
      (и «лишние в личке», и «пропавшие из личек»). Проверка мутацией:
      временно снять `GROUP_ONLY` со `/stats` — тест падает и называет
      `stats`; временно навесить `GROUP_ONLY` на `/ping` — тест падает и
      называет `ping`. Результат обеих мутаций записать в комментарий теста

## 2. Документация

- [x] 2.1 README, раздел «Команды»: заменить абзац «Личный чат … не является
      поддерживаемым рабочим сценарием» на формулировку «рабочий сценарий —
      групповой; в личке доступны `/ping` и `/help` как канал диагностики,
      туда же приходят отчёты владельцу». Отметить доступность в личке в
      таблице команд. Проверка: в README не осталось утверждения,
      противоречащего коду
- [x] 2.2 Обновить `docs/OPEN.md`: O13 закрыт решением «личка — диагностический
      канал `/ping`+`/help`, `/config` — в группы» (2026-09-01, причина: у
      владельца нет доступа к докеру и логам); перенести запись в
      `docs/CLOSED.md`. Проверка: строки O13 в OPEN.md нет, в CLOSED.md есть
      с датой и решением

## 3. Приёмка

- [x] 3.1 `ruff` + `mypy app/` + `unittest` зелёные, покрытие не ниже храповика.
      Проверка: команды выполнены, вывод чистый. Влияние на `generation_hash`
      заявлено: отсутствует по построению (тронут только хендлер `/config`,
      путь генерации не задет) — снятие снимков не требуется
