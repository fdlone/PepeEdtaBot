# Tasks — db-snapshot-to-owner

## 1. Модуль снимка

- [x] 1.1 `app/services/db_snapshot.py`: функция снятия снимка (`VACUUM INTO`
      во временный файл коротким `sqlite3`-соединением, в
      `asyncio.to_thread`) + gzip; отсутствие файла БД → `None` без ошибки.
      Проверка: юнит-тест на временной БД с синтетическими ID — снимок
      открывается sqlite и содержит те же данные; тест первого старта без БД
- [x] 1.2 Отправка: `send_document` владельцу с именем
      `markov-YYYYMMDD-HHMMSS.db.gz` (D6); файл > лимита Telegram → текстовое
      предупреждение вместо документа (D3); временные файлы удаляются в
      `finally`. Проверка: тесты с моком `Bot` — документ ушёл, предупреждение
      при превышении, temp-каталог пуст после обеих веток
- [x] 1.3 Любое исключение ступени → `logger.warning`, наружу не пробрасывается.
      Проверка: тест «ошибка отправки не поднимается и пишет warning»
      (образец — `test_delivery_failure_does_not_break_the_message_path`)

## 2. Настройка

- [x] 2.1 `Settings`: булева env-настройка `DB_SNAPSHOT_TO_OWNER`, дефолт
      `true`, парсинг через существующий `_parse_bool`. Проверка: юнит-тесты
      на дефолт и `false`
- [x] 2.2 `.env.example`: строка `DB_SNAPSHOT_TO_OWNER=true` с пояснением;
      `SETTINGS_ONLY_ENV_VARS` в `test_settings.py` пополняется. Проверка:
      тесты дрейфа `.env.example` зелёные

## 3. Старт процесса

- [x] 3.1 `main.py::run_bot`: при включённой ручке и заданном `OWNER_ID`
      снять снимок **до** `db.init()`; после создания `Bot` отправить фоновой
      задачей (D2). Проверка: гейты и отказоустойчивость — юнит-тестами
      вынесенной `startup_snapshot`; позиция «до init» — одна строка в
      `run_bot`, закреплена комментарием и видна в диффе (полный тест порядка
      требовал бы мокать весь `run_bot` — осознанно не делается)
- [x] 3.2 Первый старт без файла БД и выключенная ручка — оба пути молча
      пропускают. Проверка: тесты обоих путей

## 4. Команда /db_snapshot

- [x] 4.1 Хендлер в `app/handlers/admin.py`: фильтры private-чата и
      `OwnerOnly()`, без `GROUP_ONLY`; отвечает свежим снимком тем же модулем.
      Проверка: тесты — владелец в личке получает документ; в группе и для
      не-владельца хендлер не зарегистрирован/не срабатывает
- [x] 4.2 `main.py`: `db_snapshot` в `COOLDOWN_EXEMPT_COMMANDS` с причиной
      (owner-only); пиннящий тест списков обновлён. Проверка: тест зелёный
- [x] 4.3 `TELEGRAM_COMMANDS` не пополняется (owner-only команды в меню не
      выносятся — как `pivo_check`/`quirk_stats`). Проверка: состав меню
      не изменился
- [x] 4.4 `TestPrivateChatCommandSurface.PINNED`: `db_snapshot: True` — явное
      решение о личке. Проверка: тест зелёный, мутация «снять фильтр
      приватности» роняет его с именем команды

## 5. Документация и приёмка

- [x] 5.1 README: строка `/db_snapshot` в таблице команд (owner-only, личка);
      абзац о личке упоминает снимки. Проверка: README не противоречит коду
- [x] 5.2 Синк спеки `private-chat-diagnostics` (MODIFIED) при архиве;
      `openspec validate --strict` зелёный. Проверка: команда выполнена
- [x] 5.3 `ruff` + `mypy app/` + `unittest` зелёные, покрытие не ниже
      храповика. Влияние на `generation_hash` заявлено: отсутствует по
      построению (путь генерации не тронут) — снятие снимков не требуется.
      Проверка: команды выполнены, вывод чистый
