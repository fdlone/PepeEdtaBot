## 1. Дросселирование отказа по квоте

- [x] 1.1 Ввести ограничение частоты уведомления об исчерпанной квоте `/pivo`: ключ «чат — участник», окно 60 с, состояние с теми же пределами по времени и объёму, что у соседних состояний в памяти
- [x] 1.2 Внутри окна отказ остаётся молчаливым: ни ответа, ни chat action, ни записи в БД
- [x] 1.3 Тест: три вызова подряд сверх квоты дают ровно один ответ
- [x] 1.4 Тест: после истечения окна объяснение приходит снова
- [x] 1.5 Тест: дросселирование не затрагивает второго участника того же чата

## 2. Порядок списания квоты

- [x] 2.1 Перенести `consume_daily_call_quota` перед `build_call_message` в `app/handlers/pivo.py`
- [x] 2.2 Сохранить возврат квоты при сбое отправки и убедиться, что теперь он покрывает и сбой сборки сообщения
- [x] 2.3 Тест: вызов, отклонённый по квоте, не обращается к списку подписчиков (мок репозитория не вызван)
- [x] 2.4 Тест: сбой сборки сообщения после списания возвращает квоту

## 3. Ограничение аргументов

- [x] 3.1 Ограничить длину «повода» (200 символов) и строки времени (40 символов) при разборе команды
- [x] 3.2 Усекать по границе слова, если это не усложняет код; иначе просто по длине
- [x] 3.3 Тест: аргумент в 4000 символов усекается, итоговое сообщение короче лимита Telegram
- [x] 3.4 Тест: обычный короткий аргумент не изменяется

## 4. Усечение списка получателей

- [x] 4.1 В `build_call_message` заменить исключение при превышении `subscriber_fanout_limit` на усечение списка до лимита
- [x] 4.2 Добавить в сообщение пометку о том, что упомянуты не все, и число неупомянутых
- [x] 4.3 Сохранить `PivoCallLimitError` для явных упоминаний, указанных вызывающим
- [x] 4.4 Учесть усечённых в агрегате путей упоминания (в лог и в отчёт владельцу), не раскрывая персональных данных
- [x] 4.5 Тест: при подписчиках больше лимита сообщение уходит, содержит пометку, число упоминаний равно лимиту
- [x] 4.6 Тест: превышение лимита явных упоминаний по-прежнему отвечает отказом

## 5. Устойчивая пост-обработка

- [x] 5.1 Обернуть запись анти-повтора, логирование агрегата и отчёт владельцу так, чтобы их сбой не всплывал из обработчика
- [x] 5.2 Тест: исключение при записи анти-повтора не приводит к исключению из обработчика и не возвращает квоту

## 6. Закрытие

- [x] 6.1 `ruff check app/ tests/ tools/ main.py`
- [x] 6.2 `mypy app/`
- [x] 6.3 `python -m unittest discover tests -v`
- [x] 6.4 README и `.env.example`: `PIVO_SUBSCRIBER_FANOUT_LIMIT` — порог усечения, поведение при исчерпанной квоте
