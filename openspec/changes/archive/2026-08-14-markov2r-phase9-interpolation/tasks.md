# Tasks: markov2r-phase9-interpolation

Блок 0 идёт первым и до кода: гейт обязан быть зарегистрирован раньше любого
прогона, иначе пре-регистрация не пре-. Блок 5 (замер хеша) — последним, он
проверяет главный инвариант фазы.

## 0. Пре-регистрация гейта (до кода)

- [x] 0.1 Записать контрольное значение `generation_hash` на текущем `HEAD` (прод-копия + синтетика) — эталон для регресса β = 0 — прод-копия `13e496c0…`, ревизия — вершина ветки W1-C
- [x] 0.2 Гейт `phase9_interp` в `tools/eval/eval_thresholds.yaml`: шесть условий по §7 спецификации, с датой регистрации. После этого пороги не трогаются — блок `phase9_interp`, шесть условий, дата регистрации 2026-08-14
- [x] 0.3 Проверить, что имя блока читается кодом буква в букву, и вписать его в кортеж `tests/test_eval_protocol.py` — иначе опечатка в ключе не поймается, а гейт молча уедет на хардкод-дефолты — вписан в кортеж `test_thresholds_preregistered_gates_present`
- [x] 0.4 Условие 6 — раунд связности по соло-протоколу M3R-020, а не панельный kappa. **Решено владельцем 2026-08-14, до регистрации гейта:** содержательный порог («не ниже C0 более чем на 10 п.п.») переезжает дословно, меняется только инструмент — валидность по само-консистентности (≥20% повторно, согласие с собой ≥ 0.8) вместо Fleiss' kappa ≥ 0.4, потому что панель проекту недоступна (роадмап §3). Синхронизированы: `docs/v2/M2R_PHASE9_INTERPOLATION.md` §7/§8/§9/§10, дельта `generation-eval`, `design.md`
- [x] 0.5 Внести именную зависимость «промоушен β ← тулинг M3R-020» в таблицу `docs/PRE_ROADMAP.md` §5 — сделано; счётчик жёстких зависимостей в тексте поднят с трёх до четырёх. Сам M3R-020 в объём фазы не входит

## 1. Ручка

- [x] 1.1 `MARKOV_INTERP_ORDER2_WEIGHT` в `RUNTIME_FIELDS` (`app/config/registry.py`), дефолт `0.0`, валидация диапазона [0.0, 1.0] — `MARKOV_INTERP_ORDER2_WEIGHT`, дефолт 0, `_float_in_range(0.0, 1.0)`
- [x] 1.2 Поля в `Settings` и `RuntimeState`, доступность через `/set` с действием до рестарта — как у остальных ключей — поле в `Settings`; `RuntimeState` собирается из `RUNTIME_FIELDS` автоматически; `/set` подтверждён через `runtime_field_names()`
- [x] 1.3 Тест валидации границ: 0.0 и 1.0 принимаются, отрицательное и > 1 отклоняются — 0.0/1.0 принимаются, −0.1/1.5/нечисло отклоняются `ValueError`

## 2. Слияние (M2R-900)

- [x] 2.1 `app/core/interpolation.py`: `OrderInterpolation(beta)` с `enabled` и ранним return, `InterpolatedPool` (слитые строки + нормализованные веса + телеметрия шага) — по образцу `TemporalBlend`/`BlendedPool` (design.md D1) — `app/core/interpolation.py`: `OrderInterpolation` + `InterpolatedPool`
- [x] 2.2 Слияние на нормализованных распределениях по объединению токенов; порядок результата — по токену, согласован с предусловием `weighted_next_choice` (design.md D2) — слияние по объединению токенов, порядок — по токену
- [x] 2.3 Композиция с временнóй смесью: вклад слоя берётся из бленда, если он включён, иначе из сырых счётчиков, нормированных в пуле слоя (design.md D3) — `base3`/`base2` берутся из бленда, когда он включён
- [x] 2.4 Вырождение: пустая или отсутствующая проекция order-2 даёт чистое P3, а не ошибку и не пустой пул — пустая или ничего не добавляющая проекция → `None`, шаг остаётся на существующем пути
- [x] 2.5 Внедрить в `_run_generation_loop` в ветку существующего order-3-состояния; чтение проекции — существующим `_get2`, порт не расширять (design.md D6) — ветка `if pool3 and order >= 3` в `_run_generation_loop`; проекция читается существующим `_get2`, порт не тронут
- [x] 2.6 Слитые веса идут в `weighted_next_choice` через `base_weights` — пол веса 1e-12, а не 0.01 (design.md D4) — слитые веса идут через `base_weights` (`BlendedPool`), пол 1e-12
- [x] 2.7 Путь бэкоффа не тронут: шаг без состояния order-3 идёт в order-2 как прежде, order-1 не возвращается — путь бэкоффа не изменён — подтверждено 63 тестами характеризации генерации

## 3. Телеметрия (M2R-901)

- [x] 3.1 Счётчик доли шагов, где слой order-2 добавил хотя бы одного нового кандидата — `interp_covered_steps`, инкремент только при `added > 0`
- [x] 3.2 Средняя TV-дистанция слитого распределения от чистого P3 — `interp_displacement_sum`, TV против чистого P3
- [x] 3.3 Оба числа в тот же канал, что существующие метрики, и видны eval-отчёту — в `snapshot()`: `interp_step_coverage`, `mean_interp_displacement`
- [x] 3.4 «Выставлено, но ничего не добавило» отличимо от «не выставлено» — правило интента против эффекта, как у смеси и коллокаций — тест `test_neutral_beta_leaves_telemetry_at_zero` против `test_telemetry_reports_intent_and_effect`
- [x] 3.5 Тест privacy: только числа, без текста, токенов и идентификаторов — покрыто privacy-тестом маршрутов + счётчики только числа

## 4. Тесты (M2R-902)

- [x] 4.1 Unit: корректность весов и нормализации; границы β = 0 и β = 1 — `TestMergeArithmetic` — 6 тестов, включая нормализацию слоёв и β=1
- [x] 4.2 Unit: детерминизм порядка — повторное слияние тех же пулов даёт побайтно тот же список; токен из обоих слоёв появляется один раз — `TestOrderAndDeterminism` — токен-порядок и побайтная повторяемость
- [x] 4.3 Unit: вырождение при пустой P2proj — `TestDegenerateCases` — 4 случая вырождения
- [x] 4.4 Property: для случайных пулов при β = 0 результат — чистое P3 (и `None` вместо объекта, то есть арифметики не было) — `TestNeutralityProperty` — hypothesis, 60 примеров, проверяется именно `None`
- [x] 4.5 Интеграционный: обе политики кэша (`markov_cache_incremental` true/false) при β = 0.3 дают одинаковый вывод — `test_fresh_generator_matches_warm_one` — прогретый против холодного при β=0.3
- [x] 4.6 Интеграционный: смена β через настройку отражается в следующем ответе — `test_beta_changes_the_walk` — β=0.6 меняет вывод (иначе проводка декоративна)
- [x] 4.7 Регресс: `generation_hash` при β = 0 равен эталону из 0.1 — **хеш при β=0 = `13e496c0…`, бит-в-бит равен эталону 0.1**

## 5. Eval-обвязка

- [x] 5.1 `tools/eval/matrix_phase9_grid.yaml`: C0 + три руки β ∈ {0.15, 0.30, 0.50}. Идентификатор конфигурации — следующий свободный по реестру `tools/eval/`, единообразно с существующей схемой имён (проверить, не занят ли C5) — `matrix_phase9_grid.yaml`, префикс **C6** (C5 занят в `matrix.yaml`), три руки
- [x] 5.2 `tools/eval/state_census.py`: перепись состояний + симуляция интерполяции, чтобы мотивационные цифры спецификации воспроизводились одной командой на любой копии БД — `tools/eval/state_census.py` — воспроизводит §1/§2 спеки одной командой
- [x] 5.3 Если `tools/eval/report.py` не умеет условие «нижняя граница CI дельты > −0.02» — добавить и покрыть в `tests/test_eval_protocol.py` по образцу гейтов Phase 6/7 — `_delta_ci_low` + `_phase9_arm_verdict` + `PHASE9_ARM_PREFIX`; 33 теста протокола зелёные, smoke проходит
- [x] 5.4 Замер `generation_hash` после всех правок: **ожидание — не сдвинулся** (β = 0 по дефолту). Расхождение = стоп и разбор — **подтвердилось**: прод-копия `13e496c0…` не сдвинулась, синтетика `baseline OK`

## 6. Документация

- [x] 6.1 `README.md` и `.env.example` — строка о новой ручке в стиле существующих описаний — `README.md` и `.env.example`
- [x] 6.2 Строка Phase 9 в таблицу фаз `docs/v2/00_STATUS.md` (сейчас там только 0–8) и в `docs/v2/03_MARKOV_2_0R_IMPLEMENTATION_ROADMAP.md` по §10 спецификации — строка фазы 9 в `00_STATUS.md` и в таблицу приоритетов `03_…ROADMAP.md`
- [x] 6.3 Актуализировать `docs/GENERATION_MAP.md`: новая ручка в реестр §2.1 со статусом, новая связь в §4 (β → энтропия/температура: у Phase 2 впервые появляется градуированный сигнал), правка §1.6 если порядок потребления RNG меняется — реестр §2.1 + две новых связи в §4 карты (β → все sampling-ручки; β ↔ `order_mix_probability`)
- [x] 6.4 Снять статус «предложено» с `docs/v2/M2R_PHASE9_INTERPOLATION.md` и поправить в промпте ссылку на несуществующий `docs/M2R_PHASE8_INTERPOLATION.md` — статус спеки снят с «предложено»; промпт не трогал — его правит владелец

## 7. Close-out

- [x] 7.1 `openspec validate --strict` зелёный — зелёный
- [x] 7.2 `ruff check app/ tests/ tools/ main.py`, `mypy app/`, полный `unittest` — зелёные; покрытие не ниже храповика (`fail_under = 87`) — `ruff` (включая `tools/`) чисто, `mypy app/` 76 файлов без ошибок, **1216 тестов OK**, покрытие **94%** при храповике 87
- [x] 7.3 `tests/test_no_real_chat_ids.py` зелёный **после** `git add` — прогон на untracked-состоянии ничего не проверяет — зелёный после `git add` — проверено в правильном порядке для обоих коммитов
- [x] 7.4 Архивировать change после мержа — влит squash'ем как `19e0539` (PR #153), архивирован 2026-08-14 с синхронизацией дельт
