## 1. Подготовка и страховка

- [x] 1.1 Снять эталонный хеш до изменений: 1000 генераций (4 сида × 250) на прод-копии, SHA256 вывода — по образцу проверки из PR #102 (D5)
- [x] 1.2 Подтвердить факт из design: всё живое состояние по чатам словарное, а прямых присваиваний полям настроек в `app/` нет, кроме `admin.py:130`

## 2. Оверлей в состоянии

- [x] 2.1 Добавить в `RuntimeState` хранилище переопределений по `chat_id` (только поля настроек)
- [x] 2.2 Реализовать построение эффективного состояния для чата: поверхностная копия с применёнными переопределениями; при отсутствии переопределений возвращать сам базовый объект, а не копию (D1)
- [x] 2.3 Включить оверлей в `prune_inactive` и `forget_chat` (D4)
- [x] 2.4 Тесты: словари живого состояния разделяются по ссылке, `forget_chat` снимает переопределения, вытеснение по неактивности работает

## 3. Подстановка эффективного состояния

- [x] 3.1 Добавить мидлварь, подставляющую эффективное состояние чата в контекст хендлеров (D1)
- [x] 3.2 Оставить базовое состояние доступным отдельным ключом для админских хендлеров (D2)
- [x] 3.3 Убедиться, что порядок регистрации мидлварей не конфликтует с троттлингом
- [x] 3.4 Тест: хендлер в чате с переопределением видит переопределённое значение, в соседнем чате — глобальное

## 4. Команды

- [x] 4.1 `/set` по умолчанию пишет переопределение для чата вызова; добавить явную глобальную форму
- [x] 4.2 `/setprob` — то же самое; убрать прямое присваивание в обход общего пути записи либо явно свести его к нему
- [x] 4.3 Добавить снятие переопределения в чате (возврат к глобальному значению)
- [x] 4.4 Ответы команд явно называют область изменения: «в этом чате» / «глобально»
- [x] 4.5 `/config` показывает действующие значения чата и отмечает переопределённые
- [x] 4.6 Обе формы остаются owner-only (`OwnerOnly`, фикс O5) — проверить тестом

## 5. Валидация

- [x] 5.1 `try_apply` валидирует кросс-полевые инварианты против эффективного состояния той области, куда идёт запись (D3)
- [x] 5.2 Тест: комбинация, допустимая с переопределениями чата и недопустимая с глобальными значениями, принимается для чата
- [x] 5.3 Тест: недопустимая для чата комбинация отвергается целиком, действующие значения не меняются

## 6. Проверка нейтральности и защита от регресса

- [x] 6.1 Повторить прогон из 1.1 после изменений: для пути без переопределений хеш обязан совпасть побайтово (D5)
- [x] 6.2 Тест-сторож: единственные точки записи в поля настроек — общий путь применения и обработчик `/setprob` (D2)
- [x] 6.3 Тест: `/set` в чате продолжает действовать на следующем апдейте, а не только в ответе команды
- [x] 6.4 Тест: задание и снятие переопределения не сбрасывают анти-повтор, настроение и время последнего ответа

## 7. Закрытие

- [x] 7.1 Прогнать полный сьют, `ruff`, `mypy`
- [x] 7.2 README: формы `/set` и `/setprob`, вывод `/config`, и то, что переопределения живут в памяти и вытесняются вместе с состоянием чата
- [x] 7.3 Убрать пункт из бэклога `docs/OPEN.md`, записать итог в `docs/CLOSED.md` — включая явную оговорку, что это удобство тюнинга, а не безопасность
