# Tasks — meme-regression-support-floor

## 1. Пре-регистрация чисел (до кода)

- [x] 1.1 Блок `meme_regression` в `tools/eval/eval_thresholds.yaml`:
      `support_min: 2`, `reproduced_tolerance: 0.10`, `set_min_size: 8` — с
      обоснованием на месте (D1–D3: замер поддержки 1079/1097 при `cnt = 1`,
      цена одного мема против допуска) и датой регистрации. Проверка: ключи
      читаются `yaml.safe_load`, тест состава гейтов видит новый блок

## 2. Набор

- [x] 2.1 `tools/eval/prompts.py`: отбор n-грамм для набора учитывает порог
      поддержки из файла порогов. Проверка: тест на фикстуре с n-граммами
      поддержки 1 и 2 — в набор входят только вторые
- [x] 2.2 Порог доезжает до генерации набора из файла порогов, а не
      хардкодом. Проверка: тест с подменённым порогом меняет состав набора

## 3. Метрика и вердикт

- [x] 3.1 `meme_regression` возвращает пару «воспроизведено / всего» для
      **любой** конфигурации (не только C0). Проверка: юнит-тест на записях
      двух армов
- [x] 3.2 Вердикт в отчёте: относительный к C0 с допуском (D2); печатаются
      обе доли, абсолютные числа, допуск и размер набора. Проверка: тест
      текста отчёта — присутствуют оба числа и допуск
- [x] 3.3 Набор меньше минимума ⇒ `insufficient data` с указанием размера и
      минимума (D3). Проверка: тест
- [x] 3.4 Пустой набор ⇒ `insufficient data`, а не `pass`. Проверка: тест
      (поведение сохраняется из текущей реализации)
- [x] 3.5 Отсутствие блока порогов ⇒ `insufficient data`, а не дефолт из кода
      (правило пре-регистрации, прецедент `gate-phase5-ndocs-floor` D4).
      Проверка: тест с пустым блоком

## 4. Приёмка

- [x] 4.1 Прогон на свежей прод-копии: строка гейта печатает долю, числа и
      размер набора; результат (включая возможный `insufficient data` из-за
      тонкого корпуса) записан в заметку change'а как наблюдение
- [x] 4.2 `ruff` + `mypy app/` + полный сьют, покрытие не ниже храповика.
      Влияние на `generation_hash`: отсутствует по построению (путь генерации
      не тронут) — снятие снимков не требуется
- [x] 4.3 `docs/v2/05_MARKOV_2_0R_EVAL_PROTOCOL.md` §3.5: врезка «известный
      дефект» заменяется описанием починенного гейта
- [x] 4.4 `docs/GENERATION_MAP.md` §3.6 аннотировать закрытием; синк спеки при
      архиве
