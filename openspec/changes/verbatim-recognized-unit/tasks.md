# Tasks — verbatim-recognized-unit

## 1. Заявка по хешу — до правок

- [ ] 1.1 Снять `generation_hash` на прод-копии и синтетике **до** правок,
      записать в `hash-log.md` с ожиданием: **сдвига нет** — при выключенной
      ручке подсчёт доли идёт прежним путём (D3)

## 2. Подсчёт доли с исключением единицы

- [ ] 2.1 `candidate_scorer`: подсчёт доли принимает флаг гарда; при
      включённом — находит самый длинный непрерывный корпусный отрезок и, если
      его длина не больше `verbatim_unit_max_tokens`, исключает его окна из
      числителя и знаменателя (D1). Проверка: юнит-тесты на трёх случаях —
      одна единица внутри своего текста, две единицы, отрезок длиннее предела
- [ ] 2.2 Пустой знаменатель после исключения ⇒ исключение отменяется, доля
      считается как без гарда (D2). Проверка: тест «ответ = одна единица»
- [ ] 2.3 Выключенный гард даёт ровно прежнее число на тех же входах.
      Проверка: параметризованный тест против прежней реализации

## 3. Ручка и проводка

- [ ] 3.1 `verbatim_recognized_unit` (bool, дефолт `false`) в реестре +
      `.env.example` с пояснением, что это гард атрибуции, а не ослабление
      штрафа. Проверка: тесты дрейфа `.env.example` и полноты `/config`
      зелёные
- [ ] 3.2 Проброс в `_build_score`; триггер дописки остаётся на сырой доле
      (D4). Проверка: тест — при включённой ручке решение о дописке не
      изменилось, а штраф изменился

## 4. Приёмка

- [ ] 4.1 `generation_hash` после правок сверен с 1.1 на обоих снимках
- [ ] 4.2 Замер эффекта на прод-копии: доля кандидатов со штрафом и средний
      штраф при выключенной и включённой ручке — числа в `hash-log.md`, чтобы
      «гард что-то делает» было наблюдением
- [ ] 4.3 `ruff` + `mypy app/` + полный сьют, покрытие не ниже храповика
- [ ] 4.4 `docs/GENERATION_MAP.md` — актуализировать по её §5 (реестр ручек:
      новая ручка со статусом «выключена, ждёт грида M2R-430»); синк спеки при
      архиве
