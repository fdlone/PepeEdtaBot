## 1. Права

- [x] 1.1 Заменить `OwnerOnly()` на `AdminOrOwner()` в регистрации `/set` и `/setprob` (`app/handlers/admin.py`)
- [x] 1.2 Внутри обоих хендлеров: глобальная форма (`global`) выполняется только владельцем — проверка через `is_owner`
- [x] 1.3 Отказ по глобальной форме объясняет причину и **не** применяет значение к чату вызова (молчаливая подмена области недопустима)
- [x] 1.4 Обновить текст отказа для тех, кто не прошёл `AdminOrOwner`: он больше не должен называть `OWNER_ID` единственным допущенным
- [x] 1.5 Обновить комментарии про O5 в коде: вектор закрыт скоупом, а не правами

## 2. Тесты

- [x] 2.1 Админ чата меняет настройку своей формой — значение попадает в оверлей его чата
- [x] 2.2 Админ чата на `/set global …` получает отказ; глобальное значение не изменилось и переопределения для его чата не появилось
- [x] 2.3 То же для `/setprob global …`
- [x] 2.4 Владелец может обе формы
- [x] 2.5 Обычный участник не может ни одну
- [x] 2.6 `/set reset <key>` доступен админу чата
- [x] 2.7 Проверить фильтры в регистрации хендлеров (`AdminOrOwner` на обеих командах)

## 3. Закрытие

- [x] 3.1 Полный сьют, `ruff`, `mypy`
- [x] 3.2 README: кто и что может — скоупленная форма админам чата, глобальная владельцу
- [x] 3.3 Записать в `docs/CLOSED.md`, что интерим-ограничение O5 снято и почему это безопасно
