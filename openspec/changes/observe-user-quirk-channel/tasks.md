## 1. Заявка по хешу — до правок

- [ ] 1.1 Снять `generation_hash` на прод-копии и на синтетике **до** любых
      изменений, записать оба значения в change: `.venv/Scripts/python.exe -m
      tools.generation_hash --db db_prod_copy/markov.db` и `--synthetic --check`
- [ ] 1.2 Записать ожидание прямо здесь, до правок: **сдвига нет ни на одном из
      двух снимков**. Расхождение с ожиданием останавливает работу и разбирается,
      а не переписывается задним числом (§2 CLAUDE.md)

## 2. Счётчики воронки

- [ ] 2.1 Добавить в `GenerationTelemetry` пары «дошло / отказало» на четыре
      гейта плюс число сработавших причуд; проверить юнит-тестом, что каждая
      пара считается независимо и что нейтральная ручка даёт нули в обоих числах,
      а не отсутствие полей
- [ ] 2.2 Вывести их в `snapshot()` со знаменателями; проверить тестом, что
      знаменатель следующего гейта равен числителю предыдущего минус его отказы
      (воронка сходится арифметически, Р3 design)
- [ ] 2.3 Разложить условие в `_apply_user_quirk`
      (`app/services/reply_pipeline.py:697`) на последовательные ранние возвраты
      со счётчиком на каждом (Р1 design); проверить тестом, что при фиксированном
      зерне решения о срабатывании совпадают с прежними на серии входов
- [ ] 2.4 Тест на каждый гейт: адресованность, нулевая ручка, суточный лимит,
      неудачный розыгрыш, недобор порога — отказ засчитывается **тому** гейту,
      который сработал, и только ему
- [ ] 2.5 Тест: учёт не потребляет розыгрышей — число обращений к ГСЧ на серии
      входов совпадает с прежним

## 3. Вывод воронки в `/stats`

- [ ] 3.1 Напечатать числа канала в `format_stats_message` рядом с остальной
      телеметрией; проверить тестом на форму строки
- [ ] 3.2 Тест приватности: в строке и в логах нет имён, `user_id` и хэшей, а
      `chat_id`, если он вообще появляется, замаскирован (§4 CLAUDE.md)
- [ ] 3.3 Тест: канал с ручкой в нуле присутствует в выводе нулями, а не
      исчезает молча

## 4. Распределение в `/quirk_stats`

- [ ] 4.1 Расширить агрегат `get_stats` в
      `app/repositories/chat_user_interactions_repo.py` группировкой по
      диапазонам, привязанным к действующему порогу (Р4 design); проверить
      тестом на подготовленных данных, включая границы диапазонов
- [ ] 4.2 Пробросить через `Database` и `LearningService` по образцу
      существующего `get_user_interaction_stats`; проверить, что пустой чат
      возвращает нули, а не ошибку
- [ ] 4.3 Показать распределение в ответе команды; проверить тестом, что оно
      выводится группировкой, а не значением на человека
- [ ] 4.4 Тесты приватности и прав: не владелец получает отказ и не видит чисел;
      в ответе нет имён, идентификаторов и хэшей
- [ ] 4.5 Тест: команда ничего не меняет — счётчики после вызова прежние

## 5. Приёмка

- [ ] 5.1 `ruff`, `mypy app/`, полный сьют зелёные; покрытие не ниже храповика
      (`fail_under = 87`)
- [ ] 5.2 Перепроверить `generation_hash` на обоих снимках и **сверить с
      ожиданием из 1.2**; расхождение — стоп и разбор
- [ ] 5.3 README рядом с `/quirk_stats` — что показывает распределение;
      `docs/GENERATION_MAP.md` — канал причуд в реестре наблюдаемости
- [ ] 5.4 Проверить, что новых пер-чатовых кэшей правка не завела; если завела —
      внести в `forget_chat` (§5 CLAUDE.md)

## 6. Снятие чисел — после выкатки

- [ ] 6.1 Выкатить рестартом; убедиться, что строка канала появилась в `/stats`
      и что рядом видно возраст окна («генераций с рестарта»)
- [ ] 6.2 Дать окну накопиться **сутки активности без рестартов** — раньше
      суточный лимит на пару (чат, человек) не проявится в воронке
- [ ] 6.3 Снять воронку и распределение, записать числа в вердикт-заметку
- [ ] 6.4 По числам решить, что чинить: порог, вероятность, суточный лимит —
      или ничего из этого, если душит гейт адресованности. Решение оформляется
      **отдельным изменением**, с числами на руках
