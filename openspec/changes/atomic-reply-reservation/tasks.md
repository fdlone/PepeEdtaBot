## 1. Троттлинг команд

- [x] 1.1 Перенести `self._last_used[key] = now` выше `await handler` в
      `app/middlewares/throttling.py`; сохранить прежнее значение ключа в
      локальной переменной. Проверка: существующие тесты
      `tests/test_filters.py::TestThrottlingMiddleware` остаются зелёными.
- [x] 1.2 Обернуть `await handler` в `try/except`, восстанавливающий
      **прежнее** значение ключа (а не удаляющий его) и пробрасывающий
      исключение. Проверка: новый тест «ошибка хендлера не занимает окно, но и
      не сбрасывает накопленное окно предыдущего успешного вызова».
- [x] 1.3 Тест на гонку: `asyncio.gather` из пяти вызовов одной команды одним
      ключом. Проверка: хендлер вызван ровно один раз, четыре отклонены.
- [x] 1.4 Проверить 1.3 мутацией: вернуть стамп на прежнее место — тест обязан
      упасть. Записать результат мутации в комментарий теста.

## 2. Резервация слота ответа

- [x] 2.1 Добавить в `app/config/runtime_state.py` операцию резервации
      («занять слот ответа для чата в момент `now`») и парную операцию
      отката, снимающую **именно добавленную** метку из `recent_reply_times`,
      а не последнюю. Проверка: юнит-тест на две одновременные резервации с
      откатом первой.
- [x] 2.2 Вызвать резервацию в `app/services/reply_pipeline.py` сразу после
      того, как принято решение отвечать, — до сбора контекста и генерации.
      Проверка: существующие тесты пайплайна зелёные.
- [x] 2.3 Откатывать резервацию при пустом результате генерации. Проверка:
      тест «генерация не дала кандидатов → часовой счётчик не вырос».
- [x] 2.4 Откатывать резервацию при исключении отправки, не затрагивая
      `learn` в `finally`. Проверка: тест «отправка упала → бюджет вернулся
      и сообщение всё равно выучено».
- [x] 2.5 Тест на гонку пути ответа: `asyncio.gather` из пяти сообщений одного
      чата при неистёкшем кулдауне. Проверка: ровно один самостоятельный
      ответ.
- [x] 2.6 Тест на границу часового капа при одновременной доставке. Проверка:
      кап не превышен.
- [x] 2.7 Проверить 2.5 и 2.6 мутацией: вернуть резервацию на прежнее место
      (после отправки) — оба теста обязаны упасть.

## 3. Прямые обращения

- [x] 3.1 Убедиться, что окно `mention_cooldown_sec` резервируется по той же
      схеме и обход кулдауна/капа для упоминаний сохранён. Проверка: тест
      «пачка упоминаний от одного участника не даёт ответов больше, чем
      допускает окно прямых обращений».

## 3a. Частичная доставка (добавлено при реализации, решение владельца)

- [x] 3a.1 `Sender` возвращает число доставленных частей; обрыв отправки несёт
      его в `PartialDeliveryError`. Проверка: mypy + тесты контракта.
- [x] 3a.2 Бюджет возвращается только при нуле доставленных; анти-повтор и
      суточные бюджеты не учитывают неотправленное. Проверка: три теста —
      обрыв до доставки, обрыв после первой части, тихий ноль.

## 4. Фиксация решения о конкурентности

- [x] 4.1 Записать в `main.py` комментарием у `dp.start_polling`, что
      конкурентная обработка оставлена намеренно и чем именно держатся
      пределы; назвать `tasks_concurrency_limit=1` отвергнутым вариантом с
      причиной. Проверка: комментарий на месте, значение вызова не изменено.

## 5. Приёмка

- [x] 5.1 `ruff` + `mypy app/` + полный `unittest` зелёные, покрытие не ниже
      храповика 87.
- [x] 5.2 `python -m tools.generation_hash --synthetic --check` — хеш **не
      сдвинулся**; ожидание было заявлено до правок (`proposal.md` § Impact).
- [x] 5.3 Обновить `docs/OPEN.md`: перенести O8 из «требует действия» в
      закрытое с датой и сутью решения.
- [x] 5.4 Актуализировать `docs/GENERATION_MAP.md`, если правки затронули
      `reply_pipeline.py` в местах, на которые карта ссылается (§5 карты).
