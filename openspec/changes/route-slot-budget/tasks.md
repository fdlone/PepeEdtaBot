# Tasks — route-slot-budget

## 1. Заявка по хешу — до правок

- [x] 1.1 Снять `generation_hash` на прод-копии и синтетике **до** правок,
      записать оба значения в `hash-log.md` change'а
- [x] 1.2 Записать там же ожидание, до правок: **сдвига нет ни на одном
      снимке** — хеш строится на дефолтах реестра, где
      `markov_seeded_candidate_ratio = 0`, и маршрутный блок не исполняется
      (D4). Расхождение с ожиданием останавливает работу и разбирается, а не
      переписывается задним числом (§2 `CLAUDE.md`)

## 2. Бюджет слотов

- [x] 2.1 `route_slot_budget(target, ratio)` рядом с `branching_aware_target`
      (D3): `min(max(1, round(target * ratio)), target // 2)`. Проверка:
      юнит-тесты — `ratio = 0` даёт 0; `target = 5, ratio = 0.3` даёт 2;
      верх диапазона (0.7) не превышает половины; `target = 1` даёт 0
      (потолок побеждает минимум, D2)
- [x] 2.2 Seeded-ветка принимает бюджет параметром вместо собственного
      расчёта из `target * ratio`. Проверка: существующие тесты ветки
      (`tests/test_markov2r_phase5_seeded_finalization.py`) остаются зелёными

## 3. Порядок сборки пула

- [x] 3.1 Перенести вызов маршрутной ветки **перед** циклом попыток (D1);
      цикл не трогать — его условие остановки уже считает общий размер пула.
      Проверка: тест — при включённой ручке пул не превышает
      `effective_target`, а seeded-кандидаты в нём присутствуют
- [x] 3.2 Тест «маршрут ничего не произвёл»: пул добирается обходом до
      целевого размера, недозаполнения нет
- [x] 3.3 Тест «маршрут не вытесняет обход»: при ручке на верхе диапазона в
      пуле остаётся хотя бы один кандидат основного обхода
- [x] 3.4 Тест вырожденной цепи: `effective_target` ужимается ниже уже
      занятых слотов — пул собирается без падения, в нём есть кандидат обхода
      (риск из design)
- [x] 3.5 Тест нейтральности: при `ratio = 0` состав и порядок пула те же,
      что до правки (мутационно: вернуть вызов за цикл — тест на размер пула
      при включённой ручке обязан упасть)

## 4. Приёмка

- [x] 4.1 `generation_hash` после правок сверен с 1.1 на обоих снимках, факт
      записан в `hash-log.md`
- [x] 4.2 Замерить фактический размер пула на арме C4 до/после (короткий
      прогон `tools/eval` или целевой скрипт на прод-копии) и записать числа
      в `hash-log.md` рядом с вердиктом: «было до 7, стало 5» должно быть
      наблюдением, а не расчётом
- [x] 4.3 `ruff` + `mypy app/` + полный сьют, покрытие не ниже храповика
- [x] 4.4 `docs/GENERATION_MAP.md` — актуализировать по её §5 (правка задевает
      `response_generator.py`): состав пула и порядок его сборки
- [x] 4.5 `docs/OPEN.md`: O10 закрыт ветвью 2 (обе ветви развилки пройдены),
      запись в `docs/CLOSED.md` с датой, решением и оговоркой о
      несопоставимости прежних seeded-чисел
