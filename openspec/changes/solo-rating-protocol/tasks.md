# Tasks — solo-rating-protocol

## 1. Пре-регистрация порогов валидности

- [x] 1.1 ~~Блок `solo_rating`~~ **ОТСТУПЛЕНИЕ ОТ ПЛАНА, сделано иначе.**
      Три из четырёх порогов заявки уже пре-регистрированы 2026-08-14 в блоке
      `phase9_interp`: `manual_self_consistency_min: 0.80`,
      `manual_repeat_share_min: 0.20`, `manual_rated_min: 30`. Отдельный блок
      был бы вторым источником истины для тех же чисел — ровно то, что
      запрещает собственное требование спеки («порог SHALL NOT дублироваться»).
      Поэтому добавлен только действительно новый порог —
      `manual_decoy_detected_min: 0.80` — в тот же блок, с обоснованием и датой
      на месте. Проверка: тест состава гейтов читает блок, инструмент берёт все
      четыре порога оттуда же

## 2. Подготовка раунда

- [x] 2.1 `tools/solo_rating_round.py prepare`: прогон армов и C0 на копии
      прода, выборка ответов на арм, декои перестановкой токенов (D2),
      повторы (D3), общий перемешанный список, ключ отдельным файлом в
      `rating_rounds/<label>/`. Проверка: тест на синтетическом снапшоте —
      список обезличен, повторы не соседствуют, ключ содержит соответствие
- [x] 2.2 Выборка меньше минимума видна при подготовке и помечается в ключе.
      Проверка: тест
- [x] 2.3 В stdout только счётчики и пути, ни одного текста ответа (privacy,
      как у раунда мемов). Проверка: тест перехвата вывода

## 3. Подсчёт раунда

- [x] 3.1 `tools/solo_rating_round.py score`: ответы + ключ → агрегат
      (доля связных по армам, само-согласие, доля пойманных декоев,
      вердикт валидности с причиной). Проверка: тесты — валидный раунд,
      провал по само-согласию, провал по декоям
- [x] 3.2 Агрегат содержит только числа; тексты в него не попадают.
      Проверка: тест структуры агрегата
- [x] 3.3 Причины плохих ответов размечаются классами таксономии (D6).
      Проверка: тест — неизвестный класс отвергается с понятной ошибкой

## 4. Потребление отчётом

- [x] 4.1 `_phase9_arm_verdict`: условие связности считается из агрегата,
      когда он передан; порог берётся из файла порогов, а не из кода.
      Проверка: тесты — условие выполнено, провалено, агрегата нет
- [x] 4.2 CLI передаёт агрегат в отчёт (по образцу `--manual-rating`).
      Проверка: тест разбора аргумента

## 5. Документация и приёмка

- [x] 5.1 `docs/SOLO_RATING_ROUND.md` — процедура для владельца по образцу
      `docs/MEME_RATING_ROUND.md`: зачем раунд, что раздаётся, что остаётся,
      как считается валидность, как из этого получается условие гейта
- [x] 5.2 `ruff` + `mypy app/` + полный сьют, покрытие не ниже храповика.
      Влияние на `generation_hash`: отсутствует по построению (оффлайн-инструмент)
- [x] 5.3 Указатель на новый документ в `CLAUDE.md` §9 и в README;
      синк спек при архиве
