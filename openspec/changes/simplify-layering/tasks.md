## 1. Порт

- [ ] 1.1 Описать в `app/core` протокол чтения модели: старты (2/3), переходы (2/3), точечные проверки старта, состояния цепи, объём токенов, частоты слов, индекс 4-грамм
- [ ] 1.2 Включить в протокол только то, что реально вызывает ядро

## 2. Переход на порт

- [ ] 2.1 Перевести `MarkovGenerator` на порт вместо `Database`
- [ ] 2.2 Перевести `ContextStateMatcher` на порт
- [ ] 2.3 Убедиться, что `Database` удовлетворяет протоколу без изменений сигнатур
- [ ] 2.4 `mypy app/` подтверждает соответствие

## 3. Устранение дублей

- [ ] 3.1 Убрать из `app/infrastructure/database.py` копии набора пунктуации, размера n-граммы и функции построения окон; импортировать их из ядра
- [ ] 3.2 Убедиться, что цикла импортов больше нет
- [ ] 3.3 Оставить копию в миграции с комментарием о причине (застывшая история)
- [ ] 3.4 Тест: окна 4-грамм, записываемые при обучении, совпадают с теми, что считает оценка кандидатов

## 4. Закрытие

- [ ] 4.1 `ruff check app/ tests/ tools/ main.py`
- [ ] 4.2 `mypy app/`
- [ ] 4.3 `python -m unittest discover tests -v`
- [ ] 4.4 `docs/ARCHITECTURE.md`: направление зависимостей
