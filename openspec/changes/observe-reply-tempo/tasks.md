## 1. Счётчики

- [x] 1.1 Добавить в `GenerationTelemetry` счётчик темпа беседы: распределение
      `rate_ewma` по диапазонам, привязанным к порогам настроения (границы
      берутся из тех же ручек, а не хардкодом). Проверка: юнит-тест на
      попадание значений в ожидаемые диапазоны.
- [x] 1.2 Добавить пару «обращений наблюдалось / обращений отвечено» и
      верхнюю оценку ответов на обращения за час процесса. Проверка: тест на
      то, что знаменатель растёт и без ответа.
- [x] 1.3 Добавить пару «ответов в фазе усиления / в фазе подавления»
      берст-ритма. Проверка: тест, где ответ сыгран в каждой фазе.

## 2. Точки наблюдения

- [x] 2.1 Вызвать счётчик темпа в `observe` — там, где ритм уже посчитан, до
      любого раннего возврата. Проверка: короткое необучаемое сообщение всё
      равно попадает в распределение.
- [x] 2.2 Вызвать счётчики обращений и фазы берста в `respond`. Проверка:
      тесты пайплайна зелёные, поведение не изменилось.

## 3. Вывод

- [x] 3.1 Напечатать три строки в `/stats` по тем же правилам, что у
      существующих: только при ненулевом знаменателе. Проверка: тест на пустой
      телеметрии — строк нет.
- [x] 3.2 Проверить длину `/stats` с новыми строками против лимита сообщения.
      Проверка: число символов зафиксировано в тесте.

## 4. Приёмка

- [x] 4.1 `ruff` + `mypy app/` + полный `unittest` зелёные, покрытие ≥ 87.
- [x] 4.2 `python -m tools.generation_hash --synthetic --check` — хеш не
      сдвинут (заявлено до правок: счётчики не участвуют в генерации).
- [x] 4.3 Записать в `docs/OPEN.md`, что E2-1/E2-2 ждут окна наблюдения, и
      назвать решающее правило: какие числа означают «чинить», а какие —
      «дефект теоретический».
