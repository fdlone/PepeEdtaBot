# PepeEdtaBot

Telegram-бот для группового чата с генерацией сообщений на базе простой Markov chain (биграммы, n=2) без ML.

## Stack
- Python 3.11+
- aiogram v3
- SQLite (aiosqlite)
- Конфиг через `.env`

## Setup
```bash
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
```

Создайте `.env` на основе `.env.example` и заполните `BOT_TOKEN`.

## Конфигурация (.env)
- `BOT_TOKEN` - токен Telegram-бота.
- `REPLY_PROBABILITY` - вероятность случайного ответа (0..1), когда бота не упоминали.
- `MIN_COOLDOWN_SEC` - минимальная пауза между ответами бота в одном чате.
- `MIN_TOKENS_FOR_MODEL` - минимальный объём обучающих переходов для генерации.
- `MAX_REPLY_CHARS` - максимальная длина сгенерированного ответа.
- `OWNER_ID` - Telegram ID владельца для команд администрирования.
- `NORMALIZE_LOWER` - приводить токены к нижнему регистру (`true/false`).
- `DB_PATH` - путь к SQLite-файлу.

## Run
```bash
python main.py
```

## Логика работы бота
Бот работает в два этапа: обучение на сообщениях чата и генерация ответа.

### 1) Сбор и очистка сообщений
Бот слушает все текстовые сообщения в группе (кроме сообщений ботов) и:
- игнорирует команды (`/something`);
- удаляет ссылки `http/https`;
- сжимает повторяющиеся символы (`аааа` -> `аа`);
- нормализует пробелы;
- отбрасывает слишком короткие (`< 3`) и слишком длинные (`> 500`) сообщения.

После очистки сообщение сохраняется в таблицу `messages` для отладки.

### 2) Токенизация
Текст разбивается на токены:
- слова;
- знаки пунктуации `.,!?;:` как отдельные токены.

Пример:
`Привет, мир! Как дела?` -> `["Привет", ",", "мир", "!", "Как", "дела", "?"]`

Это позволяет при генерации получать более естественные паузы и окончания фраз.

### 3) Как строится Markov-модель (биграммы)
Для каждого сообщения с токенами `t0..tn` бот обновляет:
- `starts(chat_id, w1, w2, cnt)` - стартовая пара `(t0, t1)`;
- `transitions(chat_id, w1, w2, w3, cnt)` - переход `(ti, ti+1) -> ti+2`.

Если токенов меньше 3, переходы не обновляются.

Идея: бот запоминает, какое слово чаще всего встречалось после конкретной пары слов.

### 4) Как бот отбирает и сопоставляет слова при генерации
1. Выбирает стартовую пару `(w1, w2)` из `starts` с учетом веса `cnt`.
2. Ищет кандидатов `w3` для пары `(w1, w2)` в `transitions`.
3. Выбирает `w3` случайно, но взвешенно по частоте `cnt`:
   более частые продолжения имеют более высокий шанс.
4. Сдвигает окно: `(w1, w2) <- (w2, w3)` и повторяет шаги.
5. Останавливается, если:
   - достигнут `MAX_REPLY_CHARS`;
   - нет кандидатов для текущей пары;
   - достигнут лимит шагов (по умолчанию 80).

Итоговые токены склеиваются обратно в текст:
- пробел перед пунктуацией не ставится;
- между словами ставится обычный пробел.

### 5) Когда бот отвечает
- Если бота упомянули (`@username`) - пытается ответить обязательно.
- Если не упомянули - отвечает с вероятностью `REPLY_PROBABILITY`.
- В обоих случаях учитывается кулдаун `MIN_COOLDOWN_SEC`.
- Если данных мало (`MIN_TOKENS_FOR_MODEL` не достигнут), при упоминании бот отвечает:
  `Пока мало материала, поболтайте ещё 🙂`.

### 6) Seed-направление генерации
Команда `/seed "текст"` (только `OWNER_ID`) задаёт временное направление:
- из seed берутся первые 1-2 токена;
- при следующей генерации бот пытается начать именно с этой пары;
- seed одноразовый и сбрасывается после использования.

Если такой стартовой пары нет в модели, используется обычный выбор старта.

### 7) Защита от спама и мусора
- не отвечает на собственные сообщения;
- ограничивает частоту ответов кулдауном;
- не отправляет слишком короткий результат (менее 5 символов);
- ограничивает длину ответа.

## Команды
- `/stats` - статистика модели по текущему чату
- `/clear` - очистка данных текущего чата (только OWNER_ID)
- `/setprob 0.2` - изменить вероятность ответа (только OWNER_ID)
- `/seed "текст"` - одноразовый seed для следующей генерации (только OWNER_ID)

## Важно для групп
Отключите privacy mode у бота в BotFather: `Bot Settings` -> `Group Privacy` -> `Turn off`.
