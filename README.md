# PepeEdtaBot

Telegram-бот для группового чата с генерацией сообщений на базе Markov chain (биграммы, `n=2`) без ML.

## Stack
- Python 3.11+
- aiogram v3
- SQLite (aiosqlite)
- Конфиг через `.env`

## Setup
```bash
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
```

Создайте `.env` на основе `.env.example` и заполните `BOT_TOKEN`.

## Конфигурация (.env)
- `BOT_TOKEN` - токен Telegram-бота.
- `REPLY_PROBABILITY` - вероятность случайного ответа (0..1), когда бота не упоминали.
- `MIN_COOLDOWN_SEC` - минимальная пауза между ответами бота в одном чате.
- `MIN_TOKENS_FOR_MODEL` - минимальный объём обучающих переходов для генерации.
- `MAX_REPLY_CHARS` - максимальная длина сгенерированного ответа.
- `OWNER_ID` - Telegram ID владельца для админ-команд.
- `NORMALIZE_LOWER` - приводить токены к нижнему регистру (`true/false`).
- `DB_PATH` - путь к SQLite-файлу.
- `TYPING_MIN_MS` - минимальная задержка перед ответом (эффект "живого" общения).
- `TYPING_MAX_MS` - максимальная задержка перед ответом.
- `RANDOMNESS_STRENGTH` - сила рандома генерации (`0..3`).

## Run
```bash
python main.py
```

## Как это работает
Бот работает в 2 этапа: обучение на сообщениях чата и генерация ответов.

### 1) Сбор и очистка сообщений
Бот слушает все текстовые сообщения в группе (кроме сообщений ботов), затем:
- игнорирует команды (`/something`);
- удаляет ссылки `http/https`;
- удаляет `@mentions` (чтобы они не попадали в модель);
- сжимает повторяющиеся символы (`аааа` -> `аа`);
- нормализует пробелы;
- отбрасывает слишком короткие (`< 3`) и слишком длинные (`> 500`) сообщения.

В `messages` сохраняется исходный `raw_text` для отладки.

### 2) Токенизация
Текст разбивается на токены:
- слова;
- знаки пунктуации `.,!?;:` как отдельные токены.

Пример:
`Привет, мир! Как дела?` -> `["Привет", ",", "мир", "!", "Как", "дела", "?"]`

### 3) Как строится Markov-модель (биграммы)
Для каждого сообщения с токенами `t0..tn` бот обновляет:
- `starts(chat_id, w1, w2, cnt)` - стартовая пара `(t0, t1)`;
- `transitions(chat_id, w1, w2, w3, cnt)` - переход `(ti, ti+1) -> ti+2`.

Если токенов меньше 3, переходы не обновляются.

### 4) Генерация и анти-копипаст
- Старт выбирается из `starts` взвешенно.
- Следующие токены выбираются из `transitions` с управляемой рандомизацией.
- `RANDOMNESS_STRENGTH` усиливает exploration и "перемешивание" переходов.
- Встроена защита от копирования:
  - отбрасываются ответы, которые дословно совпадают с уже сохранённым сообщением;
  - используется переключение контекстов, чтобы снижать линейное повторение.

### 5) Когда бот отвечает
- При обращении к боту отвечает обязательно (если данных достаточно):
  - `@username`;
  - `mention` в entities;
  - reply на сообщение бота.
- Без обращения отвечает с вероятностью `REPLY_PROBABILITY` и с учётом кулдауна.
- Если данных мало (`MIN_TOKENS_FOR_MODEL` не достигнут), при обращении пишет:
  `Пока мало материала, поболтайте ещё 🙂`.

### 6) Seed-направление генерации
`/seed "текст"` (только `OWNER_ID`) задаёт временное направление:
- из seed берутся первые 1-2 токена;
- при следующей генерации бот пытается начать именно с этой пары;
- seed одноразовый и сбрасывается после использования.

### 7) Realtime и "живость"
- Long polling, без webhook.
- При старте бот снимает webhook, чтобы избежать конфликтов.
- Перед ответом отправляет `typing...` и выдерживает случайную задержку (`TYPING_MIN_MS..TYPING_MAX_MS`).

## Команды
- `/help` - список команд.
- `/ping` - проверка, что бот онлайн и отвечает в реальном времени.
- `/stats` - статистика модели по текущему чату.
- `/config` - показать текущие runtime-настройки.
- `/set <key> <value>` - изменить runtime-настройку (только `OWNER_ID`).
- `/setprob 0.2` - быстро изменить `reply_probability` (только `OWNER_ID`).
- `/seed "текст"` - одноразовый seed для следующей генерации (только `OWNER_ID`).
- `/clear` - очистка модели и сообщений по чату (`OWNER_ID` или админ чата).

## Runtime-ключи для /set
- `reply_probability`
- `min_cooldown_sec`
- `min_tokens_for_model`
- `max_reply_chars`
- `normalize_lower`
- `typing_min_ms`
- `typing_max_ms`
- `randomness_strength`

Изменения через `/set` действуют до перезапуска процесса.

## Важно для групп
Отключите privacy mode у бота в BotFather: `Bot Settings` -> `Group Privacy` -> `Turn off`.

## Безопасность
- Никогда не коммитьте `.env` в репозиторий.
- Храните реальные `BOT_TOKEN`, `OWNER_ID` только локально/в секрет-хранилище.
- В публичный репозиторий кладите только `.env.example` с плейсхолдерами.
