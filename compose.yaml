services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pepe-bot
    image: pepe-bot:latest
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
