# Открытые вопросы

Единственный список того, что не закрыто. Всё закрытое — в [CLOSED.md](CLOSED.md).

## Требует действия

| # | Что | Детали |
|---|---|---|
| O1 | Наблюдение за фичами «живости» и пакетом «контекст+хаос» в живом чате | Бот перезапущен 2026-07-16, наблюдение идёт. Под наблюдением: трек Stage 1–4 (настроение, директор ответов, локальные мемы, редкие события, причуды завсегдатаев — судятся только перцептивно, нужно несколько дней) и пакет O3 «контекст+хаос» (`context_jump_boost=2.0`, `verbatim_extension_share=0.8`, `order_mix_probability=0.5` — включён через `/set` 2026-07-16, откат поштучно). **Добавка 2026-07-16 (#92/#93/#95):** сегментация якоря (`context_anchor_splice_probability=0.25`) и эскалационные цепочки (`mood_mention_heated_share=0.6`) — дефолты подняты по замерам на прод-копии (см. CLOSED.md), но работающий процесс живёт на старых значениях: активируются рестартом или `/set`, и с этого момента окно наблюдения фактически стартует заново. Перцептивно судить: позицию якоря в ответах (не только в начале), «тик связок» (замер: связочная доля 0.65) и «завод» от серий обращений. |
| O2 | `/pivo`: одному человеку не приходит уведомление | **Симптом ровно такой, шире не читать:** бот пингует всех, упоминания в сообщении корректны, но одному конкретному человеку не приходит уведомление о пинге — остальным приходит. Уточнено 2026-07-16. У этого человека есть `@ник`, клик по нику в сообщении открывает чат с ним: упоминание валидно и резолвится. Исключено, проверено: пропуск в фан-ауте (`PIVO_SUBSCRIBER_FANOUT_LIMIT`), протухший `@ник`, рендер и резолв упоминания. PR #80 (освежение устаревших `@ников`, вживую с рестарта 2026-07-16) полезен сам по себе, но этот симптом не объясняет — ник и так валиден. Остаток подозреваемых — вне бота: настройки уведомлений, мьют чата или клиент у самого пользователя; кодом такое не чинится. Под наблюдением. **Механику упоминаний `/pivo` не трогать, пока O2 открыт** — любая правка смажет наблюдение (см. гейт у L2.1 в бэклоге). |

## Осознанно отложено — решения приняты, менять не нужно

| Что | Почему так |
|---|---|
| Fernet-ключ `/pivo` = `sha256(secret)` без KDF | Low. Переход на HKDF сломает расшифровку существующих подписок. Секреты по инструкции — длинные случайные. Решение от 2026-06-30: оставить. |
| Метрики/трейсинг, единый lock на SQLite-соединение, in-memory state (throttle, кулдауны, кэши) | Все три упираются в допущение «один инстанс». Актуальны, только если появится второй инстанс или воркер. |
| `/config` без `AdminOrOwner` | Read-only показ рантайм-настроек; секретов в `RuntimeState` нет. |
| `AUTO_CAPITALIZE_REPLIES=false` | Модель учится в нижнем регистре (`NORMALIZE_LOWER=true`); капитализация в чате читается неестественно. |

## Бэклог идей — не запланировано

- Verbatim у джампов: ответы с одним джампом несут ~0.3 дословности против ~0.02 без джампа (замер 2026-07-15) — точка входа, если цитирование корпуса снова станет проблемой.
- Лексическая отсебятина: слот-мутации фрагментов (замена контент-слова корпусного сегмента на частотное слово чата). После O4-фикса ценность пересмотрена: честная новизна уже 0.57–0.62, треть-пересказов больше нет — мутации теперь про «добить хвост», а не спасать положение. Метрика готова (`novel_ngram_share`). Риск несогласованности русской морфологии, нужен гард.
- L2.1 (уточнено 2026-07-17): исходная идея — обращение по имени **в генерации** (вокатив L2-причуды), а не в `/pivo`; трактовка через `/pivo`-подписки была ошибочной. **Реализовано за выключенной ручкой** (ветка `feat/quirk-name-vocative`): `user_quirk_name_share` — доля причуд, где вокатив — имя завсегдатая, взятое живьём из входящего апдейта (санитизация, фолбэк в пул), без хранения и логирования — privacy-модель L2 (анонимный HMAC-счётчик) не тронута. Гейт O2 не задет: `/pivo` не меняется. Включение — отдельным решением после окна наблюдения O1 (это изменение живого поведения). Прежняя заметка про именную ссылку `tg://user?id=` в `/pivo` неактуальна для этой задачи.
- Интонационный профиль чата: выучиваемая средняя длина и пунктуация.
