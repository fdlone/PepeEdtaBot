# Вердикт M3R-230: L1 как маршрут с бюджетом слотов — 2026-09-02

Change: `l1-hot-route`. Гейт — `l1_hot_channel` (пре-регистрирован 2026-09-01,
пороги не тронуты); грид — `tools/eval/matrix_l1_route_grid.yaml`: C0, C7r20
(`hot_ngram_slot_ratio` 0.2 = 1 слот из 5), C7r40 (0.4 = 2 слота), оба при
порогах горячести 2 / 0.25. Отчёты `eval_2026-09-02_l1-route_{noctx,ctx}.md`
(+ JSON). Прод-копия 01.09 (`f108b13d…`), 500 × 3 сида на арм.

## Что изменилось против M3R-145

M3R-145 нашёл, что затравка доходила до ответа в 0.7–2.1% самостоятельных
ответов, потому что сидировалась только первая попытка. Маршрут сидирует
первые `route_slot_budget` попыток цикла, каждую своей n-граммой; кандидат
идёт общим путём (прогулка, финализация, гейты, дописка, мутации) и несёт
`route = hot`. Ожидание до прогона (design): coverage 15–20% при двух слотах.

## Числа, noctx (канал действует)

| | C0 | C7r20 (1 слот) | C7r40 (2 слота) |
|---|---|---|---|
| **coverage: победитель стартовал из затравки** | 0 | **14.1%** | **29.9%** |
| маршрут `hot`: доля пула / присутствие / win при присутствии | — | — | 0.400 / 1.000 / **0.405** |
| `exact_context_copy_rate` | 0.004 | 0.007 (Δ +0.003) | 0.003 (Δ −0.001) |
| `repetition_rate` | 0.000 | 0.000 | 0.000 |
| `context_affinity_without_copy` | 0.023 | 0.025 | 0.020 |
| ECB пула | 4.536 | 4.545 | 4.515 |
| **escape окна** | 2.863 | 2.943 (**+0.081***) | 2.977 (**+0.114*** [0.035, 0.185]) |
| `historical_meme_rate` | 0.019 | 0.013 (Δ −0.005) | 0.021 (Δ +0.003) |
| мем-регресс (набор 27) | 6/27 | 6/27 | 5/27 |
| розыгрышей маршрута / пусто | — | 1845 / 0% | 1845 / 0% |
| p95, мс | 26.5 | 25.9 | 23.9 |

Ни одна дельта вреда не значима; escape окна вырос значимо в обоих армах.

**ctx:** маршрут по построению не запускается при контексте (13 генераций
из 1500 — промпты без контентных токенов — прошли как самостоятельные);
copy Δ +0.001, affinity Δ −0.000, escape Δ +0.002 — инертен.

## Вердикт

Механика работает и ожидание перевыполнено: **coverage 29.9% при полу 5%**
(против 2.1% у M3R-145), hot-кандидат присутствует в каждом пуле и выигрывает
**40% пулов**, вреда нет (copy, repetition, ECB — плоские, латентность даже
ниже), а **escape окна вырос на +0.114*** — маршрут добавил в окно отбора
траектории, которых обход не давал. Это первый маршрут, сдвинувший главную
метрику проекта (M3R-011), и сдвинул он её в noctx — режиме, где отбор был
ближе всего к жребию (map §1.2).

**Гейт при этом — `insufficient data`, и причина инструментальная, а не
маршрута.** Must-improve `historical_meme_rate` не сдвинулся, потому что
измерить он не может: набор мем-регресса живёт в `eval_prompts.yaml`
(версия `308b7deaea0f`, сгенерирована на снапшоте **2026-07-13**), а отбор
горячих n-грамм — на копии 01.09. Проверено прямо: пересечение 8 n-грамм
пула маршрута с 27 мемами набора — **0**; ни один мем набора не содержит ни
одной n-граммы пула. Метрика слепа к маршруту по построению: он
воспроизводит сегодняшние мемы чата, а набор помнит июльские.

## Что остаётся владельцу

1. **Решение о перегенерации набора промптов/мемов** на копии 01.09:
   `python -m tools.eval --generate-prompts --db db_prod_copy/markov.db --label snapshot_full_2026-09-01`.
   Это смена `version` — осознанный разрыв сопоставимости со **всеми**
   прошлыми отчётами (протокол §6), поэтому решение не принимается здесь.
   Без него must-improve гейта L1 не может сработать ни для какого маршрута.
2. После перегенерации — повторный прогон грида (обоих режимов, ~8 минут) и
   C0-базлайн новой версии.
3. Раунд связности в noctx (`solo_rating_round prepare --matrix
   tools/eval/matrix_l1_route_grid.yaml --context-mode noctx`).
4. Промоушен `hot_ngram_slot_ratio` (0.4, при порогах 2 / 0.25) — отдельное
   изменение дефолтов с перезаморозкой хеша; до него дефолты 0 / 3 / 0.5.

## Сопоставимость

Все армы парные по промптам и сидам на одной копии; C0 совпадает с C0 гридов
M3R-145 и M3R-110 (та же копия, та же версия промптов).

## Повтор на новой версии набора (bababb4b7693, снапшот 2026-09-01)

После решения O18 набор перегенерирован (см. `CLOSED.md`, 2026-09-02:
определение мема выровнено по каналу, 18 мемов при поле поддержки 2, 6 из 8
n-грамм пула маршрута входят в набор). Отчёты
`eval_2026-09-02_l1-route-v2_{noctx,ctx}.md`, базлайн новой версии —
`eval_2026-09-02_baseline-2026-09-01_{noctx,ctx}.md`.

**noctx:**

| | C0 | C7r20 (1 слот) | C7r40 (2 слота) |
|---|---|---|---|
| coverage | 0 | 14.6% | **30.5%** |
| `historical_meme_rate` | 0.053 | 0.192 (**+0.139***) | 0.352 (**+0.299*** [0.243, 0.352]) |
| `exact_context_copy_rate` | 0.003 | 0.009 (**+0.006*** [0.001, 0.011]) | 0.004 (+0.001, н.з.) |
| `repetition_rate` | 0.000 | 0.000 | 0.000 |
| ECB пула | 4.527 | 4.546 | 4.526 |
| escape окна | 2.868 | 2.957 (+0.089*) | 2.959 (**+0.091***) |
| мем-регресс (набор 18) | 10/18 | 11/18 | 11/18 |
| p95, мс | 25.9 | 21.8 | 21.9 |

**ctx:** маршрут инертен (copy Δ +0.001, affinity Δ 0.000, escape Δ 0.000).

**Вердикт `l1_hot_channel`:**

- **C7r40 (ratio 0.4, два слота): все автоматические условия пройдены** —
  coverage 30.5% при полу 5%, must-improve взят с большим запасом (доля
  ответов на meme-bait с воспроизведённым мемом выросла с 5% до 35%), copy и
  repetition не выросли, ECB на месте, escape окна вырос, латентность ниже
  базлайна. Гейт остаётся `insufficient data` ровно по одному пункту —
  **раунд связности не проведён**; список подготовлен:
  `rating_rounds/l1-route-v2/rating_list.txt`.
- **C7r20 (один слот): fail** по copy — +0.006* (0.003 → 0.009). Абсолютная
  величина ничтожна, но правило пре-регистрировано: значимый рост
  дисквалифицирует. Один слот не даёт и половины эффекта двух, так что арм
  не жалко.

Инструментальная оговорка первого прогона снята: метрика больше не слепа, а
её рост — это ровно то, что маршрут обещает (воспроизводить сегодняшние мемы
чата), при нулевой цене по копированию. Оговорка о цикличности (design D4
`l1-hot-channel-sweep`) остаётся: набор и пул маршрута читают одну таблицу
одним определением, поэтому метрика измеряет «доживает ли затравка до
ответа», а не «нашёл ли бот мем». Доживает: 30.5% ответов против 2.1% у
одной попытки.

**Что остаётся владельцу:** оценить `rating_rounds/l1-route-v2/rating_list.txt`
(noctx; C0 против C7r20/C7r40), затем
`python -m tools.solo_rating_round score --label l1-route-v2 --answers rating_rounds/l1-route-v2/answers.txt`
и прогон грида в noctx с `--solo-rating`. При связности не ниже C0 − 10 п.п.
— промоушен `hot_ngram_slot_ratio = 0.4` при порогах 2 / 0.25 отдельным
изменением дефолтов (перезаморозка хеша; можно одним пакетом со splice 0.4).
