# Вердикт M3R-110: состав пула в ctx-режиме (свип контекстных ручек) — 2026-09-02

Change: `pool-composition-sweep`. Гейт `pool_composition` зарегистрирован в
`tools/eval/eval_thresholds.yaml` **до** прогона (2026-09-01); грид —
`matrix_pool_grid_1.yaml` (C0, C8b30, C8b40, C8s40) и `matrix_pool_grid_2.yaml`
(C0, C8a10, C8m); отчёты `eval_2026-09-02_pool-grid{1,2}_{ctx,noctx}.md`
(+ JSON). Прод-копия 01.09 (`f108b13d…`), 500 × 3 сида на арм, ревизия ветки
`chore/session-sync-and-track-b`. **Сводки C0 между двумя файлами совпали
побитово в обоих режимах** — парные дельты сравнимы между файлами.

## Армы

| арм | `reply_context_start_bias` | `context_anchor_splice_probability` | `generation_attempts_with_context` |
|---|---|---|---|
| C0 | 2.2 | 0.25 | 5 |
| C8b30 / C8b40 | **3.0 / 4.0** | 0.25 | 5 |
| C8s40 | 2.2 | **0.4** | 5 |
| C8a10 | 2.2 | 0.25 | **10** |
| C8m | **3.0** | **0.4** | **10** |

Третья ось стала измеримой только с ручкой (`context-attempts-knob`, тот же
день). Coverage гейта — сдвиг доли ctx-ответов, чей победитель стартовал из
контекста (`context` / `hidden_context` / `context_spliced`); источник
победителя восстанавливается через дописку и мутацию (design D2), поэтому
доля у C0 — 50.3%, а не заниженные ~35% при поиске по тексту.

## Числа, ctx (парные дельты к C0, `*` — интервал исключает 0)

| | C0 | C8b30 | C8b40 | C8s40 | C8a10 | C8m |
|---|---|---|---|---|---|---|
| контекстных стартов у победителя | 50.3% | 56.3% (+5.9) | 59.9% (+9.5) | 56.0% (+5.7) | 55.9% (+5.5) | 66.7% (+16.4) |
| `context_affinity_without_copy` | 0.211 | +0.013* | +0.026* | **+0.031*** [0.023, 0.039] | +0.028* | +0.075* |
| `exact_context_copy_rate` | 0.219 | **+0.041*** | **+0.054*** | +0.001 | **+0.025*** | **+0.059*** |
| `repetition_rate` | 0.003 | −0.001 | −0.001 | −0.002 | 0.000 | −0.001 |
| ECB пула (пол 4.0) | 4.430 | 4.411 | 4.369 (−0.061*) | 4.437 | 4.375 (−0.055*) | 4.363 (−0.067*) |
| escape окна | 2.081 | **−0.046*** | −0.026 | −0.019 | **−0.055*** | −0.050 [−0.105, 0.006] |
| `historical_meme_rate` | 0.309 | +0.019 | +0.037* | +0.008 | +0.008 | +0.032 |
| p95, мс | 33.8 | 34.6 | 33.3 | 34.7 | 33.6 | 32.8 |

**noctx** (оба файла): все дельты всех армов ровно 0.000 — ручки инертны без
контекста по построению, взвешивание по прод-доле (89% ctx) сводится к
ctx-вердикту.

## Вердикт по армам

| арм | автоматика | что решило |
|---|---|---|
| C8b30 | **fail** | copy +4.1 п.п.*, escape окна −0.046* |
| C8b40 | **fail** | copy +5.4 п.п.* |
| **C8s40** | **все условия пройдены**, `insufficient data` до раунда связности | affinity +0.031* при copy +0.001, ECB и escape на месте, coverage +5.7 |
| C8a10 | **fail** | copy +2.5 п.п.*, escape −0.055*, ECB −0.055* |
| C8m | **fail** | copy +5.9 п.п.* (affinity +0.075* — самый большой, но куплен копией) |

Закономерность одна и читается по всем пяти армам: **всё, что держит прогулку
ближе к контексту** (сильнее контекстный старт, дольше контекст в попытках,
их сумма), поднимает тематичность **и** копирование вместе — контекстный старт
пересказывает корпус вокруг промпта, ровно то, о чём предупреждала регистрация
гейта. Единственная ручка, дающая тематичность **без** копии, — **отложенный
якорь** (`context_anchor_splice_probability`): прогулка стартует глобально, а
контекст вклеивается позже. Это согласуется с R1 (сплайс-старты выигрывают
0.484 при доле 0.103) и с июльским A/B (splice 0.25 → якорность 0.33 → 0.58).

Побочно: ECB пула падает значимо у трёх армов (до 4.36–4.38) — всё ещё выше
пола 4.0, но направление то же: чем ближе к контексту, тем однороднее пул.

## Что остаётся владельцу

1. **Раунд связности для C8s40** (ctx, соло-протокол M3R-020):
   `python -m tools.solo_rating_round prepare --db db_prod_copy/markov.db --matrix tools/eval/matrix_pool_grid_1.yaml --context-mode ctx --label pool-c8s40`
   (в раунде участвуют все армы файла; оценивать имеет смысл C0 и C8s40).
   Порог: связность не ниже C0 − 10 п.п.; валидность — само-консистентность
   ≥ 0.8 и ≥ 80% пойманных декоев.
2. **Тик связок не измерен здесь** (метрика `connective_reply_rate` живёт в
   `eval_prod`, не в протокольном харнессе): июльские числа — 0.65 у живого
   пакета при splice 0.25, 0.75 при 0.5, порог ~0.8. Перед промоушеном 0.4
   снять её одним прогоном `eval_prod` (или перенести метрику в `tools/eval`
   отдельным изменением).
3. **Промоушен** `context_anchor_splice_probability` 0.25 → 0.4 при
   положительном раунде — отдельное изменение: это смена дефолта, она
   **сдвигает `generation_hash`** и идёт пакетом с перезаморозкой базлайна
   (§2 `CLAUDE.md`).

`reply_context_start_bias` и `generation_attempts_with_context` остаются на
дефолтах: их оси закрыты с цифрами — тематичность там покупается копией.

## Сопоставимость

C0 этого грида (ctx: affinity 0.211, copy 0.219, escape 2.081, ECB 4.430)
снят на копии 01.09 и с escape-базлайном 14.08 (копия 13.08: 2.169 / 4.455)
не сравнивается — другой корпус. Между собой все армы парные по промптам и
сидам; C0 двух файлов совпал побитово.

## Повтор на новой версии набора (bababb4b7693, снапшот 2026-09-01)

Файл грида 1 (C0, C8b30, C8b40, C8s40) прогнан заново после смены версии
набора (O18, `CLOSED.md` 2026-09-02). Отчёты
`eval_2026-09-02_pool-grid1-v2_{ctx,noctx}.md`; C0 совпал побитово с C0
базлайна новой версии и грида hot-маршрута в обоих режимах. Файл 2 (C8a10,
C8m) не перегонялся: его армы проваливались по copy на +2.5…+5.9 п.п., и
смена промптов этого не отменяет.

**ctx** (парные дельты к C0):

| | C0 | C8b30 | C8b40 | C8s40 |
|---|---|---|---|---|
| контекстных стартов у победителя | 52.4% | 58.9% (+6.5) | 63.1% (+10.7) | 58.5% (+6.1) |
| `context_affinity_without_copy` | 0.253 | +0.018* | +0.029* | **+0.032*** [0.023, 0.041] |
| `exact_context_copy_rate` | 0.226 | **+0.028*** | **+0.053*** | +0.007 [−0.004, 0.018] |
| `repetition_rate` | 0.002 | 0.000 | 0.000 | −0.001 |
| ECB пула | 4.485 | 4.466 | 4.449 (−0.035*) | 4.469 |
| escape окна | 2.038 | −0.033 [−0.077, 0.010] | **−0.049*** | **−0.042*** [−0.077, −0.011] |

**noctx:** все дельты всех армов ровно 0.000 (ручки инертны без контекста).

Латентность в этом прогоне снята при шести параллельных процессах (p95
41–104 мс при базлайне 48) — в бюджете 150, но между армами не сравнима;
одиночные прогоны той же копии дают p95 33–35 мс.

**Вердикт по армам, канонический (новая версия):**

| арм | вердикт | что решило |
|---|---|---|
| C8b30 | fail | copy +2.8 п.п.* |
| C8b40 | fail | copy +5.3 п.п.*, escape −0.049*, ECB −0.035* |
| **C8s40** | **fail** | **escape окна −0.042*** при affinity +0.032* и copy в шуме |

На старой версии escape у C8s40 был −0.019 [−0.051, 0.014] — интервал
накрывал ноль, и арм шёл на раунд связности. На канонической версии
интервал ноль исключает, а правило пре-регистрировано: значимое сужение
окна дисквалифицирует. Механика читается одинаково в обоих прогонах —
отложенный якорь покупает тематичность тем, что скорер сверх-выбирает
сплайс-кандидатов (замер 2026-07-16: unknown-победители 60 → 13), то есть
сужением окна, — а ровно этот размен M3R-011 назвал узким местом. Вывод
M3R-110 на канонической версии: **ни один арм не проходит**; тематичность в
ctx-режиме за счёт контекстных ручек покупается либо копией, либо окном.

Раунд связности для C8s40 (`rating_rounds/pool-c8s40-v2`) **не нужен**:
связность спасала бы арм, прошедший автоматику, а он её не прошёл. O17
закрывается с цифрами.
