# Воронка причуд завсегдатаев — окно 24.08→01.09 (observe-user-quirk-channel)

Снято владельцем из `/stats` перед редеплоем 01.09.2026; окно — с рестарта
24.08 (сборка с воронкой M3R-141-образца), 409 генераций, 63 адресованных
ответа. Все числа — процессные счётчики, обнулены редеплоем.

## Числа

```
причуды: сработало 2 из 63 ответов
по гейтам: адресность 12/63, ручка 0/51, сутки 7/51, розыгрыш 41/44, порог 1/3
```

Читается цепочкой отказов (числитель — отказы, знаменатель — дошедшие до
гейта):

| гейт | дошло | отказало | прошло |
|---|---|---|---|
| адресность | 63 | 12 | 51 |
| ручка `user_quirk_chance > 0` | 51 | 0 | 51 |
| суточный лимит на пару (чат, человек) | 51 | 7 | 44 |
| ролл `random() < user_quirk_chance` | 44 | 41 | 3 |
| порог `interactions ≥ user_quirk_min_interactions` | 3 | 1 | 2 |
| **сработало** | | | **2** |

## Вывод по решающему правилу заявки

Канал **жив**: две причуды сыграли в живом чате — вопрос «какой гейт душит»
получил ответ фактом, а не расчётом. Душит **ролл**: 41 отказ из 44 дошедших
(93%), тогда как порог взаимодействий, которого касалась исходная гипотеза
`diagnose-user-quirk-threshold`, отсёк 1 из 3. Суточный лимит заметен, но
вторичен (7 из 51).

Решение — что менять и менять ли (`user_quirk_chance`, суточный лимит, ничего)
— за владельцем, **отдельным изменением** (задача 6.4 заявки): при текущей
ручке канал выдаёт ~2 срабатывания на ~60 обращений, и если такая редкость —
задуманный вкус, чинить нечего.
