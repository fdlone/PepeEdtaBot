# Журнал закрытого

Что находили и что с этим сделали. Детали — в git: `gh pr view <N>`, `git log`.
Аудит-документы и планы реализации свёрнуты сюда 2026-07-14; их полный текст
остаётся в истории (`git show 1f4734f:docs/audits/`).

Всё незакрытое — в [OPEN.md](OPEN.md).

## Аудиты и ревью

| Дата | Охват | Итог |
|---|---|---|
| 2026-09-02 | Заявка `assoc-route-pilot` — пилот ассоциативного маршрута M3R-200 снят, viable | `CandidateRoute.ASSOC`, ручка `assoc_slot_ratio` (дефолт 0, хеш не сдвинут на обоих снимках), `rank_associates` (PMI-соседи якорей из `transitions`, без df), общий helper сборки с seeded. Прод-копия 01.09, оба режима, C0 побитово равен базлайну версии: present 72–73%, ECB пула +0.10…+0.18\* (не дублирует обход), escape окна не растёт (noctx −0.115\* при двух слотах), p95 22–37 мс, ctx copy −2.3…−3.0 п.п.\* при флэт affinity, noctx affinity без копий +0.007…+0.012\*, meme rate +0.045…+0.064\*. Планки `assoc_pilot` пре-регистрированы; вердикт `eval_2026-09-02_assoc-pilot-verdict.md`; дальше — гейт M3R-220. |
| 2026-09-02 | Заявка `knob-census` — перепись ручек M3R-151 закрыта с цифрами | 126 армов × 2 режима на копии прода 01.09, оба C0 побитово равны базлайну версии `bababb4b7693`. 56 свипнутых ручек: strong 23, weak 12, gated 13, inert 8; статически мёртвых нет (первый отчёт показал шесть «dead» — ошибка скана `_read_sites`, пропускавшего `runtime_state.py`; починено с тестом). Две ручки горячего пула мертвы данными при дефолтных порогах (пул пуст при 3 / 0.5, 8 фраз при 2 / 0.25 — см. O18), кэш и тень order-4 нулевые по построению, штраф недавних и три ручки формы харнесс не видит. Удалять по цифрам нечего; вход M3R-150 — 12 weak на сужение домена. Вердикт: `eval_2026-09-02_knob-census.md`. |
| 2026-09-02 | O17 закрыт с цифрами: `context_anchor_splice_probability` 0.4 не проходит гейт на канонической версии набора | Повтор грида 1 M3R-110 на `bababb4b7693`: affinity без копий +0.032*, copy +0.007 (шум), но **escape окна −0.042* [−0.077, −0.011]** — пре-регистрированное условие `window_escape_delta_floor` нарушено (на старой версии −0.019, интервал накрывал ноль). Механика: отложенный якорь поднимает тематичность сверх-выбором сплайс-кандидатов, то есть сужением окна отбора — размен, который M3R-011 назвал узким местом. Раунд связности не нужен; тик связок (0.858 → 0.860) остаётся справочным. Итог M3R-110 на канонической версии: ни один арм не проходит; дефолты контекстных ручек остаются. Разбор — `eval_2026-09-02_pool-grid-verdict.md`, раздел «Повтор на новой версии». |
| 2026-09-02 | Смена версии набора промптов/мемов: `308b7deaea0f` (снапшот 2026-07-13) → `bababb4b7693` (снапшот 2026-09-01) | Решение владельца (O18). Прежний набор помнил мемы июля: пересечение его 27 мемов с горячими n-граммами копии 01.09 — ноль, гейт L1 был слеп по построению. Первая перегенерация дала **3** мема при 13 строках `cnt ≥ 2`: генератор требовал, чтобы **каждый** токен n-граммы был контентным, тогда как канал (`hot_ngrams.is_content_ngram`) считает контентной n-грамму хотя бы с одним контентным токеном — два читателя одной таблицы расходились в том, что такое фраза (правило фразового индекса). Определение выровнено по каналу (`tools/eval/prompts.py`), вторая перегенерация — **18 мемов** при поле поддержки 2, 6 из 8 n-грамм пула маршрута входят в набор. **Осознанный разрыв сопоставимости** (протокол §6): все отчёты до 02.09 сняты на старой версии и с новыми числами не сравниваются; базлайн новой версии — `eval_2026-09-02_baseline-2026-09-01_{ctx,noctx}.md`. Пороги гейтов не тронуты. |
| 2026-09-02 | Заявки `selection-knobs` и `selection-window-grid` — M3R-100, окно отбора измерено | `SELECTION_SCORE_MARGIN`, `CONTEXT_RELEVANCE_WEIGHT/CAP` стали ручками с прежними дефолтами; новый candidate-level бонус различности `selection_diversity_bonus` (дефолт 0; кандидату с перекрытием рёбер < 0.5 относительно лучшего — `bonus × (1 − overlap)`); `trajectory_edges`/`edge_overlap` переехали в `app/core/trajectory.py` — одно определение для гейта и механизма. Хеш не сдвинут на обоих снимках. Гейт `selection_window` зарегистрирован до прогона (coverage — доля входов с одной траекторией −5 п.п., escape ↑, affinity/copy/repetition не хуже, ECB, связность в ctx, два режима); 6 армов × 2 режима, C0 совпал с базлайном версии. **Итог:** окно открывается сильно (запас 0.5 / бонус 0.2: доля ctx-входов без альтернативы 36% → 23%, escape +0.4…+0.55*, copy −2 п.п.*), цена — affinity без копий −0.010* (−4%) — ни один арм не проходит правило без допуска; бонус 0.2 в noctx проходит всё, бонус выше запаса окна сужает окно. Решение владельца: допуск на тематичность (иначе названный гейт) либо расщепление бонуса по режиму. Вердикт: `eval_2026-09-02_selection-grid-verdict.md`. |
| 2026-09-02 | Заявка `l1-hot-route` — L1 как маршрут с бюджетом слотов (M3R-230) | По решению владельца после M3R-145. `CandidateRoute.HOT`, ручка `hot_ngram_slot_ratio` (0..0.7, дефолт 0 — хеш не сдвинут на обоих снимках); первые `route_slot_budget(target, ratio)` попыток цикла сидируются своими горячими n-граммами без возврата из `generation_rng`, только для ответов без контекста; кандидат идёт общим путём (прогулка, финализация, гейты, дописка, мутации), легаси-затравка конвейера при включённом маршруте не подаётся. Замер на копии 01.09 при 2 / 0.25: coverage **14.1% / 29.9%** (1 / 2 слота; пол 5%, у M3R-145 было 2.1%), hot в каждом пуле, win при присутствии 0.405, copy/repetition/ECB плоские, p95 ниже, **escape окна +0.081* / +0.114*** — первый маршрут, сдвинувший главную метрику M3R-011. Гейт `l1_hot_channel` — `insufficient data`: must-improve `historical_meme_rate` слеп по построению — набор мем-регресса из `eval_prompts.yaml` (снапшот 2026-07-13) не пересекается с горячими n-граммами копии 01.09 ни одной позицией (проверено). Дальше — O18. Вердикт: `eval_2026-09-02_l1-route-verdict.md`. |
| 2026-09-02 | Заявки `context-attempts-knob` и `pool-composition-sweep` — свип M3R-110 снят | `GENERATION_ATTEMPTS_WITH_CONTEXT` стал ручкой `generation_attempts_with_context` (дефолт 5, хеш не сдвинут на обоих снимках) — иначе третья ось свипа неизмерима. Гейт `pool_composition` зарегистрирован до прогона (coverage — сдвиг доли контекстных стартов победителя ≥ 5 п.п., must-improve affinity без копий, copy/repetition/escape окна не хуже, ECB ≥ 4.0, связность в ctx, два режима); источник старта победителя теперь восстанавливается через дописку и мутацию. Пять армов × 2 режима на прод-копии 01.09, C0 двух файлов грида совпал побитово. **Итог:** `start_bias` 3.0/4.0, `attempts_with_context` 10 и мягкая комбинация поднимают тематичность вместе с копией (+2.5…+5.9 п.п.*) и сужают окно — fail; **`context_anchor_splice_probability` 0.4** даёт affinity без копий +0.031* при copy +0.001, ECB и escape на месте — все автоматические условия пройдены, ждёт раунда связности (ctx) и замера тика связок в `eval_prod`. Дефолты не менялись; промоушен 0.4 — отдельное изменение с перезаморозкой хеша. Вердикт: `eval_2026-09-02_pool-grid-verdict.md`. |
| 2026-09-01 | Заявка `l1-hot-channel-sweep` — M3R-145 закрыта с цифрами | Свип порогов горячести L1 (2/0.5, 2/0.25, 2/0.0; `min_count = 1` инертен по построению — `LIMIT 8`) в двух режимах на прод-копии 01.09, гейт `l1_hot_channel` зарегистрирован до прогона. Харнесс впервые воспроизводит затравку конвейера (только noctx: адресованные ответы не сидируются; отдельный ГСЧ, C0 байт-в-байт), запись несёт `seed_drawn` и `start_source`, отчёт печатает розыгрыши и долю пустых. Итог: отбор оживает (пусто 0%, 2–6 из 8 n-грамм открывают старт), вреда нет (copy/repetition/affinity плоские, p95 24 мс), мем-регресс 6 → 9 из 27 у 2/0.0, но **coverage 0.7 / 1.7 / 2.1% при полу 5%** — `insufficient data` во всех армах, дефолты 3/0.5 остаются. Причина не в порогах: `seed_tokens` подаётся только первой попытке, и один сидированный кандидат выигрывает ~1 пул из 10 (потолок ≈ 2%, ≈ 8% даже при шансе 1.0). Раунд связности не проводился — нечего спасать. Оживление канала — задача дизайна затравки (несколько попыток или маршрут с бюджетом слотов O10), отдельным решением владельца. Хеш генерации не задет (`tools/eval` только). Вердикт: `eval_2026-09-01_l1-grid-verdict.md`. |
| 2026-09-01 | Заявка `eval-route-breakdown` — отчётная часть M3R-103 | Рантайм атрибуции маршрутов жил с 14.08, а в `tools/eval` поле `route` не доходило ни до записи, ни до отчёта — первый маршрутный грид не принялся бы по правилу роадмапа «без атрибуции результат не принимается». Запись генерации получила маршрут победителя и маршруты кандидатов пула (от генератора, не из текста; захват в том же хуке отбора, что у M3R-011, вне таймера латентности); телеметрия маршрутов едет в снапшот прогона плоскими ключами, отклонения — суммой по классам M3R-021. Отчёт: раздел «Per-route breakdown» — попытки, доля пула, присутствие, win при присутствии, affinity без копий и copy у выигранных ответов, латентность с маршрутом / без (верхняя оценка цены, не шаг), отклонения по классам; выключенный маршрут — `not attempted`, не нули. `metrics_summary` не тронут (закреплено тестом). Хеш генерации не задет по построению (правки только в `tools/eval`), синтетика сверена. |
| 2026-09-01 | Заявка `tune-user-quirk-channel` — рабочая точка причуд L2 (пункт 6.4 `observe-user-quirk-channel`) | Воронка 24.08→01.09 (`eval_2026-09-01_quirk-funnel.md`): 63 адресованных ответа, сработало **2**; душит **ролл** — 41 отказ из 44 дошедших при шансе 0.1, порог 25 отсёк 1 из 3, суточный лимит 7 из 51. Решение владельца: `user_quirk_chance` 0.1 → **0.3**, `user_quirk_min_interactions` 25 → **10** (дефолты реестра и `.env.example`; код канала, порядок гейтов и суточный лимит не тронуты; откат `/set` без рестарта). Ожидание записано до окна: при прежнем темпе ~13 прошедших ролл и 8–10 срабатываний за такое же окно против 2; решающее правило — в `design.md` заявки. Хеш генерации не задет по построению (канал после генерации), сверено на обоих снимках. Вступает в силу на ближайшем рестарте; окно наблюдения — `docs/OPEN.md`. |
| 2026-09-01 | Заявка `route-slot-budget` (O10, ветвь 2 — вопрос закрыт целиком) | Маршруты клали кандидатов **сверх** заполненного пула, и абсолютный порог `pool_ecb_min: 4.0` сравнивал счёт с плывущим знаменателем. Замер до правки (прод-копия, 3 сида × 60): C0 ровно **5.000**, C4 (`ratio=0.3`) — **6.400** при максимуме **7**; расчётные 11 на трёх маршрутах Track B сделали бы порог 4 тривиальным. Решение — бюджет слотов: `route_slot_budget(target, ratio) = min(max(1, round(target·ratio)), target // 2)`, маршрут производит кандидатов **до** обхода, обход добирает остаток (его условие остановки уже считало общий размер пула, поэтому цикл не переписывался). Потолок в половину пула не даёт маршруту выставить пул без кандидатов обхода; при пуле в одно место маршрут не запускается. После правки C4 даёт **5.000** (макс 5) при seeded в 0.78–0.83 пулов — маршрут не вытеснен, а переведён в конкуренцию за места. **Порог не двигался**: изменение вернуло ему знаменатель. Ветвь 1 (доля `structural_pool_ecb_share` рядом с абсолютом) закрыта ревью 26.08 — обе ветви развилки пройдены. `generation_hash` не сдвинут на обоих снимках: хеш снимается на дефолтах реестра, где ручка маршрута равна 0 и блок не исполняется — **перезаморозка не понадобилась**, вопреки первоначальной оценке в записи O10. Оговорка: числа seeded, снятые до этой правки, с послеправочными несравнимы — менялся состав пула, в котором seeded конкурирует. |
| 2026-09-01 | O7 закрыт: прод-доля режимов измерена | Владелец снял `/stats` перед редеплоем 01.09: **`ctx` = 89% ответов** (409 генераций, окно 24.08→01.09 — сборка от 24.08 уже несла счётчики M3R-140/141, что выяснилось по `applied_at` миграции 022 в автоснимке). Это вес для `requires_both_modes`-гейтов. Тем же снятием: контекст терялся у **20%** ctx-ответов (на копии 13.08 было 37%), «горячие фразы: пусто в 100% из 3» — L1-канал мёртв данными и в живом проде (map §3.2б подтверждена), order-4 в тени 0/43, причуды L2 **сработали 2 раза из 63** — канал жив, воронка снята (разбор — заявка `observe-user-quirk-channel`). |
| 2026-09-01 | Ротация прод-копии: канонической стала копия из первого автоснимка | `db_prod_copy/markov.db` теперь распакованный первый живой автоснимок `db-snapshot-to-owner` (01.09, 13:25:33 UTC; пришёл владельцу в личку; integrity ok, состояние прода до миграции 023). Прежняя копия сохранена как `markov_2026-08-13.db`. **Осознанный разрыв хеш-непрерывности прод-копии:** прежний базовый `13e496c0…` относится к копии 13.08, новый канонический — `f108b13da3571bb080290e6e37d56a5713f502c3b4bbfc62e8641ff6a1c3d54a`. Синтетический CI-базлайн не тронут. |
| 2026-09-01 | Заявка `persist-maintenance-cadence` (O11) | Суточный таймер ленивого обслуживания был монотонным и процессным: каждый рестарт откладывал мемо-пасс и пересборку фразового индекса ещё на сутки (сами decay-пассы на старте выполнялись). Момент последнего полного пасса теперь хранится в БД (`maintenance_meta`, миграция 023): `init()` доигрывает остаток интервала либо делает пасс просроченным сразу; метка пишется только на `DONE`, ретраи после сбоя — как были. Проверено сквозным тестом рестарта (мутация «вернуть монотонный срок» роняет обе половины) и прогоном миграции на копии прода. Хеш генерации не сдвинут (ожидание записано до правок, `hash-log.md`). Побочный пункт O11 «прод-копия старше сборки → требование свежей копии в §7» уходит в проход по документации; сам канал свежих копий закрыт заявкой `db-snapshot-to-owner`. |
| 2026-09-01 | Заявка `chat-timezone-for-temporal-branches` (O12) | «Ночные»/«пятничные» ветки (`_temporal_bottoms` в `/pivo`, `is_late_night` фолбэков) считались по naive `datetime.now()` при UTC-контейнере — для MSK-чата ночь играла с 03:00 до 09:00 по местному. Решение из развилки O12: явная env-настройка `CHAT_TIMEZONE` (IANA-имя, дефолт `UTC` — байт-идентичен прежнему поведению), а не `TZ` контейнера; невалидная зона роняет запуск, а не молча падает в UTC (логов у владельца нет). Служебные сутки (капы/квоты/обслуживание) остаются на UTC осознанно. Новая зависимость `tzdata` (данные IANA без кода, fallback stdlib — на Windows-машине разработки базы зон нет вовсе). Хеш генерации: ожидание «сдвига нет» записано до правок и подтверждено (`hash-log.md` change'а). **Включение в проде — действие владельца: `CHAT_TIMEZONE=Europe/Moscow` в env на ближайшем редеплое, без этого поведение прежнее.** Выставлено на редеплое 01.09 (подтверждено владельцем 2026-09-01). |
| 2026-09-01 | Заявка `pin-private-chat-diagnostics` (O13) | Расхождение «README: все команды только для групп» против кода (`/ping`, `/help`, `/config` отвечали в личке) закрыто решением владельца: личка — диагностический канал `/ping`+`/help`, потому что у владельца нет доступа к докеру и логам (туда же приходят отчёты `/pivo` и алерты обслуживания); `/config` ушёл под `GROUP_ONLY` — настройки чат-скоуплены, в личке им взяться неоткуда. Состав закреплён тестом `TestPrivateChatCommandSurface` (полный инвентарь команда→доступность, проверен мутацией в обе стороны), README переформулирован. Хеш генерации не задет по построению. |
| 2026-08-26 | Мультиагентное ревью, раунд 1: архитектура, безопасность, надёжность, качество, домен/UX (50 находок) | 0 Critical. Закрыто правкой 8: обход кулдаунов подписью к медиа (мидлварь читала `text`, фильтр aiogram — `text or caption`); знаменатели маршрутов не считались при пустом пуле (смещение в сторону «маршрут полезен» перед первым маршрутным гридом); маскирование `chat_id` в двух перехватах чужих исключений; тест `forget_chat` проверял 3 кэша из 11; `/config` и `/setprob` выдавали мёртвую при включённом директоре `reply_probability` за живую; `ORDER BY` в чтении, питающем розыгрыш эмодзи; доля `pool_ecb/pool_size` рядом с абсолютным порогом; два протухших указателя в CLAUDE.md + гард `tests/test_doc_pointers.py`. **Отклонено Арбитром 2:** «утёкшие тестовые БД в корне» (файлов нет) и «суточные пассы в проде мертвы» — доказательство опиралось на `db_prod_copy` от 13.08, которая старше рестарта прода 14.08 и двух миграций; живое наблюдение обратного в `/stats` 21.08. Открытое вынесено в O8–O13 `OPEN.md`. |
| 2026-08-26 | Заявки `atomic-reply-reservation` (O8) и `config-full-from-registry` (O9) | O8: решение «отвечаем» и окно команды стали атомарными — резервация в момент решения с откатом, стамп троттлинга до хендлера. `tasks_concurrency_limit=1` рассмотрен и отвергнут: сериализация ставит каждое сообщение за чужую имитацию набора (до 4 с). При реализации найдены и исправлены два дефекта собственного дизайна — ложное утверждение о разнице «восстановить/удалить» стамп и трактовка частичной доставки как отсутствия ответа (`Sender` теперь сообщает число доставленных частей). O9: полная форма `/config` порождается из реестра — было 52 ключа из 97 при трёх обещаниях «полный список», причём спека `chat-scoped-settings` требовала полноты с самого начала. Хеш генерации не сдвинут обеими заявками. |
| 2026-06-29 | Полный независимый аудит: архитектура, безопасность, БД, async, тесты, долг | 0 Critical/High. Закрыты: CI-гейт зависимостей (`safety` → `pip-audit`), отказ от плейсхолдерных секретов, полный `mypy strict`, 8 избыточных индексов, инкрементальный объём модели, кэш админов, сложность `markov.py` под характеризационными тестами. PR #49 + follow-ups. |
| 2026-07-02 | Аудит генерации диалога | Движок сильный, «театр» слабый: нет состояния, вариативности формы, персональности. Родил план Stage 1–4 — весь реализован (ниже). |
| 2026-07-04 | Follow-up ревью (N1–N7) | Обращения обходили кулдаун и часовой кап; `/clear` не чистил данные `/pivo`; анти-повтор `/pivo` крутился при отказе по квоте; throttle-notify не был ограничен; HEALTHCHECK ходил в БД от root. Всё исправлено (PR #56). |
| 2026-07-06 | Дифференциальное ревью PR #55–58 | 0 уязвимостей. Три LOW (локальная TZ дневного капа, decay только на старте, `strip_trailing_emojis` резал голую пунктуацию) закрыты сразу (PR #59). |
| 2026-07-12 | Симуляционный аудит генерации: 2 сценария × 900 сообщений через реальный хендлер | SIM-1…SIM-9: мёртвая order-1 цепь, дублирующие компоненты скоринга, невидимые на дефолтах шансы L1/L3, вокатив без разделителя, мёртвая ручка hidden-context. Закрыты PR #70, #71, #73, #75, #76, #77. |
| 2026-07-14 | `eval_prod` до/после стека #70–#77 на копии прод-БД (чат `-1001234567890`, 1000 сообщений; 4 сида × 300 генераций на каждой стороне; база — `3f6f497`, мерж #69) | Регресса нет, обе headline-метрики сдвинулись в нужную сторону и знак дельты одинаков на всех четырёх сидах: `context_anchored_win_rate` 0.273 → 0.293, `verbatim_run_ratio_mean` 0.180 → 0.168. Средняя длина ответа 10.34 → 9.90 токена, латентность не изменилась (~152 мс), пустых ответов нет. На одном сиде разница лежала бы в пределах шума (SE ≈ 0.025 при n=300) — вывод держится только на согласованности знака по сидам. Побочно: stem-тир из #73 не даёт ни одного старта — разобрано в бэклоге [OPEN.md](OPEN.md). |
| 2026-07-15 | Полный per-knob свип генерации на прод-копии (29 конфигураций × 3 сида × 200 генераций + диалоговый режим с накоплением анти-повторов) | Сильные ручки: `reply_context_start_bias` (главный рычаг «в тему»), `markov_jump_probability`, `length_mode_weights`, `verbatim_penalty_strength` (насыщение после 1.5), `markov_order`, `length_context_adaptation`. Инертные: `candidate_selection_temperature` (margin решает), `randomness_strength` (best-of-5 сглаживает), `repetition_penalty_strength` (дублирующие гейты), шаговый `reply_context_bias`, `fuzzy_context_casefold` (**этот вывод опровергнут проходом B 2026-07-21**: ручка реальная, ctx_anchored −0.031 CI[−0.050,−0.013] — проход A был недомощен; см. OPEN.md). `recent_reply_penalty_strength`: точные повторы режет жёсткий гейт даже при 0. Ось компромисса всех сильных ручек одна: «в тему» ↔ «цитирует корпус». Найдены: мёртвая ручка `reply_context_last_tokens` (не читается с 3b6f174, phase 4.1d), цена `context_start_affinity` ~90 мс/генерацию (~60% латентности), слепое пятно verbatim-индекса (→ O4). |
| 2026-07-15 | Разнообразие швов: пул связок 6 → 12 фраз + «тихий сплайс» (новое предложение через точку, 35% швов) в обоих сплайс-путях (M4 и дописка цитат); метрика `connective_reply_rate` в eval_prod | Тик «82% ответов со связкой из шестёрки» разобран: связочная доля 0.78 → 0.53 (рекоменд. конфиг), видимость каждой конкретной фразы ~13% → ~4.4% ответов. Новизна/verbatim/distinct-2/пустые — без изменений; цены: длина −0.6 токена, якорность −0.03 (~1σ, направление устойчивое). |
| 2026-07-15 | O4-фикс (накопительный verbatim-индекс, миграция 016) + честный пере-baseline + калибровка share (10 сидов × 200 на конфигурацию) | Оконный индекс покрывал ~5k из 19.5k 4-грамм памяти цепи (75% слепоты). После фикса штраф и дописка прозрели: baseline — честная новизна 0.38→0.57, чистые корпусные ответы 31%→0.4%, verbatim(вся история) 0.51→0.37; пакет O3 — новизна 0.62, чистых пересказов 0%, якорность 0.434, дописка выигрывает 21% ответов (+50 мс, тонет в typing-паузе). Порог `verbatim_extension_share` в 0.8–0.95 инертен — распределение корпусной доли кандидатов бимодальное (пересказ ≈1.0 или новизна ≤0.6). Новая метрика `novel_ngram_share` + `pure_corpus_reply_rate` в eval_prod. Побочно: 82% победителей пакета несут связку из пула в 6 фраз → бэклог «швы». |
| 2026-07-15 | Замер структуры цепи на прод-копии + эксперименты «контекст+хаос» (A: `context_jump_boost`, B: `verbatim_extension_share`, C: `order_mix_probability`; свипы по 10 сидов × 200 генераций) | Ключевой факт: 97.9% order-3 состояний имеют ровно одно продолжение (ветвление 1.03; order-2 — 1.27), корпус цепи молодой (27.6k экземпляров 4-грамм ≈ 3–4 окна ретенции, почти все виданы однажды) — проходка на уровне слов детерминированно пересказывает источник, инертность ручек сэмплинга объясняется этим. Комбо boost=4 + share=0.8 + mix=0.5: якорность 0.295 → 0.442 (+50%), distinct-2 0.917 → 0.914 (в шуме), полные цитаты ≥90% → 0, пустых нет, латентность на месте; verbatim_run_ratio 0.177 → 0.216 — рост композиционный (ответ с прыжком = две корпусные половинки по ~0.31 при чистых безпрыжковых 0.015). Честный итог: выигрыш — якорность и структурное разнообразие (двухтемность, дописки); лексическая новизна не выросла (distinct флэт) и ручками ходьбы недостижима — новые словосочетания рождаются только в точках склейки. Код смёржен (#87, разнообразие швов — #88). Пакет (бывш. O3: boost=2.0, share=0.8, mix=0.5) включён вживую через `/set` после рестарта 2026-07-16; наблюдение — O1 в [OPEN.md](OPEN.md). |
| 2026-07-21 | O4 закрыт — доразбор свипа ручек (проход B: 15 плеч × 10 сидов × 400 = 4000 генераций/плечо) | Проход A (3×200) был статистически недомощен: межсидовый sd 0.040 топил эффекты ±0.02–0.04, отчего три ручки ошибочно назвали «шумными». На 400 генерациях sd 0.015, SE разности ~0.005. Итоги: `fuzzy_context_casefold` реальна (−0.031, CI не пересекает 0), её падение distinct_1 — артефакт; `context_jump_boost` монотонна (1.0→−0.019, 4.0→+0.036); `order_mix` монотонна и двусторонняя; «аномалия» `verbatim_extension_share=0.6` оказалась **артефактом самого eval** (расширённые победители уходили в «unknown» вместо «context» — починено, ручка плоская по тематичности); комбинации **суб-аддитивны** (резонанса нет), но `combo_hot` пробивает порог тика 0.80 накоплением — запас считать по сумме ручек. Латентность (A2): причина — пере-стемливание статичных стартов в `weighted_start3_choice`, `@lru_cache` на `stem_token` дал 203→79 мс при побайтово идентичном выводе. Метод-урок: ≥400 генераций на плечо, иначе мелкие эффекты не отличить от шума. |
| 2026-07-21 | Группа B: пакет «контекст+хаос» запечён в реестр + B1/B2 (order_mix 0.5→0.75, slot_mutation 0→0.15); B3/B4 отложены | Дефолты реестра и `.env.example`: `context_jump_boost` 1.0→2.0, `verbatim_extension_share` 0→0.8 (жили только как `/set`-оверлей — рестарт молча их сбрасывал), `order_mix_probability` 0→0.75, `slot_mutation_probability` 0→0.15. Совместная конфигурация проверена **как одно плечо** (6 сидов × 400 на прод-копии) — одиночные замеры её не покрывали: ctx_anchored флэт (−0.004), новизна **+0.024**, connective 0.651 (верх CI 0.665, порог тика 0.80 далеко), verbatim −0.006, distinct и длина без изменений, пустых нет, латентность 85 мс. Мутации выигрывают 8.9% ответов. B3 (intonation) отложен — оказался единственной ручкой, стоящей тематичности (−0.017) и укорачивающей ответы на 9%; B4 (combo_mild) отложен по решению владельца. Тесты: фикстура `TestNearQuoteExtension` явно гасит новые каналы (её `AsyncMock` их не заглушал), сьют 789 зелёный. **Вступает в силу на рестарте.** |
| 2026-07-21 | Код-ревью проекта: безопасность + упрощения (bandit, pip-audit, скиллы insecure-defaults / sharp-edges / python-anti-patterns + ручной разбор горячего пути) | bandit 3 low (все безобидные), pip-audit 0 CVE. Чисто и проверено: f-string SQL под `# nosec B608` безопасен (каждый интерполируемый идентификатор трассируется до литерала, пути от текста сообщения или `/set` в SQL-идентификатор нет), секреты fail-secure, `AdminOrOwner` fail-closed, Docker дропает привилегии, CI без `pull_request_target`, HTML `/pivo` экранирован во всех трёх путях с пользовательским вводом, `.env` никогда не коммитился. **Закрыто (PR #101):** NaN проходил валидацию **37 из 64** ручек — `parsed < min_v or parsed > max_v` для NaN всегда False, а дальше `min(1.0, nan)` возвращает 1.0 (мусор превращался в максимум: бот отвечает на каждое сообщение) либо `random.choices` падает на каждой генерации (бот немеет во всём чате после `/set`, отрапортовавшего успех); те же парсеры использует `load_settings`, так что дыра была и в `.env`. Добавлен гард финитности в оба места, вызывающих `float()`, плюс сеть по всему реестру в тестах. Там же `.dockerignore` синхронизирован с `.gitignore` (`data/` = дефолтный `DB_PATH` и точка бинд-маунта, `*.log`, `scrin/`, `eval_prod_baseline.json` попадали в слой образа через `COPY . .`; на dev-машине экспозиции не было — гигиена). **Закрыто (PR #102):** прогрев словарей pymorphy3 на старте в отдельном потоке (ленивая загрузка ~1 с срабатывала внутри async-пути ответа и блокировала event loop — «спало», пока `slot_mutation_probability` был 0, но 8485f44 запёк 0.15 в дефолты); ручки генерации сделаны keyword-only (20 позиционных параметров, включая соседнюю тройку `context_bias`/`context_start_bias`/`context_start_affinity`); три дословных копии блока оценки кандидата свёрнуты в `_evaluate_candidate`, из пролога вынесены `_read_mutation_inputs` и `_plan_length` (`generate_with_result` 390→335 строк). Поведение-нейтральность рефакторинга **доказана**, а не заявлена: 1000 генераций (4 сида × 250) на прод-копии дают побайтово одинаковый вывод до и после, SHA256 `b9717d9d…8bf6abf4` с обеих сторон — этот хеш пиннит порядок потребления RNG, любая перестановка бросков разошлась бы на первой генерации. **Не закрыто, заведено в [OPEN.md](OPEN.md):** O5 (`/set` правит процесс-глобальный `RuntimeState` по правам админа любого чата) и четыре Low в бэклоге — отсутствие верхних границ у части ручек, сырой `chat_id` в тексте `TelegramRetryAfter`, троттлинг только у `/clear`, `_admin_cache` без вытеснения. |
| 2026-07-21 | O1 закрыт — окно наблюдения за фичами «живости» (Stage 1–4) и пакетом «контекст+хаос» | Больше недели живой обкатки, замечаний по фичам под наблюдением нет (решение владельца 2026-07-21). Наблюдение завершено успешно. Открывает путь группе B (order_mix 0.5→0.75, slot_mutation→0.15, intonation→1.0) и `combo_mild`. **Важно для деплоя:** пакет «контекст+хаос» всё ещё живёт только как `/set`-оверлей — в реестре дефолты `context_jump_boost=1.0`, `verbatim_extension_share=0`, `order_mix_probability=0`; при запекании группы B в реестр надо заодно внести туда весь живой пакет, иначе рестарт даст нетестированную смесь. |
| 2026-07-22 | O5 закрыт интерим-фиксом: `/set` и `/setprob` сужены до `OWNER_ID` | Владелец решил не ждать per-chat скоупа `RuntimeState` (оценён в день-полтора, см. бэклог в OPEN.md) и закрыть вектор немедленно правкой авторизации. Новый фильтр `OwnerOnly` (`app/filters/owner_only.py`) заменил `AdminOrOwner` на обеих командах в `app/handlers/admin.py` — чат-админ (не владелец) больше не может задеть процесс-глобальный `RuntimeState` ни в одном чужом чате. `/clear` не тронут — он честно скоупится по `message.chat.id`, риска там не было. Текст отказа обновлён (`_reply_no_permission`), README и оба handler'а прокомментированы отсылкой к O5. Тесты: новый `TestOwnerOnly` в `tests/test_filters.py`, обновлён `test_set_denied_replies_with_explanation` (текст отказа больше не должен упоминать «админ»). Полный per-chat скоуп оверлеев остаётся в бэклоге OPEN.md как отдельная задача — это про удобство (владелец сможет тюнить один чат не трогая остальные), не про безопасность: без него дыры уже нет. |
| 2026-08-05 | `_admin_cache` получил вытеснение (Low из ревью 2026-07-21) | Единственное in-memory состояние проекта без политики роста: `filters/admin_or_owner.py` держал запись на каждый чат, где хоть раз вызывали админскую команду, и никогда не чистил — TTL отвечал за свежесть, но протухшая запись только перезаписывалась при следующем обращении, а забытый чат оставался навсегда. Закрыто тем же паттерном, что у соседей (`RuntimeState.prune_inactive`, `ThrottlingMiddleware._prune_state`): чистка по счётчику тиков (каждые 64 обращения или при переполнении), удаление протухших по TTL плюс потолок `ADMIN_CACHE_MAX_CHATS=512` с вытеснением самых давних. Вытеснение не меняет решения о доступе — чат просто перезапрашивает список админов, как после TTL; отказ остаётся fail-closed, неудачный запрос прав по-прежнему не кэшируется вовсе. Тесты: `TestAdminCacheEviction` в `tests/test_filters.py` (протухание, переполнение, ограниченность роста через реальный фильтр на 712 чатах, неизменность вердикта после вытеснения, fail-closed). План — `openspec/changes/evict-admin-cache`. |
| 2026-08-05 | `chat_id` внутри текста ошибок Telegram (Low из ревью 2026-07-21) | Инвариант «`chat_id` в логах маскируется» применялся к аргументам записи, а aiogram зашивает сырой идентификатор **внутрь** текста части исключений (`TelegramRetryAfter`, `TelegramMigrateToChat`), и `handlers/errors.py` логировал его через `%s`. Добавлен `mask_chat_ids_in_text` (`app/log_masking.py`), применяется ко всем `TelegramAPIError`, а не к двум известным классам — список принадлежит aiogram и может вырасти. Распознавание по форме, а не по фразе: любое отрицательное целое от 9 цифр (в плане было только `-100…`, но это форма супергруппы, а флуд-контроль обычной группы даёт `-123456789` — расширено при реализации, решение переписано в `design.md`). Функция никогда не бросает: обработчик ошибок — последний рубеж, без выведенного ключа ставится заглушка. Тесты: обе формы, два идентификатора в одной строке, отрицательные случаи (`retry after`, `error_code`, `message_id`), поведение без инициализации. PR #107. |
| 2026-08-05 | Верхние границы у пяти ручек (Low из ревью 2026-07-21) | `typing_max_ms` → `0..15000`, `min_cooldown_sec` / `reply_burst_boost_sec` / `reply_burst_suppress_sec` → `0..3600`, `min_tokens_for_model` → `0..1000000`; каждая цифра обоснована в реестре в терминах поведения бота, а не типа данных. Правило «заданный `typing_max_ms` главнее `TYPING_HARD_CAP_MS`» намеренно **не** пересматривалось — оно осталось достижимым через легальный конфиг (потолок выше и 4 с, и 5 с из пиннящего теста), просто его больше нельзя поднять до десяти минут. Живые значения `.env` проверены до выкатки — ни одно не выпало. Побочно: сторожевой тест нашёл ещё четыре ручки без потолка, они не чинились в этой правке (ревизия точечная по решению из `design.md`), а зафиксированы в `KNOWN_UNBOUNDED` и в OPEN.md. PR #109. |
| 2026-08-05 | Границы у оставшихся четырёх ручек — ревизия диапазонов закрыта полностью | Остаток от точечной ревизии выше: `hot_ngram_min_count` → `1..1000` (при тысяче ни одна n-грамма не укладывается в окно ретенции — локальные мемы молча выключаются), `rare_event_daily_cap` → `0..100` (сотня «сломов формы» в сутки — это не бюджет, а его отсутствие), `user_quirk_min_interactions` → `1..10000` (счётчик затухает за ~30 дней, тысячи недостижимы), `reply_context_max_tokens` → `2..300` (масштаб от соседнего `max_reply_tokens`; контекст длиннее самого длинного ответа матчеру бесполезен, но стоит латентности). `KNOWN_UNBOUNDED` в `tests/test_registry.py` сократился до одного `typing_min_ms`, и он теперь не просто объявлен транзитивно ограниченным, а проверен тестом: `try_apply` с гигантским значением отвергается кросс-полевым инвариантом против `typing_max_ms`. Сторож «у новой ручки должен быть потолок» остаётся. PR #110. |
| 2026-08-05 | Кулдауны на все пользовательские команды (Low из ревью 2026-07-21) | `COMMAND_COOLDOWNS_SECONDS` содержал одну запись (`clear`), при полностью готовой мидлвари — не хватало не механизма, а списка. Значения назначены **по классам**, чтобы новая команда получала окно по принадлежности, а не по вкусу: дешёвый ответ из памяти (`ping`, `help`, `config`, `pivo_privacy`) — 15 с, чтение из базы (`stats`) — 60 с, запись в базу (`pivo_on`, `pivo_off`) — 30 с, `clear` — час без изменений. Уведомление об отказе добавлено `pivo_on`/`pivo_off` (человек ждёт подтверждения, тишина читается как поломка), остальные молчат — ответ на отказ и есть тот трафик, который гасим. Права доступа не менялись: решение «`/config` без `AdminOrOwner`» касалось раскрытия данных и не пересматривалось. **`/pivo` намеренно оставлен без окна**: план предлагал дать ему кулдаун «для страховки», но `282051f` уже снимал троттлинг с `/pivo` по конкретной причине — мидлварь глушит вызов до хендлера, и человек вместо «лимит исчерпан» получал тишину; возврат окна вернул бы этот баг, а частоту и так ограничивает суточная квота. Причина записана в коде рядом со списком исключений (`COOLDOWN_EXEMPT_COMMANDS`, туда же owner-only `/set`, `/setprob`, `/pivo_check`). Тест-сторож: каждая команда из `TELEGRAM_COMMANDS` обязана иметь окно либо запись в исключениях, а пересечение списков пусто. PR #112. |
| 2026-08-05 | O5 follow-up закрыт: `/set` скоуплен по чатам | `RuntimeState` остаётся один на процесс, но поверх него появился **оверлей настроек по `chat_id`**: `/set` и `/setprob` пишут в чат вызова, `/set global …` меняет глобальное значение, `/set reset <key>` возвращает чат к глобальному, `/config` показывает эффективные для чата значения и отмечает переопределённые. Ключевое решение — **не трогать 58 мест чтения**: правка каждого была бы дорогой и ненадёжной (пропущенное место не падает, а тихо берёт глобаль), поэтому подстановку делает мидлварь `ChatSettingsMiddleware`, а `effective(chat_id)` для чата без оверрайдов возвращает **тот же объект**, а не копию. Живое состояние по чатам не разделяется: все поля состояния словарные, поэтому поверхностная копия делит их по ссылке — анти-повтор, настроение и счётчики продолжают копиться на одном стейте (есть тест). Кросс-полевые инварианты валидируются против значений конкретного чата: комбинация, допустимая с его переопределениями и недопустимая с глобалями, принимается. Оверлей вытесняется вместе с остальным состоянием чата (`forget_chat`, `prune_inactive`) — новый неограниченный рост не заведён. `/setprob` больше не присваивает поле напрямую, а идёт общим путём; на это есть тест-сторож, разбирающий AST всего `app/`. **Нейтральность доказана, а не заявлена:** 1000 генераций (4 сида × 250) на прод-копии дают побайтово одинаковый вывод до и после, SHA256 `5a72e2d4…9eb664b` с обеих сторон. Харнесс на этот раз закоммичен — `tools/generation_hash.py` (в PR #102 он был разовым и потерялся). PR #113. |
| 2026-08-05 | Интерим-ограничение O5 снято: админы чата снова могут настраивать свой чат | Ограничение `/set` и `/setprob` до `OWNER_ID` (2026-07-22) вводилось потому, что команды меняли **процесс-глобальный** `RuntimeState` — админ любого чата задевал все остальные. После скоупинга оверлеев (в тот же день, см. выше) вектор закрыт самим скоупом: скоупленная форма трогает только тот чат, где вызвана, а им админ и так управляет — может выгнать бота или стереть его данные через `/clear`. Поэтому скоупленные формы (`/set <key> <value>`, `/set reset <key>`, `/setprob <value>`) вернулись к `AdminOrOwner`, а глобальные (`/set global …`, `/setprob global …`) остаются `OWNER_ID`-only. Проверка владельца делается **внутри** хендлера, потому что область задаётся словом в тексте, а не отдельной командой. Важная деталь отказа: при попытке применить глобальную форму без прав значение **не** применяется и к чату вызова — тихая подмена области скрыла бы от вызывающего, что произошло не то, о чём он просил (на это есть тест). Решение владельца 2026-08-05; права не расширялись побочным эффектом рефакторинга, это был отдельный осознанный шаг. PR #118. |
| 2026-08-06/07 | Комплексный аудит: упрощение / безопасность / производительность / код-ревью | Тринадцать изменений, каждое отдельным коммитом. **Безопасность:** отказ `/pivo` по исчерпанной квоте перестал быть потоком сообщений; предел на подписчиков стал усечением с пометкой, а не отказом, ломавшим команду для всего чата навсегда; полная форма `/config` закрыта тем же кругом, который может эти значения менять; реальные `chat_id` убраны из отслеживаемых файлов и закрыты тестом-сторожем. **Ключ шифрования `/pivo` переведён на HKDF** с доменной меткой `pivo:fernet` вместо голого `sha256(secret)` — решение 2026-06-30 «оставить как есть» снято: миграции данных не потребовалось, чтение идёт `MultiFernet`, прежний ключ остался читающим, строки переходят на новый при очередной перезаписи профиля. Порог длины секретов поднят до 32 символов (ломающее для эксплуатации). **Производительность** на копии прода: генерация ответа 91.0 → 63.5 мс, чтение объёма модели (на каждое сообщение) 12.7 → 0.014 мс, выборка «горячих» n-грамм 40.8 → 0.42 мс, кэши обучения ~95 → 0.015 мс. **Упрощение:** ядро генерации развязано с инфраструктурой через порт чтения цепи; из фасада БД убраны сорок методов-делегатов; конвейер ответа (430 строк в одной функции) вынесен в сервис и разложен на три шага, ветки политики проверяются без моков Telegram. Документация переписана заново под правило «один факт — одно место»: значения ручек живут только в `.env.example`, который сверяется с реестром тестом. Нейтральность доказана хешем генерации: `5a72e2d4…9eb664b` не менялся ни на одном шаге. Тестов 878 → 941, покрытие 94%. PR #125. |
| 2026-08-07 | Обслуживание базы больше не стоит выученного сообщения | Найдено по ходу аудита: суточное затухание «личностных» счётчиков висело первой строкой внутри `save_message_and_update_model`, поэтому сбой обслуживания стоил сообщения — и не одного. Отметка о прогоне ставится только после успеха, значит условие «пора» оставалось истинным, и каждое следующее сообщение падало там же: пока причина не ушла, обучение стояло **во всех чатах сразу** (`decay_stale` не фильтрует по чату). Снаружи — «бот жив, просто перестал развиваться»; исключение вдобавок прилетало из `finally` обработчика и затирало исходную ошибку пути ответа. Самый вероятный сбой — занятая база (бэкап, `VACUUM`), а при `busy_timeout` 5000 мс каждый из трёх decay ждёт до 5 с на каждом statement под общей блокировкой: «не учится» превращалось в «залипает». Обслуживание вынесено отдельным шагом конвейера после обучения — свойство держится устройством кода, а не расстановкой `try`; планировщик стал отказоустойчивым с паузой 10 минут после сбоя (сутки заморозили бы окно «горячести» из-за одного бэкапа, повтор на каждом сообщении дал бы ретрай-шторм). Устойчивый сбой теперь доходит до владельца в личку: не раньше часа непрерывных неудач, напоминание не чаще раза в сутки, отдельное сообщение о починке — логи владельцу недоступны, без сигнала это замечалось бы неделями. PR #126. |

## Трек «живости диалога» — план 2026-07-02, выполнен полностью

| Этап | Что вошло | PR |
|---|---|---|
| Stage 1 | softmax-отбор кандидата вместо argmax, пулы fallback-фраз, задержка набора ∝ длине, casefold-матчинг контекста по умолчанию, вариатор финальной пунктуации | #50 |
| Stage 2 | анти-повтор полных ответов, режимы длины short/medium/long, анти-повтор шаблонов `/pivo`, временные вариации (ночь/пятница/понедельник) | #51, #52 |
| Stage 3 | настроение чата (M1), AI-директор вероятности ответа (M2), эмодзи-канал (M3), topic-drift jumps (M4) | #53, #54, #55 |
| Stage 4 | горячие n-граммы — «локальные мемы» (L1), редкие события и фальстарты (L3), причуды завсегдатаев (L2) | #57, #58, #79 |

L2 сделан не по исходному наброску: `chat_members` не трогали (присутствие там ≡
подписка на `/pivo`), имён не храним — только анонимный HMAC-счётчик обращений
(`chat_user_interactions`, миграция 014).

## Настройка генерации

| Проблема | Решение | PR |
|---|---|---|
| 69% ответа — дословная цитата корпуса, контекст-попадание 0.13 | Видимый контекстный старт + `verbatim_penalty` в скоринге: ответов ≥90% verbatim 33% → 5.5%, контекст-попадание → 0.42 | #61 |
| Ответы «не в тему» | Стемминг русской морфологии в IDF-релевантности, контекст-аффинные глобальные старты, обязательная «отсебятина» к корпусным ответам | #63, #66, #67, #68 |
| Бессвязные длинные ответы (18% победителей несли ≥2 джампа) | Кап в 1 джамп на ответ + гигиена сплайса: срез висящей запятой, никаких задвоенных коннективов | #64, #65 |
| Фрагментация модели по регистру («Пиво» ≠ «пиво») | `NORMALIZE_LOWER=true` по умолчанию, миграция 015 приводит уже накопленную модель к нижнему регистру | #80 |
| Устаревшие `@ник`и в упоминаниях `/pivo` | Профиль подписчика освежается по его сообщениям, не чаще раза в сутки | #80 |
| Длина ответа не зависела от длины вопроса | `LENGTH_CONTEXT_ADAPTATION`: короткое сообщение наклоняет выбор режима к short, длинное — к long (дефолт 1.0) | #81 |
| Мусор нумерованных списков в модели: схлопывание переводов строк превращало «5. хоссейн» в пару токенов «5 .», модель училась энумераторным коридорам и открывала ответы с «5.» | Два конца: `remove_list_markers` в `sanitize_text` режет маркеры («5.», «5)») в начале строк при обучении; миграция 017 вычищает уже выученное из starts/starts3/transitions/transitions3/hot_ngrams. Десятичные дроби («0 . 5») защищены проверкой следующего токена. Прод-копия: 100% энумераторов удалено, дроби целы; eval_prod 3×200 — якорность 0.265→0.287, остальное флэт | — |
| Контекстный якорь всегда открывал ответ — позиция контекста в ответе не варьировалась (идея «сегментация якоря» из свипа 2026-07-15) | Ручка `context_anchor_splice_probability` («прыжок наоборот»): с этой долей видимый якорь откладывается — проходка стартует глобально, а эмиссия якоря вклеивается позже тем же швом, что M4-прыжок (позиция — равномерный ролл по токен-бюджету, тупик цепи вклеивает раньше). Делит бюджет «один прыжок на ответ» с M4: висящий якорь подавляет глобальные прыжки, вклейка съедает слот; `start_source="context_spliced"`. A/B-замер на прод-копии (5 сидов × 200, база — боевой пакет): якорность 0.33→0.58/0.71/0.88 при ручке 0.25/0.5/1.0, новизна +0.03…+0.07, verbatim −0.02; цена — связочная доля 0.56→0.65/0.75/0.87 и латентность +14/+28/+67 мс; скорер сверх-выбирает spliced-кандидатов, дописки вытесняются (unknown-победители 60→13). Дефолт 0.25 (замер 2026-07-16; 0 = прежнее поведение, RNG байт-идентичен) | #92 |
| Эскалационные цепочки (бэклог): бот никак не «заводился» от серии обращений — `mention_ewma` питал только momentum директора M2 | Ручка `mood_mention_heated_share`: сглаженная доля обращений к боту, с которой `classify_mood` эскалирует чат в **heated** (randomness +0.5, flavor ×1.4, короче длина — готовые heated-модификаторы) даже при спокойном темпе/пунктуации. При alpha 0.3 три обращения подряд ≈ 0.66. Замер на реплее 1000 реальных сообщений (2026-07-16): порог 0.4 ⇒ 2 подряд, 0.6 ⇒ ровно 3 (обращения «через одно» — плато ~0.59, не поджигают; гаснет после 1 чужого сообщения), 0.8 ⇒ 5 (мёртв); фон без обращений — 0 ложных срабатываний. Heated-профиль генерации в eval — всё в шуме, кроме длины 11.0→10.7 (живой эффект — flavor/эмодзи/reply_probability, вне eval). Дефолт 0.6 (замер 2026-07-16; 0 выключает) | #93 |
| Мёртвый stem-тир контекстного матчинга (`FUZZY_CONTEXT_STEM`, #73) | Удалён вместе с `include_stem`/стем-индексом в `ContextStateMatcher`, ручкой в registry и стем-фикстурой eval. Три замера на копии прод-БД (4 сида × 300 генераций): (1) как есть — 0 стартов при `context_exact` ≈ 785 и `context_casefold` ≈ 50; (2) с поднятием stem выше casefold — 3–8 стартов и ни одной улучшившейся метрики (`context_anchored_win_rate` 0.293 → 0.284); (3) прямой зонд матчера — окон, где stem единственный, кто что-то нашёл, всего 0.36% (66 из ~18 тыс.), то есть потолок механизма меньше процента. После удаления eval даёт **побитово те же** метрики на всех сидах. Морфология осталась там, где работает: `context_start_affinity` и IDF-релевантность (`stem_token` жив) | #85 |
| Сломанная грамматика в `/pivo`: «Дорогой конченый коллектив подозрительные личности», «Так, конченые конченый состав чата» | Слот `{mentions_inline}` заполнялся именной группой, когда явных упоминаний не было, — а шаблон шапки уже несёт своё обращение и управляет слотом (приложение, вин.п., дат.п.). Список @-упоминаний грамматически непрозрачен, именная группа — нет: пул фолбэков удалён, слот стал необязательным. Заодно: тема (произвольный текст юзера) больше не склоняется шаблонами — только свободная позиция; `{time_phrase_soft}` без времени давал придаточное без запятой посреди предложения. Правила размещения слотов закреплены тестами-инвариантами | #84 |

## /pivo

- Рекурсивные саб-слоты и сезонные пулы (2026-07-16, бэклог): шаблон может ссылаться на пул (`PIVO_SUB_POOLS`), запись пула — на другой пул (глубина капнута, циклы отбрасываются); раскрытие идёт до подстановки пользовательского текста, так что скобки юзера не сканируются. Сезонные закрывающие строки (зима/весна/лето/осень) конкурируют за bottom-слот в том же flavor-ролле, что ночь/пятница/понедельник; нейтральный пул — фолбэк. Записи общих саб-пулов обязаны быть нейтральными к режиму (инвариант target-режима из #84).
- Мёртвая ручка `reply_context_last_tokens` удалена (2026-07-16): не читалась генерацией с `3b6f174` (phase 4.1d, найдено свипом 2026-07-15) — жила только в registry/Settings/RuntimeState и выводе `/config`. Живой `reply_context_max_tokens` не тронут.

## Документация и CI

- Документация сведена с 36 файлов до 7 (#81): аудиты и планы реализации свёрнуты в этот журнал, незакрытое — в [OPEN.md](OPEN.md). Попутно исправлено: `ARCHITECTURE.md` описывал `transitions3` как `(w1, w2) → w3` (на деле это `transitions`, а `transitions3` ключуется по `(w1, w2, w3) → w4`); из `GENERATION_PIPELINE.md` убрана устаревшая врезка, а `verbatim_penalty` вернулся в раздел скоринга.
- Красный CI после #80 (#81): bandit давал три Medium `B608` на миграции 015 — имена таблиц подставляются из литерала в том же модуле, помечено `# nosec B608`. Гейт security-шага — ноль Medium/High, поэтому падал именно он, а не тесты.
- bandit `B311` (некриптографический `random`) отключён в `[tool.bandit]` (#82): все 23 Low были на бросках кубика в генерации и в dev-скриптах, а криптография (`PivoSecurity`) случайность не использует вовсе — HMAC и ключ Fernet выводятся из секретов `.env`, IV Fernet берёт из `os.urandom`. Прогон стал 0/0/0, так что будущая Low-находка другого типа сразу видна. Для секретов — `secrets`, не `random`. `B608` (Medium) намеренно оставлен включённым: исключения точечные, по месту.
- Маркеры `# nosec` (#82): причина пишется строкой выше, сам маркер — голый (`# nosec B608`). Причина после id ломает подавление молча — bandit парсит хвост как список тестов.
- **O5 — тёплый путь сверяется с холодным через генерацию** (#164, закрыт 2026-08-23). Контракт §2 требует совпадения `generation_hash` тёплого пути с холодным, но единственный проверявший его гард по построению не мог: `tools/generation_hash.py` строит генератор на холодном старте, то есть сравнивает холодное с холодным. Цена известна — дефект `get_word_frequencies`/`pick_replacement` прожил до внешней разведки при зелёном гарде. **Живого дефекта на момент закрытия не было:** все три инкрементальных пути (`_fold_transition`, `_fold_start_row`, `apply_state_deltas`) уже держали порядок явно и с обоснованием; это гард против следующего, а не починка текущего. Из трёх рассмотренных подходов выбран не тот, что предлагала запись O5: второй проход хеша в гарде (+31 с к CI, замерено) и перечисление тёплых структур по одной отклонены в пользу **сквозного теста** — перечисление и подвело в прошлый раз. `tests/test_warm_cold_generation_equivalence.py` сравнивает текст ответа на прогретом кэше после дообучения против холодного чтения, не называя структур, и содержит две проверки против собственного вырождения (прогрев обязан закэшировать, дообучение обязано свернуться). **Охват проверен мутациями:** дописка вместо вставки по порядку в `_fold_transition` тест роняет (эти кэши не были покрыты ничем), снятие сортировки в `pick_replacement` — не роняет, словарь компактного корпуса не оставляет замене выбора; расширение словаря втрое не помогло и стоило пяти секунд. Тот путь закрыт прицельно тестом `test_warm_frequency_cache_draws_like_a_cold_read` — проверено той же мутацией. Граница двух тестов записана в §7 `CLAUDE.md`. Заодно отвергнута собственная идея «усилить» соседние `test_incremental_*_match_full_rebuild` сверкой порядка: порядок словаря частот контрактом **не является**, `pick_replacement` сортирует пул у потребителя — закрепление порядка противоречило бы решению W1-C. Попутная находка: `IsolatedAsyncioTestCase` жёстко включает asyncio-debug, а тот умножает цену каждого `await` в `aiosqlite` примерно на 15 (сборка снапшота 2.5 с против 39 с) — отсюда компактный корпус в тесте вместо eval-снапшота и 6 с вместо 102 с.
- **O6 — фикстуры runtime-состояния собираются из реестра** (#161, закрыт 2026-08-23). Пять мест строили `RuntimeState` перечислением полей руками — рукописные зеркала `RUNTIME_FIELDS`, расходящиеся с ним молча. Новая ручка роняла не тест правки, а всех, кто ходит через фикстуру: на M2R-900 сначала 71 падение с `TypeError: missing 1 required positional argument`, затем ещё 7 из двух других фикстур. Ловится только полным сьютом — падают файлы, которых правка не касается. Переведены на `**{spec.name: spec.parse(spec.default) for spec in RUNTIME_FIELDS}` (идиома уже жила в `tools/`), отклонения от дефолтов перенесены дословно; −427/+199. Три поправки к тому, что говорила запись O6: **корень был не в пяти файлах, а в `make_runtime_state`** — `test_handlers` туда делегирует и починился сам, а `test_fallback_phrases` оказался её дублем с расхождением ровно в двух значениях; **числа были занижены** — не 33/27/10/7 полей, а 76 и 68 при 97 в реестре, то есть недоставало 21 и 29 ручек, и расхождение было фактом, а не гипотезой; **`_fake_state` в `test_handlers` к этому классу не относится** — `MagicMock` отдаёт новой ручке мок-атрибут, а не `AttributeError`. Сторож `TestFixtureMirrorsTheRegistry` не удалён: у него сменился охраняемый инвариант — теперь ловит расхождение реестра с самим `RuntimeState`. Главный риск правки — сдвиг закоммиченных базлайнов `eval_generation`, которому недоставало 42 ручек; базлайны не сдвинулись, 42 добавленные ручки для синтетического eval инертны (почти все из `ReplyPipeline`, который eval обходит).
- CI: push-триггер снова только для `main` (2026-07-16, решение владельца — откат утреннего снятия фильтра): вариант «push на любую ветку» удваивал прогоны каждого PR (4 джоба × push + pull_request), двойной CI признан неприемлемой ценой. Ветки собираются через `pull_request`; известная цена — ветка без открытого PR не собирается (урок #80: открывать PR раньше). `concurrency` сохранён: повторные пуши в ветку отменяют устаревший прогон, `main` не отменяется.

## Прочее

- Источник истины по зависимостям — `requirements*.txt`/`.lock` (pip); `uv` — только локальный раннер (#78).
- `GEN_TRACE_LOG` включает трейс генерации независимо от `LOG_LEVEL` (#78).
- Доаудитные документы 2026-05/06 удалены 2026-07-04 — их выводы поглощены аудитом 2026-06-29.
