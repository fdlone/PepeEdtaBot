# Markov 2.0R — статус реализации

Трекер фаз по roadmap (`03_MARKOV_2_0R_IMPLEMENTATION_ROADMAP.md`). Обновляется
в конце каждой фазы. Решения по гейтам — только по отчётам в
`docs/eval_reports/` и порогам `tools/eval/eval_thresholds.yaml`
(предрегистрированы 2026-08-11, правка — отдельным коммитом с обоснованием).

| Фаза | Change | Статус | Итог |
|---|---|---|---|
| 0. Baseline + eval | `markov2r-phase0-baseline-eval` | **закрыта 2026-08-11** (PR #131, архив) | Аудит (`MARKOV_2_0R_PRE_IMPLEMENTATION_AUDIT.md`); раннер `tools/eval` (матрица C0–CF, промпты `308b7deaea0f`, bootstrap CI, CI-smoke); baseline C0 заморожен на `db_prod_copy` 2026-07-13: success 1.0, copy 0.222, affinity 0.280/0.209, циклы 0.0007; воспроизводимость бит-в-бит подтверждена дважды. Поправка протокола v1.0.1: temporal-метрики недоступны до Phase 3. |
| 1. Телеметрия + тень + кэш | `markov2r-phase1-telemetry-shadow-cache` | **закрыта 2026-08-11** (PR #132/#133, архив) | M2R-010: энтропия/ветвление/confidence пулов → трасса и `/stats`. M2R-020: теневой order-4 (estimator=window); **первые данные гейта Phase 7: 0.0% на 897 шагах** (вердикт с ≥1000). M2R-030: инкрементальная вкатка дельт в кэши вместо сброса (kill switch `markov_cache_incremental`); hit-rate 41% оффлайн. Нейтральность: generation_hash идентичен, eval-контент побайтно равен C0 (`eval_2026-08-11_phase1-gate.md`), p95 48 мс (бюджет 150). |
| 2. Entropy sampling | `markov2r-phase2-entropy-sampling` | **закрыта 2026-08-12 с отрицательным результатом** | M2R-100/110 реализованы, протестированы и **выключены по умолчанию** (`markov_entropy_temp_gain=0`, `markov_branching_degenerate_max=0` ⇒ бит-в-бит 1.x, хеш `5a72e2d4`). Гейт `phase2_entropy` (предрегистрирован до прогона) провален всеми шестью плечами сетки. Причина измерена, а не предположена: энтропия шага **бимодальна** — 78.8% шагов имеют ровно одно продолжение (выбор безальтернативен), 20.9% почти равномерны (степень там почти не имеет рычага), в середине 0.3%. M2R-110 не инертен, но торгует: при пороге 2.5 copy −0.039 и тематичность −0.040 (оба значимы) — гейт отвергает размен. Ответ на ADR-вопрос #2: ни одно направление. Отчёты: `eval_2026-08-12_phase2-grid.md`, `eval_2026-08-12_phase2-verdict.md`. |
| 3. Temporal layer (core) | `markov2r-phase3-temporal-layer-core` | **закрыта 2026-08-12: схема выкачена, blend выключен** | M2R-200 выкачен **включённым** — миграция 018 даёт цепи временную запись (4.0 мс на прод-копии, строки до миграции остаются с NULL: проштампованная дата неотличима от наблюдения). M2R-210 реализован и **выключен** (`markov_alpha_*=0` ⇒ бит-в-бит 1.x, хеш `5a72e2d4`). Гейт `phase3_temporal` (предрегистрирован до прогона) провален всеми девятью плечами сетки — не размен, а инертность: ни один интервал не исключает ноль. Причина измерена: на фикстуре **99.0% состояний order-3 имеют ровно одно продолжение**, а blend переваживает существующий выбор и не может создать кандидата. Телеметрия прогулок подтверждает независимо: короткий слой участвует на 11.2% шагов, но сдвигает распределение всего на 0.012–0.026 по total variation. Побочно: энтропия с blend'ом слегка **падает** (0.117 → 0.108), а не растёт — смешивание двух пиковых распределений заостряет. Цена обучения +0.36 мс/сообщение. Отчёты: `eval_2026-08-12_phase3-grid.md`, `eval_2026-08-12_phase3-verdict.md`. |
| 3b. Temporal calibration | `markov2r-phase3-calibration` (M2R-215) | ждёт окна накопления | Перезамер blend на живом корпусе, когда цепь наберёт собственные метки. Нужен **новый** предрегистрированный гейт. |
| 4. PMI + коллокации | `markov2r-phase4-pmi-collocations` | **код выкачен 2026-08-12, гейт ждёт ручной разметки** | M2R-300: анализатор (nPMI × support × recency, порог в SQL `HAVING`) в суточном maintenance, 37.7 мс на все чаты прод-копии (оценка была 41). M2R-310: meme-ordering `get_hot` за ручкой (0.18 мс против 0.14 частотного). M2R-320: скоринг кандидатов — бонус/штраф с гардом доступности из кэшированных пулов (D4), withheld считается отдельно. Все веса нейтральны ⇒ хеш `5a72e2d4` бит-в-бит. Автоматика гейта пройдена (C0 vs C3, 1500 ген/арм: copy −3.3 п.п. значимо, affinity-без-копий держится, p95 40 мс; repetition_rate 0.001→0.007 — вне гейта, записано); вердикт — `insufficient data` до раунда разметки. Отчёт: `eval_2026-08-12_phase4-c3.md`. |
| 5. Lexical anchoring | `markov2r-phase5-reverse-index` (M2R-400, влит) + `markov2r-phase5-seeded` (M2R-410/420, выкачен) → решение M2R-430 | **обе половины выкачены 2026-08-12; гейт ждёт накопленного df** | M2R-400: реверсные order-2 запросы — индексом над `transitions` вместо таблицы-дубликата из ТЗ §13 (согласованность по построению, откат `DROP INDEX`); `markov_token_df` + `n_docs` — инкрементальный df, бэкфилла нет намеренно. M2R-410: seed-score (nPMI × support × полоса ветвления, не max-IDF), двунаправленная сборка (хвост прямой цепью, голова реверсным индексом), seeded-кандидаты в пуле без приоритета за ручкой `markov_seeded_candidate_ratio` (дефолт 0). §9.7 candidate-IDF остаётся оконным (df идёт в выбор сида, design D6). Нейтрально: хеш `5a72e2d4` бит-в-бит. Эксперимент ADR-012; **гейт `phase5_promotion` честно отдаёт `insufficient data`, пока df оконный — вердикт требует прогона на проде с накопленным df (M2R-430)**. |
| 6. Anti-cycle + jumps | gated | ждёт данных | Гейт rate×harm; наблюдаемый rate 0.0007 при пороге 0.05 — пока и близко нет; harm требует ручной раунд (док 05 §5). |
| 7. Order-4 | gated | ждёт данных | Тень: 0.0% на 897 шагах при пороге 10% — ранний сигнал «закрыть без реализации»; вердикт при ≥1000 шагов. |
| 8. Реакции-бандит | эксперимент | не начата | Не заблокирована (aiogram 3.30 умеет реакции; нужен хендлер `message_reaction` + бот-админ в чате — аудит §5). |

## С чего начинать следующую сессию

0. **Фаза 5 выкачена целиком (обе половины), гейт M2R-430 ждёт накопленного
   df.** seeded-код за ручкой `markov_seeded_candidate_ratio=0`, генерация
   бит-в-бит. df-агрегат (миграция 020) копится с первого рестарта прода на
   новом коде — как у фазы 3, читателю нужно окно накопления, иначе IDF по
   пустоте. **Прод стоит перезапустить всем пакетом миграций** (013→020
   применяются одним заходом чисто, ~0.4–2.8 с на копии), чтобы `n_docs`/df и
   реестр коллокаций начали копиться. Через 2-4 недели — прогон
   `python -m tools.eval --db db_prod_copy/markov.db --label phase5-verdict`
   на накопленном df, решение M2R-430 (блок 10 tasks) строго по гейту.
1. **Фаза 4 ждёт людей, не кода.** Раунд разметки подготовлен:
   `rating_rounds/2026-08-12/rating_list.txt` (раздать оценщикам) и
   `rating_key.json` (остаётся у владельца) — процедура в
   `docs/MEME_RATING_ROUND.md`. После сведения ответов в
   `manual_rating.json`: `python -m tools.eval --db db_prod_copy/markov.db
   --manual-rating <папка>/manual_rating.json --label phase4-verdict`,
   затем блок 11 tasks.md (решение по дефолтам строго по гейту).
2. **Решение по выключенному коду фаз 2 и 3 принято 2026-08-12: оставить.**
   Обоснование — фаза 3 проверила единственную живую гипотезу напрямую: blend
   переводит **43% пулов, у которых есть выбор**, в полосу, где температурная
   ручка имеет рычаг (0 → 23 из 53). Но такие пулы — 1.0% всех шагов на
   фикстуре, поэтому адресуемая масса выросла с ~0% до 0.4%. Экстраполяция на
   полный корпус (20.9% многовариантных шагов) дала бы ~9% против измеренных в
   фазе 2 0.3% — **арифметика на допущении, а не замер**. Гипотеза сдвинулась в
   пользу кода, а стоимость хранения — ранний возврат с полным покрытием
   тестами. Разбор: `docs/eval_reports/eval_2026-08-12_phase3-verdict.md`.

   **Точка пересмотра — M2R-215, а не «когда-нибудь».** Если перезамер на живом
   корпусе не состоялся или снова провалился, blend удаляется **вместе с
   расширением строки пула** `(token, cnt)` → `(token, cnt, s_value,
   s_updated_at)`: колонки в БД нужны независимо (GC по возрасту, метрики
   свежести), а расширенная строка в портах, кэшах и сэмплере нужна только
   blend'у и является единственной сложностью, которую фаза 3 добавила в
   горячий путь. Перезамер — только с **новым** предрегистрированным гейтом,
   `phase2_entropy` и `phase3_temporal` израсходованы.
3. Окно накопления: цепь начинает копить собственные метки с первого сообщения
   после рестарта с миграцией 018 (архив change фазы 3 уже сделан в ветке
   фазы 4). Через 2-4 недели — `/opsx:propose markov2r-phase3-calibration`
   (M2R-215): перезамер blend на живом корпусе. То же окно нужно df-агрегату
   фазы 5 (миграция 020): IDF в M2R-410 бессмысленен, пока `n_docs` не набрал
   объём.
4. Фоновая гигиена: **сделано 2026-08-12 (PR #139)** — `aiogram 3.29.0`
   (отозван) → 3.30.0, транзитивные пины актуализированы, `actions/checkout`
   v4 → v7 (Node 24).

## Инструменты

- Полный прогон: `python -m tools.eval --db db_prod_copy/markov.db --label <тег>`
- Гард рефакторингов: `python -m tools.generation_hash --db db_prod_copy/markov.db`
- Смок (как в CI): `python -m tools.eval --smoke`
